# 水平传送带演示网页 · 优化需求 v3

## 1. 背景与目标
- **对标文件**：`horizontal-conveyor-belt.html`（水平传送带演示｜高中物理）。
- **在线地址**：https://wenqiaowuli.github.io/demo/horizontal-conveyor-belt.html
- **前置**：v1（滚筒/环形带面/机架/v-t 拆面板/s相对+Q/落下动画/离带/快捷键/g=10/aria）已上线；v2（物块默认左端、ST 图、8 辐条、擦痕、m 标签、倍速/L 滑条）已上线。
- **目标（v3）**：基于 v2 做以下改动，并**对齐三页标准骨架**（斜抛/平抛/竖直上抛）。
- **参照**：`oblique-throw-motion.html`、`projectile-motion.html`、`vertical-throw-demo.html`——`主画布 → 图表行 → control-panel(左参数卡 | 右数据卡) → 操作按钮 → 图例`。

### 划痕物理分析（编码依据）
1. **划痕方向**：由相对速度 $v_{rel} = v_{物块} - v_{带}$ 决定。$v_{rel}>0$ 时物块相对带面向右滑，划痕在带上向右延伸；$v_{rel}<0$ 时向左延伸。带坐标 $q = x - v_{带}\cdot t$，$\dot{q} = v_{rel}$，划痕区间 $[q_{start}, q_{end}]$ 的延伸方向与 $v_{rel}$ 一致。
2. **划痕方向与摩擦力方向**：**相反**。滑动摩擦力作用在物块上，方向与 $v_{rel}$ 相反（$f = -\mu mg \cdot \text{sign}(v_{rel})$）；划痕是物块在带面上刮过的轨迹，方向与 $v_{rel}$ 相同。物理直觉：摩擦力把物块往带运动方向拉，而物块在带上刮过的痕迹往反方向延伸。
3. **划痕长度**：等于相对滑动距离 $s_{rel} = \int |v_{rel}|\,dt = \frac{(v_0 - v_{带})^2}{2\mu g}$。带坐标区间长度 $|q_{max} - q_{min}| = s_{rel}$。代码中 `state.slip` 已正确累计该值，`addScratch` 记录区间逻辑正确。

## 2. 改动清单

### L1 布局对齐标准骨架
- DOM 顺序：`h1 → canvas-wrap → charts-row → control-panel → actions → legend`
- `control-panel`：CSS grid 2 列（左参数卡 + 右数据卡），`gap: 14px`；移动端 `<720px` 变单列堆叠。
- **去掉中间结论部分**：原 phase 区（"物块向内传来…"中文长句）删除；阶段信息仅保留在数据卡中。

### L2 参数卡（左列，标题"参数设定"）
- 控件行：
  - 小方块初速度 v₀ 滑条（−6 ~ +6 m/s，步长 0.1，默认 0），正负表示方向
  - 小方块起点位置按钮（"⬒ 左端" / "⬒ 右端"），仅控制 `state.startSide`，不自动改动其他参数
  - 传送带速度 v带（**只读显示 `id="vbv"`**，跟随 vbSize 滑条实时更新；该元素必须存在于参数卡）
  - 带速大小滑条 `id="vbSize"`（−6 ~ +6 m/s，步长 0.1，默认 3），**正负表示方向**（正 = 向右，负 = 向左，0 = 静止）
  - 动摩擦 μ 滑条（0.05 ~ 0.8，步长 0.01，默认 0.30）
  - 重力 g 滑条（5 ~ 15，步长 0.1，默认 10）
  - 带长 L 滑条（5 ~ 40 m，步长 1，默认 24）
  - 播放速度滑条（0.25 ~ 4×，步长 0.25，默认 1×）
- 底部 `fixed-note`（公式说明框）：
  - 滑动：`f = μmg`，`a = μg`（方向与相对速度相反）
  - 相对位移：`s相对 = (v₀ − v带)² / (2μg)`
  - 摩擦生热：`Q = μmg · s相对`
  - 共速后：`v = v带`，`a = 0`，匀速
  - 正方向：物块位移向右为正；传送带位移 v带>0 向右
  - 参数说明：μ（动摩擦因数）、m（物块质量，取 1 kg）、g（重力加速度）、v₀（初速度）、v带（传送带速度）、L（带长）

### L3 实时数据卡（右列，标题"实时数值"）
- 3 列 grid（移动端 <560px 变 2 列）
- 数据项（9 格，小卡片 `.data-item`，span 标签名 + b 数值）：
  1. 物块速度 v（`id="vNum"`）
  2. 相对带速 v−v带（`id="vrel"`）
  3. 摩擦力 f（`id="fNum"`）
  4. 加速度 a（`id="aNum"`）
  5. 已用时间 t（`id="tNum"`）
  6. 相对滑动距离 s相对（`id="sNum"`）
  7. 摩擦生热 Q（`id="qNum"`）
  8. 带速 v带（`id="vbv2"`，青 `cyan`）
  9. 运动方向 ←/·/→（`id="dirText"`）
- **`dirText` id 全局唯一**（参数卡不得再出现第二个 `id="dirText"`）

### L4 传送带方向由 vbSize 正负控制
- **（替代原方向三按钮）**：带速大小滑条 `vbSize` 范围 −6 ~ +6，正负直接表示方向
- `vBelt = vbSize`（直接读取带符号的值），无需单独的方向按钮
- 只读显示 `id="vbv"` 实时同步 `v带` 的值（含正负号和方向文字）

### L5 物块开始位置固定
- 固定两种起点：最左端（默认，`x0 = −(L/2−1)`）或最右端（`x0 = +(L/2−1)`）
- 不随 v₀ 自动判断；由情景按钮（起点左端 / 起点右端）设定
- `state.startSide` 字段：`'left'` 或 `'right'`

### L6 去掉落下动画
- 清除 `state.dropP` 及相关逻辑
- 物块直接出现在设定起点，无初始下落过程

### L7 轮毂空心
- 当前实心轮毂 → 外圈 rim（R−6）暗腔填充 + 内圈（R−8.5） + 中心细轴（r=3）
- 视觉：空心圆环感，中心空出

### L8 ST 图更明显
- 纵轴动态缩放：`yMin = 0`，`yMax = max(sMax×1.2, 1)`（sMax = max(物块位移, 带位移)）
- 零线（s=0）加粗高亮（`lineWidth: 1.5`，色 `rgba(255,255,255,0.55)`）
- 两线阴影（=相对位移）透明度加深
- **VT 图与 ST 图均使用实线**：物块速度/位移 橙实线 `#ffaa44`，带速/带位移 青实线 `#44ddff`（不再使用虚线）

### L9 去掉底部中文长句提示
- 删除 phase 区的"物块向内传来，向左滑 / 向右滑 / 已离带"提示
- 阶段信息由数据卡实时数值体现

### L10 初速度可调
- 参数卡中保留 v₀ 滑条（−6 ~ +6 m/s），可自由设置（含正负）

### L11 传送带速度 v带 可调
- 带速大小滑条 `vbSize`（−6 ~ +6 m/s，步长 0.1，默认 3），正负表示方向
- `v带 = vbSize`（直接读取带符号的值）；只读显示 `vbv` 实时同步
- 情景按钮联动：起点左端 → `vbSize = 3`（向右）；起点右端 → `vbSize = -3`（向左）

### L12 传送带长度 L 实时更新图形
- 改 L 滑条后（idle/done 状态），画布**立即重绘**：pix 缩放、0/L 刻度、放上点、出料箭头位置跟随变化
- 小模块起点随 L **自适应端部**：`x0 = ±(L/2 − 1)` 实时重算，始终保持在新 L 的左端或右端，不跳至其他位置
- 运行中 L 滑条仍禁用（安全限制）

### L13 重置按钮在 done 态必须生效
- 物块滑离带端或随带到达带端后，`phase='done'`，`bStart.textContent='已离带'`，`bStart.disabled=true`
- 点击「重置」后必须无条件恢复：
  - `phase = 'idle'`
  - `offbelt = false`
  - `x = x0`，`v = v0`，`t = 0`，`slip = 0`，`tSync = null`，`scratches = []`，`trace = [[0, v0, 0]]`
  - `bStart.disabled = false`，`bStart.textContent = '▶ 开始'`
  - `bPause.disabled = true`，`bContinue.disabled = true`
  - 所有滑条恢复可用
  - `drawAll()` + `updateHud()` 确保物块在 `x0` 位置可见
- 键盘 `R` 同样生效（已在 keydown 中调用 reset()，无需额外修改）

### L14 done 态运动立即停止
- 物块越界（`px > w+80 || px < -80`）触发 `phase='done'` 后，`loop()` 下一帧不再执行 `step()`，物块位置冻结
- 不得出现物块继续飘出屏幕或画布空白

### L15 修复 `vbv` 空引用（页面崩溃）
- JS 第 421/1080 行引用 `$('vbv')`，但 HTML 参数卡删掉了 `id="vbv"` 只读显示元素 → `null.textContent` 抛 TypeError → 初始 `readControls(); reset();` 崩溃 → **页面加载后传送带/物块都不显示**
- 修复：参数卡恢复 `id="vbv"` 只读显示元素（L2 已要求），或 JS 中删除 `vbv` 引用（数据卡 `vbv2` 已显示带速）

### L16 修复 `dirText` 重复 id
- 参数卡（原 320 行）与数据卡（376 行）都有 `id="dirText"` → `$('dirText')` 只取第一个，数据卡方向读数不更新
- 修复：参数卡删除 dirText（方向由 vbSize 正负控制）；数据卡 `id="dirText"` 全局唯一

### L17 情景按钮点击后必须 reset()
- 起点左端/右端情景按钮设置参数后必须调用 `reset()`（当前只调 `setDirBtn()` 不重置模拟状态）
- 否则运行中/结束态点击情景按钮，物块不归位，`offbelt`/`trace` 等残留

### L18 `updateL()` 必须完整重置模拟状态
- 改 L 滑条时（idle/done 态）除 `x0/x/v/trace` 外，还需重置 `offbelt=false`、`slip=0`、`tSync=null`、`scratches=[]`、`prevQ=x0`、`beltClock=0`、`lastTs=0`
- 否则从 done 态改 L 后 `offbelt` 仍 true，物块画不出来 / 擦痕残留

### L19 `reset()` 起点按钮状态恢复
- `reset()` 恢复起点按钮时，按 `state.startSide` 禁用对应按钮（left 则禁用 startLeft，right 则禁用 startRight）
- 不得把两个按钮全部启用后无任何当前起点指示

### L20 划痕位置随带面平移
- 当前代码 `drawScene()` 中擦痕屏幕坐标 `G.xL + (L/2 + q) * G.pix` 缺少带面平移量 → 擦痕钉在屏幕上不动
- 正确位置：`x_scratch = q + v_带 · beltClock`（带坐标 + 带面随时间的平移）
- 屏幕坐标：`G.xL + (L/2 + q + v带 * beltClock) * G.pix`
- 效果：擦痕随带面一起漂移（带向右则擦痕向右缓慢移动），共速后擦痕定格在带上不再移动

### L21 ~~场景按钮已删除~~
- 原"起点左端/右端"场景按钮（bScenario1/bScenario2）已删除
- 起点位置完全由参数卡中的"小方块起点位置"按钮（startLeft/startRight）控制
- 不再提供参数预设快捷方式；用户手动设置 v₀、v带等参数即可

### L22 带速大小滑条独立且默认值明确
- 带速大小滑条 `vbSize`（−6 ~ +6 m/s，步长 0.1，**默认 3**）已在 L11 中定义
- 参数卡中只读显示 `vbv` 实时同步 `v带 = vbSize` 的值（含正负号）
- 滑条正负表示方向：正值向右、负值向左、零静止

### L23 VT/ST 图以传送带方向为正方向
- 以传送带初速度方向为正方向：v带符号 × `sign(v带)`
- VT 图：物块速度 v 和带速 v带 均以传送带方向为正（带速始终显示为正值）
- ST 图：物块位移 s 和带位移 s带 均以传送带方向为正（带位移始终为正值）
- 效果：无论物块从哪端出发、带向哪边运动，两图曲线都在正半轴，不会出现负值截断
- y轴范围：VT 图保持 ±7 m/s（覆盖 v₀ 和 v带 的极端组合）；ST 图从 0 起算

## 3. 保持不变的 v2 行为
- 8 辐条滚筒、双轮缘、旋转亮斑
- 带面红色滑动擦痕（长度=s相对，共速后定格）
- 物块 m 标签
- 带长刻度/放上点/出料箭头
- 播放倍速滑条（0.25–4×）、带长 L 滑条（5–40 m）
- ~~情景按钮（起点左端 / 起点右端）~~（已删除，由起点位置按钮替代）
- g=10、快捷键（Space/R）、aria-live、dead code 清理
- v-t/s-t 双面板、两线阴影=相对位移、共速竖线

## 4. 验收标准
- [ ] 整体布局：`主画布 → 图表行 → control-panel(左参数卡 | 右数据卡) → 操作按钮 → 图例`，移动端单列。
- [ ] 参数卡（左）：v₀ 滑条（±6）、起点位置按钮（左端/右端）、**带速只读显示 `vbv`**、**带速大小滑条 `vbSize`**（−6–+6 m/s，±表示方向）、μ 滑条、g 滑条、L 滑条、倍速滑条 + fixed-note 公式框。
- [ ] 数据卡（右）：9 项实时数据（3 列 grid），含带速 `vbv2` 与方向 `dirText`。
- [ ] 无中间结论部分（phase 区已删除）。
- [ ] VT/ST 图均使用**实线**：物块橙实线 `#ffaa44`，带青实线 `#44ddff`（无虚线，共速竖线除外）。
- [ ] 带速大小滑条（−6–+6 m/s，±表示方向）可自由调整；只读显示 vbv 实时同步 v带 = vbSize。
- [ ] **页面加载即正常显示传送带与物块**（无 `vbv` 空引用崩溃）。
- [ ] **`dirText` id 全局唯一**（参数卡无重复）。
- [ ] **`updateL()` 完整重置**：offbelt/slip/tSync/scratches/prevQ/beltClock/lastTs 均清零。
- [ ] **`reset()` 按 `state.startSide` 恢复起点按钮禁用状态**。
- [ ] **划痕随带面平移**：擦痕位置 = 带坐标 q + v带·beltClock，带向右时擦痕向右漂移，共速后定格。
- [ ] **VT/ST 图以传送带方向为正方向**：带速始终为正值；物块从右端出发 + 带向左时，两图曲线仍在正半轴。
- [ ] **场景按钮已删除**：参数卡中无 bScenario1/bScenario2 按钮。
- [ ] **物块起点独立控件**：参数卡中有左端/右端互斥按钮（或 radio），仅控制 startSide，不改动其他参数。
- [ ] 起点固定左端（默认）或右端，不随 v₀ 变化。
- [ ] 无落下动画，物块直接出现。
- [ ] 轮毂空心（暗腔 + 双环 + 细轴）。
- [ ] ST 图纵轴从 0 起算，零线加粗高亮，两线间距可见。
- [ ] 改 L 后画布立即重绘（pix/刻度/放上点/出料箭头跟随）；小模块起点随 L 自适应端部（x0 = ±(L/2−1)）。
- [ ] 物理逻辑正确：s相对、Q、共速时间、离带时间解析解正确。
- [ ] 运行中滑条禁用；空格/R、aria、g=10 不回归。
- [ ] done 态点击「重置」按钮后：phase='idle'、offbelt=false、物块在 x0 位置可见、按钮恢复「▶ 开始」、滑条恢复可用、可重新开始演示。
- [ ] done 态物块位置冻结，不再飘出屏幕。
- [ ] 桩测试全过（含布局结构、v带大小调节、L实时更新、左右端自适应、VT/ST实线+正方向、倍速、双向 v₀、暂停/继续/快捷键、done 态重置恢复、vbv 无崩溃）。
