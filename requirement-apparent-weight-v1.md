# 需求：超重和失重演示网页

## 目标
- 新建网页：`apparent-weight-demo.html`（超重和失重演示｜高中物理）
- 风格参照：`vertical-throw-demo.html`（竖直上抛运动演示）与 `free-fall-motion.html`（自由落体运动演示）——深色主题、上中下三段式布局
- 物理内容：人教必修第一册 4.6 超重和失重——电梯升降全程中体重计视重的变化，理解视重与实重、超失重取决于加速度方向而非速度方向
- 交付目录：`/home/wq/physics-work/apparent-weight-demo.html`
- 在线地址（部署后）：`https://wenqiaowuli.github.io/demo/apparent-weight-demo.html`
- 交付时同步：将页面复制到 `_repo` 仓库，并在 `_repo/index.html` 导航页新增本演示卡片（系列惯例）

---

## 一、视觉风格（对齐竖直上抛 / 自由落体）
1. 深色主题：`#121212` 背景、白字、`system-ui` 字体。
2. 主画布：深蓝底 `#0b1828`、蓝色边框 `#3388dd`、圆角，位于页面顶部整宽（880:520 比例自适应）。
3. 面板：蓝调半透明分组（`rgba(255,255,255,0.05)` + 细边框），主色 `#3388dd`。
4. 按钮：主色 `#3388dd`，带 hover / disabled 态。
5. 图表渲染风格与系列 `drawChart` 一致：深色网格 `#1f2d3d`、刻度文字 `#8899aa`、当前点 `#ff5555`、游标半透明蓝。
6. DPR 高清画布 + ResizeObserver 响应式；图表区宽屏并排、窄屏单列；图表面板比例 20:9。
7. 键盘快捷键：**空格** 播放/暂停（toggle）、**R** 重置；焦点在输入框/按钮时不触发。
8. 无障碍：按钮/滑条带 `aria-label`，实时数值区 `aria-live="polite"`。

## 二、页面布局（上中下三段式）
- **第 1 段（上部）**：主场景画布——电梯井道场景（整宽）。
- **第 2 段（中部）**：主画布正下方并排两张副图——左 v-t 图、右 F-t（视重-时间）图，实时延伸并带当前点标记，外框与主画布一致（蓝色 `#3388dd`）。
- **第 3 段（下部）**：图表下方左右两面板并排——左侧参数设定面板、右侧实时数值面板。
- **最底部（整宽）**：控制按钮行，共 3 个按钮：开始 | 暂停/继续 | 重置；按钮行之下为图例（legend）。
- **图例内容**：电梯轿厢、简化人形（站在体重计上）、视重读数气泡（颜色随状态变化）、红色速度矢量 v、橙色加速度矢量 a、楼层线与左侧高度标尺、图中 mg 实重参考虚线、当前点。

## 三、物理模型与正方向约定
1. 模型：人（质量 m）站在电梯内体重计上，体重计读数 = 支持力 F（视重）；实重 G = mg 恒定不变。
2. **重力加速度 g 固定为 10 m/s²**（常量，无控件，说明框注明）——与系列页统一。
3. 正方向：**竖直向上为正**（与竖直上抛页统一）→ v 带符号、a 带符号。
4. 核心规律：**F = m(g + a)**（a 为竖直方向加速度，向上为正）。
   - a > 0（加速度向上）→ F > mg，**超重**；
   - a = 0 → F = mg，**不超不失**；
   - a < 0（加速度向下）→ F < mg，**失重**；
   - a = −g → F = 0，**完全失重**（自由落体）。
5. **教学要点（本页核心）**：超重/失重看**加速度方向**，与速度方向无关——上升过程既有超重（加速）也有失重（减速）。在说明框明确注明。
6. 物理约束：电梯靠钢缆悬挂，a 上限取 g（= 10 m/s²），保证 F ≥ 0 恒成立；a = g 时下行加速/上行减速阶段即完全失重。

## 四、运动脚本（一次"往返全程"自动编排）
| 阶段 | 时长 | 加速度 a（向上为正） | 速度 v | 视重 F | 状态 |
|------|------|---------------------|--------|--------|------|
| P1 加速上升 | t₁ = v_max/a | +a | 0 → +v_max | m(g+a) | 超重 |
| P2 匀速上升 | t₂（可调） | 0 | +v_max | mg | 正常 |
| P3 减速上升 | t₁ = v_max/a | −a | +v_max → 0 | m(g−a) | 失重 |
| P4 顶层停靠 | 0.8 s（固定） | 0 | 0 | mg | 正常 |
| P5 加速下降 | t₁ = v_max/a | −a | 0 → −v_max | m(g−a) | 失重 |
| P6 匀速下降 | t₂ | 0 | −v_max | mg | 正常 |
| P7 减速下降 | t₁ = v_max/a | +a | −v_max → 0 | m(g+a) | 超重 |
| P8 结束 | — | 0 | 0 | mg | 已返回（ended） |

- 总时长 T = 4t₁ + 2t₂ + 0.8 s（加减速共 4 段）；上升总高度 H = v_max·(t₁ + t₂)。
- F-t 曲线呈对称"山峰-平原-山谷"形状，直接对应教材图形。
- 默认参数（a=2、v_max=6、t₂=2）下：t₁=3.00 s，T=16.80 s，H=30.0 m（约 10 层楼）。

## 五、可调参数
| 参数 | 范围 | 步长 | 默认值 | 说明 |
|------|------|------|--------|------|
| 加速度大小 a | 0.5 ~ 10 m/s² | 0.5 | 2 | 各加/减速阶段共用；上限 = g，保证 F ≥ 0 |
| 最大速度 v_max | 1 ~ 10 m/s | 0.5 | 6 | 匀速阶段速度；决定 t₁ 与行程高度 |
| 匀速时长 t₂ | 0 ~ 6 s | 0.5 | 2 | 匀速阶段持续时间 |
| 人的质量 m | 40 ~ 100 kg | 1 | 60 | 同时影响实重 G 与视重 F 的数值 |
| 重力加速度 g | 固定 10 m/s² | — | — | 常量，无控件 |
| 播放速度 | 0.1 ~ 2× | 0.1 | 1 | 行程较长（默认 T≈16.8 s），默认 1×；运行中实时可调 |

- 运行/暂停中禁用 a、v_max、t₂、m 滑条；播放速度实时可调（不改变物理状态）。

## 六、主场景演示窗口
1. 竖直井道：左侧高度标尺（0 ~ 1.05H，自适应取整刻度）、简式楼层线（每 3 m 一层，右侧标 1F、2F…）、地面线。
2. 电梯轿厢：圆角矩形沿井道移动（位置 = 当前高度），**比例 96×84 px（高 > 窄，接近真实轿厢竖长比例）**；轿厢内画简化人形剪影（垂直居中），站在体重计（小矩形，贴地板）上。
3. **体重计读数气泡**：轿厢旁放大的读数气泡"F = 720 N"，颜色随状态变化——超重橙红 `#ff9933`、正常白 `#ffffff`、失重蓝 `#66aaff`、完全失重青 `#33ddcc`；同时给出与实重对比（如 F/G = 1.2 倍）。
4. 矢量：轿厢旁红色 v 矢量（方向随运动方向，长度 ∝ |v|，像素上限）；橙色 a 矢量（方向随 a 符号，长度固定；a=0 时不画或标 a=0）。
5. 实重参考：轿厢一侧画 G = mg 恒定参考短刻度/说明（视重对比基准）。
6. 状态提示：静止（idle）→ 运行中（阶段名）→ 已返回（ended）。

## 七、图表窗口
- **v-t 图**：梯形折线（0→v_max→0→−v_max→0），带当前时刻游标、当前点圆点、数值气泡（带符号）；横轴 0 ~ T 固定全程，纵轴对称 ±(1.05 v_max 取整)。
- **F-t 图（核心图）**：分阶段水平/斜坡线段——m(g+a) 与 m(g−a) 之间切换，叠加 **G = mg 恒定虚线参考**（`#33ddcc` 或灰色虚线）与图例标注"实重 G = mg"；纵轴 0 ~ m(g+a)·1.05；超重段/失重段可着色区分（橙/蓝）。
- 两图随时间推进实时延伸，结束后定格完整曲线；坐标轴：横轴 t/s，纵轴 v/(m/s)、F/N，含网格与刻度。

## 八、参数设定面板（左卡）
- 加速度 a、最大速度 v_max、匀速时长 t₂、质量 m、播放速度 滑条
- 公式说明框（fixed-note）：
  - 视重公式：F = m(g + a)，a 竖直向上为正；g = 10 m/s²（固定）
  - 超重 ⇔ 加速度向上；失重 ⇔ 加速度向下；**与速度方向无关**
  - 实重 G = mg 恒定不变，变的只是体重计读数（视重）
  - a = g 时 F = 0，即完全失重（如自由落体）
  - 阶段序列：加速↑(超重)→匀速↑→减速↑(失重)→停靠→加速↓(失重)→匀速↓→减速↓(超重)

## 九、实时数值面板（右卡，实时更新）
| 名称 | 含义/公式 |
|------|-----------|
| 时间 t | 当前时刻 |
| 运动阶段 | 加速上升 / 匀速上升 / … / 已返回 |
| 速度 v | 带符号（向上为正），梯形分段 |
| 加速度 a | 当前阶段 a（带符号） |
| 高度 y | 当前离地高度（分段公式） |
| 实重 G = mg | 恒定 |
| 视重 F | F = m(g+a)，体重计读数（a=g 时为 0） |
| F/G | 视重与实重之比（超重 >1 / 失重 <1 / 完全失重 0） |
| 超失重状态 | 超重 / 正常 / 失重 / 完全失重（span-full，颜色同气泡） |

## 十、操作按钮（3 个，底部一行：开始 | 暂停/继续 | 重置）
沿用系列合并按钮约定，状态矩阵：

| 运行状态 | 开始按钮 | 暂停/继续按钮 | 重置按钮 |
|---------|---------|--------------|---------|
| 初始/重置后（idle） | 启用，显示"开始" | 禁用，显示"暂停" | 启用 |
| 运行中（running） | 禁用，显示"运行中…" | 启用，**显示"暂停"** | 禁用 |
| 暂停中（paused） | 启用，显示"重新开始" | 启用，**显示"继续"** | 启用 |
| 结束（ended） | 启用，显示"重新开始" | 禁用，显示"暂停" | 启用 |

- 点击"暂停/继续"：running → 暂停（文字变"继续"）；paused → 恢复（文字变"暂停"）。
- 运行/暂停中禁用物理参数滑条；播放速度滑条实时可调。
- 键盘：空格 = 播放/暂停、R = 重置。

## 十一、标题 / 元信息
- 标题：`超重和失重演示｜高中物理`
- 加 description meta，描述页面功能。

---

## 十二、物理公式（实现参考，勿改动）
- 视重：F = m·(g + a)，a 竖直向上为正；实重 G = m·g 恒定
- 各阶段运动学（t 为进入该阶段后的局部时间）：
  - 匀加速段：v = ±a·t，y = ±½a·t²（在已有高度上累加）
  - 匀速段：v = ±v_max，y = ±v_max·t
  - 匀减速段：v = ±(v_max − a·t)，y 在该段初速度上按 v₀t − ½a t² 累加
- t₁ = v_max / a；T = 4t₁ + 2t₂ + 0.8；H = v_max·(t₁ + t₂)
- g = 10 m/s² 固定；a ≤ g 保证 F ≥ 0

## 十三、验收标准
1. 页面深色主题，与系列风格一致；布局为：主画布 → v-t / F-t 双图 → 左参数 / 右数据 → 底部 3 按钮 → 图例。
2. 默认参数（a=2、v_max=6、t₂=2、m=60）下：t₁=3.00 s、T=16.80 s、H=30.0 m、G=600 N、F 峰值 720 N、F 谷值 480 N，各读数与公式自洽。
3. 运动脚本按 P1~P8 顺序执行：v-t 为对称梯形，F-t 为"720→600→480→600→480→600→720"对称序列；阶段名与状态文本正确切换。
4. 状态判定正确：P1/P7 超重（橙）、P3/P5 失重（蓝）、匀速/停靠正常（白）；a=g 时失重段显示"完全失重"、F=0（青）。
5. 主画布：轿厢沿井道按阶段运动，人形立于体重计，读数气泡数值与颜色实时更新；v 矢量随速度大小/方向变化，a 矢量随阶段方向变化。
6. F-t 图带 G=mg 虚线参考；两图实时延伸 + 游标 + 当前点 + 气泡，结束后定格。
7. 5 个参数滑条可调且重算 t₁/T/H/G/F 预估读数；运行/暂停中禁用，播放速度实时可调。
8. 3 个按钮状态矩阵与"暂停/继续"切换逻辑正确；空格 / R 快捷键可用且不与输入框冲突。
9. 图例完整；按钮与滑条带 aria-label，数值区 aria-live。
10. 交付时已同步 `_repo` 仓库并在 `index.html` 增加卡片（置于"自由落体运动演示"之后）。

## 十四、参考文件
- 风格参照：`/home/wq/physics-work/vertical-throw-demo.html`、`/home/wq/physics-work/free-fall-motion.html`
- 按钮合并约定：`/home/wq/physics-work/_repo/requirement-horizontal-linear-motion-v2-button-merge.md`
- 交付目录：`/home/wq/physics-work/apparent-weight-demo.html`
