# 需求：超重和失重演示网页 v2（画面内容修订）

> 在 v1（`requirement-apparent-weight-v1.md`）基础上修订主画布显示内容与实时数值面板措辞。未提及的部分（物理模型、行程脚本、参数、图表、按钮、快捷键、布局骨架）维持 v1 不变。

## 一、修订内容

### 1. 主画布：删除速度图示
- 不再显示红色速度矢量 v，也不显示 "v = 0" 标注。
- 速度的大小和方向在 v-t 图与实时数值面板中查看，画布不重复。
- 图例同步删除 "→ 速度 v" 一项。

### 2. 主画布：显示加速度的大小和方向
- 保留橙色加速度矢量（方向随 a 符号：竖直向上 / 向下）。
- 同时标注大小数值（随阶段实时更新）：
  - a > 0：`a = +2.0 m/s²`
  - a < 0：`a = -2.0 m/s²`
  - a = 0：不画矢量，标 `a = 0`（含待机与匀速阶段）
- 图例保留 "→ 加速度 a"。

### 3. 实时数值面板：示重
- 数据项标签 "视重 F（体重计）" 改为 "**示重（体重计）**"，不带任何 F 符号。
- 数值仍为该体重计读数（如 720 N），逻辑不变。

### 4. 主画布读数气泡：只显示一行
- 气泡内只显示一行：**"视重（体重计）= 720 N"**（当前实时示数）。
- **"实重 G = 600 N" 一行不再显示**——实重数据在右侧实时数值面板中已有，画面上不重复。
- 气泡颜色随状态变化（超重橙 / 正常白 / 失重蓝 / 完全失重青）不变。

### 5. 图例同步
- 删除 "→ 速度 v" 项。
- "F 气泡：…" 相关文案改为 "示重气泡：…"（超重橙 / 正常白 / 失重蓝 / 完全失重青不变）。
- 其余（轿厢、加速度 a、楼层线、实重虚线、当前点）不变。

## 二、未变更部分（v1 维持）
- 物理模型与公式、8 阶段行程脚本、参数滑条、v-t 图、F-t 图（含 G=mg 实重虚线参考）、3 按钮状态矩阵、空格/R 快捷键、无障碍、交付流程（同步 `_repo` + index.html 卡片）。

## 三、验收标准
1. 主画布不再出现红色速度矢量及 "v = 0" 标注。
2. 主画布显示橙色加速度矢量 + 数值标注（a = +2.0 / a = -2.0 / a = 0），随阶段实时更新。
3. 实时数值面板标签为 "示重（体重计）"，无 F 符号。
4. 主画布读数气泡只有一行 "视重（体重计）= xxx N"，无 "实重 G" 行；颜色随状态变化不变。
5. 图例已删除 "→ 速度 v"，"F 气泡" 改为 "示重气泡"。
6. 页面与 v1 其余功能无回归（79 项断言基线保持通过，其中速度矢量相关断言按 v2 调整）。
7. 交付时同步 `_repo` 仓库并推送，线上生效。
