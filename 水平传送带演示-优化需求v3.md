# 水平传送带演示网页 · 优化需求 v3

## 1. 背景与目标
- **对标文件**：`horizontal-conveyor-belt.html`（水平传送带演示｜高中物理）。
- **在线地址**：https://wenqiaowuli.github.io/demo/horizontal-conveyor-belt.html
- **前置**：v1（滚筒/环形带面/机架/v-t 拆面板/s相对+Q/落下动画/离带/快捷键/g=10/aria）已上线；v2（物块默认左端、ST 图、8 辐条、擦痕、m 标签、倍速/L 滑条）已上线。
- **目标（v3）**：基于 v2 做以下改动。
- **参照**：`oblique-throw-motion.html`（斜抛运动演示）——公式说明框、按钮风格、布局风格。

## 2. 改动清单（逐条确认）

### CH1 物块开始位置固定（← 左端 / → 右端）
- 固定两种起点：**最左端**（默认，左滚筒内 1 m）或**最右端**（右滚筒内 1 m）；不随 v₀ 自动判断。
- 采用「← 向左 / · 静止 / → 向右」**三方向按钮**（与斜抛页运动方向按钮风格一致），替换当前数值滑条。
- 去掉「反向放上情景」自动放中央的逻辑。

### CH2 去掉落下动画
- 轻放情景（v₀=0）不再有"滑块弹起又落下"的动作；物块**直接出现**在设定起点（左端或右端），无初始下落过程。
- v₀=0 时物块初始与带相对静止（静摩擦阶段从位置静止开始）。

### CH3 传送带长度可调（保留 v2）
- 保留 L 滑条（5–40 m，默认 24 m），实时生效，物块起点/刻度/离带判定跟随更新。

### CH4 初速度可调
- 参数卡中保留并激活初速度 v₀ 滑条/输入，用户可自由设置（含正负，正=向右，负=向左）。

### CH5 轮毂改成空心
- 当前滚筒轮毂为实心圆 → 改为**外圈 rim + 空心圆环 + 中心细轴**（空心圆柱/圆环视觉感，中心空出）。

### CH6 ST 图更明显
- 调整 ST 图纵轴刻度（根据当前演示范围动态计算，而非固定范围），使两条位移线拉开间距，不被压缩到一条线。
- 零线（s=0）加粗高亮。
- 两线阴影（=相对位移）颜色加深或透明度降低，更直观。
- 保持：两线间距 = 相对位移，共速竖线，播放头，当前点。

### CH7 去掉提示句
- 去掉底部 phase 区的「物块向内传来，向左滑 / 向右滑 / 已离带」中文长句提示（保留阶段名如「滑动中」「共速」「已离带」等简短标签）。

### CH8 补充计算公式说明
- 参照斜抛页 `note` 框样式（固定框：公式 + 正方向 + 参数说明），在参数卡底部增加**公式说明框**，包含：
  - 相对滑动阶段：`a = μg`，`v = v₀ + at`，`s相对 = (v₀ - v带)² / (2μg)`，`Q = μmg·s相对`
  - 共速后：`v = v带`，`a = 0`，匀速
  - 正方向：物块位移向右为正；传送带位移：v带>0 向右
  - 各参数说明（μ、m、g、v₀、v带、L）

## 3. 保持不变的 v2 行为
- VT 双面板布局（v-t 橙物块/青带速 + s-t 橙物块位移/青带位移）、两线阴影=相对位移
- 8 辐条滚筒、双轮缘、轴钉、旋转亮斑
- 带面红色滑动擦痕（长度=s相对，共速后定格）
- 物块 m 标签
- 带长刻度/放上点/出料箭头
- 播放倍速滑条（0.25–4×）、带长 L 滑条（5–40 m）
- 三情景按钮（轻放/刹停/反向放上 → 改为"起点在左"/"起点在右"）
- g=10、快捷键（Space/R）、aria-live、dead code 清理

## 4. 验收标准（更新）
- [x] 起点固定为左端（默认）或右端，通过方向按钮切换，不随 v₀ 变化。
- [x] 轻放/刹停情景无落下动画，物块直接出现在起点。
- [x] 初速度滑条/输入可自由调整（含正负）。
- [x] 轮毂为空心圆环（外圈 + 空心 + 中心轴）。
- [x] ST 图纵轴动态缩放，两线间距明显可见；零线加粗高亮。
- [x] 底部无中文长句提示，仅保留阶段名。
- [x] 参数卡底部有公式说明框（参照斜抛页样式）。
- [x] 物理逻辑：s相对、Q、共速时间、离带时间解析解正确；v-t/s-t 曲线数据正确。
- [x] 运行中滑条禁用；空格/R、aria、g=10 不回归。
- [x] 30 项桩测试全过（含左端/右端情景、倍速、带长、双向 v₀、暂停/继续/快捷键）。
