# 需求：板块运动演示 v4（初始位置 / 板厚比例 / 物块缩小 / 板默认长 / 标注与边界隐藏）

> 本需求由用户于 2026-09-13 确认，基于 v3 需求文件修订。
> 待修改文件：`/home/wq/physics-work/_repo/block-plate.html`

---

## 一、初始位置：从活动区最左侧出发
1. 演示开始时，长木板 B 位于活动区域最左端（xB = 0），物块 A 位于板上随板从最左侧出发向右运动。
2. 活动区域左边界即出发点：**REGION_MIN = 0 m**（由 v3 的 −10 m 改为 0）。

## 二、长木板厚度：改为现有厚度的 1/2
1. 板厚（视觉高度）由 24 px 改为 **12 px**（PLATE_H = 12）。
2. 板长加长（见下条）后整体呈细长板形态；板上文字标注随板厚自适应
   （12 px 板身不再容纳完整标签，改为板身内嵌短标记 "B"，速度/板长信息由
   参数滑条与数据面板承载）。
3. 下落过渡、受力箭头（f₁' 板上表面 / f₂ 地面接触处）等既有元素位置
   随 PLATE_H 自适应，不得与板身/地面刻度错位。

## 三、物块体积缩小为原来的 1/4
1. 物块 A 世界尺寸由 2.0 m 正方形改为 **1.0 m 正方形**（BLOCK_W_M = 1.0，
   边长 1/2 → 体积 1/4），保持"正方形、不随板长/运动/缩放变化"的 v3约定。
2. 物块上名称/速度文字沿用上移自适应规则。

## 四、木板默认长度 = 小方块的 20 倍
1. 默认板长 = 20 × 1.0 m = **20 m**（滑条默认值 6 → 20，P.L 默认 20）。
2. 板长滑条范围由 2–10 m（步长 0.5）扩大为 **2–40 m、步长 1 m**。

## 五、正方向标志移至左上角
1. "正方向 →"标注从右上角移至演示区域**左上角**。
2. 原"运动区域"文字标签去除。

## 六、隐藏活动区域边界线 + 越界结束提示
1. **不再绘制**左右边界红色虚线及任何开始线/截止线；活动区域仅作为内部
   判定存在，不在画面中显示。
2. 活动区域设定为 **0 ～ 60 m**（REGION_MAX 由 40 改为 60）。
3. 越界判定保持"完全穿过"：物块左端越过右界（xA ≥ 60）或板左端越过右界
   （xB ≥ 60）→ 演示立即结束；反向运动中物块/板完全穿出左界（xA + 1 ≤ 0
   或 xB + L ≤ 0）同样结束。
4. 越界结束状态栏提示："（物块 A/板块 B）未停止即穿过活动区域，演示结束。"

## 七、验收标准
1. 初始帧：板与物块位于画面最左侧（x = 0 起步）。
2. 板厚 12 px；默认板长 20 m 在 880 画布上宽约 239 px（scale ≈ 11.9 px/m）。
3. 物块 1.0 m 正方形（像素约 12×12，含描边 ≈14），板长 2/20/40 m 下尺寸不变。
4. 默认参数（v₀A=12、v₀B=2、μ₂=0、L=20）：相对滑移 17 m < 20 m 不滑出，
   共速后一起匀速，约 t ≈ 9 s 整体穿出右界，演示结束并显示提示。
5. 画面中无任何边界虚线；左上角有"正方向 →"标注。
6. 回归：双摩擦、锁死/slipAfter、生热、数据面板语义、移动端自适应均不受影响。
