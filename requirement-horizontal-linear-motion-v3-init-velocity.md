# 需求：水平直线运动演示——默认初速度改为 +5 m/s

## 目标
- 修改：`horizontal-linear-motion.html`（水平直线运动演示｜高中物理）
- 页面初始默认初速度由 0 m/s 改为 **+5.0 m/s（水平向右）**，默认状态由"静止"变为"匀速"

## 具体改动
1. **v₀ 滑条默认值**：`value="0"` → `value="5"`（范围 −10 ~ +10 m/s、步长 0.5 均不变）
2. **初始参数标签**：`v0Val` 初始文本 `+0.0 m/s` → `+5.0 m/s`
3. **默认加速度保持 0 不变**：默认运动状态显示"匀速"（v₀ = 5 m/s，a = 0）

## 验证要点
- 页面加载后：v₀ 显示 +5.0 m/s，速度 v = 5.00 m/s，运动状态为"匀速"
- 点击"开始"后小球向右匀速运动
- 点击"重置"后回到 v₀ = +5 m/s 的初始状态
- 手动把 v₀ 拉回 0 时仍可正常演示静止状态
