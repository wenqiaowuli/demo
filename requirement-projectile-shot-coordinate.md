# 需求：平抛运动演示网页参数协调性调整

## 目标文件
- `/home/wq/physics-work/_repo/projectile-motion.html`（GitHub 仓库根目录，非 physics/ 子目录）

## 需求主旨
调整滑块参数的范围与默认值，使"高度"与"水平位移"两轴数值处于同一数量级，画面更协调；同时把"初速度"的可调范围调大。

---

## 一、参数调整（滑块）

| 参数 | 项目 | 当前值 | 目标值 |
|------|------|--------|--------|
| 高度 h | 最小值 min | 60 | 20 |
| 高度 h | 最大值 max | 360 | 120 |
| 高度 h | 默认值 value | 220 | 45 |
| 高度 h | 步长 step | 10 | 5 |
| 初速度 v0 | 最小值 min | 5 | 5 |
| 初速度 v0 | 最大值 max | 25 | 40 |
| 初速度 v0 | 默认值 value | 12 | 20 |
| 初速度 v0 | 步长 step | 0.5 | 1 |

对应代码位置：`<input id="h" ...>` 与 `<input id="v0" ...>` 两处 range 控件的 min/max/value/step 属性。

## 二、联动逻辑（执行方需保证，勿遗漏）

1. **实时数据显示**：初始"离地高度""落地时间""水平射程"等面板数值由 JS 按新默认值自动重算，无需手改硬编码显示值（原页面的更新函数在运行时统一刷新）。
2. **坐标轴刻度**：xMax / yMax 由 `niceCeil`、`niceStep` 函数按参数自动计算缩放，新默认值下轴刻度量级会自动变小，无需手改坐标逻辑。
3. **默认状态一致性**：页面初始 data 面板、画布、坐标轴均以新默认值（h=45、v0=20）为准。

## 三、默认值的物理与画面校验（供验收参考）

- 高度 h=45 m → 落地时间 t=√(2×45/9.8)≈3.03 s
- 初速度 v0=20 m/s → 水平射程=20×3.03≈60.6 m
- 两轴量级：高度约 45，射程约 60，比例约 1:1.3，画面对比原默认（220 对 80）明显更协调。

## 四、禁止改动项

- 重力 g=9.8
- 画布尺寸 880×520、配色、按钮逻辑、暂停/继续/重置/轨迹绘制、数据面板结构
- 物理公式（x=v0·t、y=h−½gt²、t_end=√(2h/g)）
- 其余任何与本次参数调整无关的逻辑

## 五、验收标准

1. 页面初始显示：高度 45 m、初速度 20 m/s。
2. 高度滑块范围为 20~120、步长 5；初速度滑块范围 5~40、步长 1。
3. 调整滑块后数据面板与坐标轴实时联动，物理数值正确。
4. 默认状态轨迹横跨画布比例协调（高度与射程轴数量级相近）。
5. 动画开始/暂停/继续/重置均正常，轨迹与理论轨迹一致。