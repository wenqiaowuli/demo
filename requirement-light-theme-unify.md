# 需求：全系列演示页配色统一为浅色主题（参照追及相遇页）

## 背景与目标
- 参照 `chasing-meeting-model.html`（追及相遇模型演示）的配色风格，将其余所有演示网页由深色主题改为浅色：**背景换成白色系**，全系列配色风格统一、对比鲜明。
- 硬性要求：**所有显示元素（文字、矢量、网格、曲线、按钮、面板）在浅色背景下都能正常出现、被看见**。

## 参照页调色板（chasing-meeting-model.html，不改此页）
- CSS 变量：`--cA:#2563eb`（主蓝）、`--cB:#f97316`（橙）、`--ink:#1f2937`（正文墨色）、`--mut:#6b7280`（次要灰）、`--line:#e5e7eb`（边框线）
- 页面背景 `#eef2f6`，卡片 `#fff` + 边框 `#e5e7eb` + 阴影 `0 1px 2px rgba(15,23,42,.05)`
- 画布：白底、网格 `#e5e7eb`、轴/框线 `#94a3b8`、刻度文字 `#6b7280`、强调红 `#dc2626`、绿 `#10b981`

## 统一映射表（深色 → 浅色）

### CSS / 布局
| 深色值 | 浅色值 |
|--------|--------|
| body bg `#121212`、文字 `#fff` | bg `#eef2f6`、文字 `#1f2937` |
| 主画布/图表 bg `#0b1828`、边框 `#3388dd` | bg `#ffffff`、边框 `#2563eb`（2px 保留） |
| 面板 `rgba(255,255,255,.05)` / 边框 `.1` | `#ffffff` / `#e5e7eb` + 阴影 `0 1px 2px rgba(15,23,42,.05)` |
| data-item `rgba(255,255,255,.035)` / `.08` | `#f8fafc` / `#e5e7eb` |
| 文字 `#ccd` `#dde6f0` `#dceeff` | `#1f2937` |
| 次要文字 `#8899aa` `#667788` `#aab` | `#6b7280` |
| 按钮 `#3388dd` / hover `#2f7cc8` | `#2563eb` / hover `#1d4ed8` |
| Tab/分段未激活 `rgba(255,255,255,.06)/.18` 文字 `#ccd` | `#f8fafc` / `#cbd5e1` 文字 `#475569`；激活 `#2563eb` |
| fixed-note 提示 `#8899aa`，强调 `#ff9933` | `#6b7280`，强调 `#b45309` |

### 画布绘制
| 深色值 | 浅色值 |
|--------|--------|
| 画布填充 `#0b1828` | `#ffffff` |
| 网格 `#1f2d3d` / `rgba(255,255,255,.05)` | `#e5e7eb` / `rgba(31,41,55,.07)` |
| 轴线/边框 `#7b8ea3` `#3a4a5c` `#8a9bb0` | `#94a3b8` `#cbd5e1` `#94a3b8` |
| 刻度/标注 `#8899aa` `#667788` | `#6b7280` |
| 矢量黄 `#ffcc44` | `#d97706` |
| 矢量青 `#33ddcc` | `#0d9488` |
| 矢量红 `#ff5555` | `#dc2626` |
| 矢量橙 `#ff9933` | `#ea580c` |
| 矢量蓝 `#66aaff` | `#2563eb` |
| 紫 `#c084fc` / 绿 `#aaff66` / 粉红 `#ff5577` `#ff6666` | `#7c3aed` / `#16a34a` / `#e11d48` |
| 虚线辅助 `rgba(255,255,255,.4~.55)` | `rgba(31,41,55,.45~.5)` |
| 幽灵/参考 `rgba(255,255,255,.25)` | `rgba(31,41,55,.3)` |
| 当前点 `#ff5555`；游标 `rgba(51,136,221,.4)` | `#dc2626`；`rgba(37,99,235,.35)` |
| 白色画布文字 `#fff` `#dceeff`（画布底上） | `#1f2937` |

## 特殊规则
1. **中深色填充内部的白字保留**：物块（`#d8a050` 渐变）、电梯轿厢、动量条形等填充上的白色标签不改（对比依旧鲜明）。
2. 深色底子元素（如轿厢 `rgba(35,60,85,.92)`、体重计 `#12263c`）：改为浅色填充 + 深色描边（如 `#dbeafe` + `#2563eb`），或保留中深色但确保其中文字/图形对比鲜明——逐个判断。
3. 擦痕/痕迹类半透明红：改 `rgba(220,38,38,x)` 透明度保持。
4. 语义色状态文字（超重橙/失重蓝/正常白等）：白 → `#1f2937`，其余按主色映射加深。
5. 不改布局、DOM 结构、交互逻辑、文字内容；只改颜色。
6. 改完必须全文件搜索确认无 `#0b1828`、`#121212`、`#1f2d3d`、`#8899aa`、`rgba(255,255,255,` 残留（页面特有近似深色一并处理）。

## 实施分组
- 组A：`apparent-weight-demo.html`、`free-fall-motion.html`、`vertical-throw-demo.html`
- 组B：`block-plate.html`、`horizontal-conveyor-belt.html`、`inclined-conveyor-belt.html`
- 组C：`horizontal-linear-motion.html`、`oblique-throw-motion.html`、`_repo/projectile-motion.html`、`_repo/momentum-collision.html`
- 组D：`spring-vs-rope-demo.html`、`force-composition-demo.html`、`force-equilibrium-demo.html`
- 不改：`chasing-meeting-model.html`（参照基准）、`_repo/index.html`（已是浅色）

## 验收标准
- [ ] 全部 13 页背景为白色系（页面 `#eef2f6`、卡片/画布 `#ffffff`），无深色残留。
- [ ] 所有文字在浅色背景下清晰可读（正文 `#1f2937`、次要 `#6b7280`）。
- [ ] 画布内矢量/曲线/网格/虚线/当前点/比例尺全部可见且对比鲜明。
- [ ] 各页功能与布局不变（按钮矩阵、参数、交互不因配色改动受影响）。
- [ ] 每页浏览器加载零报错。
