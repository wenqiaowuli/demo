# 斜抛运动演示网页 · 优化需求

## 1. 背景与目标
- **对标文件**：`oblique-throw-motion.html`（斜抛运动演示｜高中物理）。
- **在线地址**：https://wenqiaowuli.github.io/demo/oblique-throw-motion.html
- **参考**：已优化的 `projectile-motion.html`（平抛）、`vertical-throw-demo.html`（竖直上抛）。
- **目标**：在**不改变斜上抛物理模型**的前提下，把斜抛页对齐平抛/上抛的教学口径与交互，并补上斜抛特有的课堂表达。
- **已核对（保持不变）**：
  - 坐标系：抛出点为原点，x 向右为正，y **向上**为正（与平抛「向下为正」不同，因斜抛有上升段，必须保留）。
  - 公式自洽：`vₓ=v₀cosθ`，`vᵧ=v₀sinθ−gt`，`x=vₓt`，`y=v₀y t−½gt²`，`T=2v₀sinθ/g`，`X=v₀²sin2θ/g`，`H=v₀²sin²θ/(2g)`。
  - 最高点横坐标 `x_H = X/2`（同高度落地）正确；最高点处 `vᵧ=0`、`γ=0`。
  - 默认 v₀=20 m/s、θ=45° 时，现用 g=9.8 的读数 T=2.89 s、X=40.82 m、H=10.20 m 与公式一致。
  - v-t / s-t 双图、理论轨迹、速度矢量、最高点/落地标记均已存在，本次不是从零新增。

## 2. 关键决策
- **重力加速度 g 固定为 10 m/s²**：将 `const g = 9.8` 改为 `const g = 10`，**不引入 g 切换下拉**。与平抛页一致，与上抛页默认值一致。
- **正方向约定不改**：水平向右为 +x，竖直向上为 +y；过最高点后 `vᵧ<0`、`γ<0`。
- **模型范围不扩**：仍为平地斜上抛、落回抛出点高度。

## 3. 功能需求

### FR1 重力加速度 g = 10
- 将 `const g = 9.8` 改为 `const g = 10`。
- 参数区增加固定说明（对标平抛 `fixed-note`）：
  - `g = 10 m/s²`（固定值）。
  - 水平方向匀速直线，竖直方向竖直上抛。
  - 正方向：水平向右为 +x，竖直向上为 +y（故过最高点后 vᵧ、γ 为负）。
  - 公式：`vₓ = v₀cosθ`，`vᵧ = v₀sinθ − gt`，`x = vₓt`，`y = v₀y t − ½gt²`。
- 默认参数（v₀=20 m/s，θ=45°）初始读数同步为：

| 量 | 计算 | 新值 |
|----|------|------|
| v₀x = v₀y | 20·cos45° | 14.14 m/s |
| T | 2·v₀sinθ/g = 2√2 / 10 | **2.83 s** |
| X | v₀²sin2θ/g = 400/10 | **40.00 m** |
| H | v₀²sin²θ/(2g) = 200/20 | **10.00 m** |

### FR2 键盘快捷键（与平抛 / 上抛一致）
- **空格**：播放 / 暂停（toggle）。idle/ended 时开始或重新开始；running 时暂停；paused 时继续。
- **R**：重置。
- 焦点在 `INPUT` / `SELECT` / `TEXTAREA` / `BUTTON` 时不触发。

### FR3 播放倍速
- 参数区新增「播放速度」滑条：范围 0.1–4，步长 0.1，**默认 0.5×**（对标上抛；1× 约 3 s 飞完，课上偏快）。
- 只缩放动画 `dt`，不改变物理时间 t、公式与读数。
- 运行中可调倍速。

### FR4 s-t 图纵轴：x、y 独立缩放
- 现状：x 与 y 共用纵轴 `0 ~ max(X, H)`。45° 时 X=40 m、H=10 m，y 曲线只占约 1/4 图高，上升/回落形态被压扁。
- 改为两条曲线**各自按其最大值映射到图高**（x 按射程 X，y 按最大高度 H），气泡与读数仍显示真实物理值（米）。
- 图内或图例标注：「x、y 纵轴独立缩放，读数为真实值」。
- 左轴刻度按 y 的量程标注（0 ~ H），避免再按 X 画一套对 y 无意义的密刻度；x 曲线用颜色区分即可。
- v-t 图维持现状（vₓ、vᵧ 量级接近，共用 ±vMax 合理）。

### FR5 斜抛核心结论（教学标注）
在参数区 `fixed-note` 或图例处固定写明：
- 射程 `X = v₀² sin2θ / g`。
- **θ 与 90°−θ 射程相同**（如 30° 与 60°）。
- **θ = 45° 时射程最大**。
不另做动画特效；滑到 45° 或互余角时不必弹窗，文字常驻即可。

### FR6 无障碍 aria-live（对标平抛）
- 播放中（`running`）：`aria-live="off"`，避免读屏每帧播报。
- 暂停 / 结束 / 重置后：`aria-live="polite"`。
- canvas `aria-label` 仍在 `updateReadouts` 中更新。

### FR7 控件禁用态
- 为禁用中的滑条增加样式（对标平抛）：
  `input[type=range]:disabled { opacity: 0.45; cursor: not-allowed; }`

### FR8 代码清理（不改行为）
- 删除未使用的 `formatValue`、`apexTime`。
- 合并重复 CSS：`.param-group, .data-group` 只保留一份。
- 方向角元素 `id="vt"` 与 v-t 图画布语义冲突，改为 `id="gamma"`（同步 JS 取值）。

## 4. 风格 / 一致性
- 保持现有深色主题、`#3388dd` 强调色、主画布 880×520、副图 20/9、DPR、ResizeObserver、开始/暂停/继续/重置四键。
- `fixed-note` 视觉对标平抛页（灰色说明，关键量用橙色加粗）。
- 主画布仍用**各向同性**米制比例（1 m 在 x、y 方向像素相同），不把抛物线纵向拉高；空余由现有居中逻辑消化。

## 5. 非目标（本次不做）
- 斜下抛、非同一高度落点（不加抛射高度 h）。
- 空气阻力。
- g 切换下拉（9.8 / 10）。
- 把正方向改成「向下为正」（斜抛必须保留向上为正）。
- 主画布改用不等比拉伸。

## 6. 验收标准
- [ ] 页面明示 `g = 10 m/s²`；默认 v₀=20、θ=45° → T=2.83 s、X=40.00 m、H=10.00 m。
- [ ] 最高点 `vᵧ=0`、`γ=0`；之后 `vᵧ<0`、`γ<0`；改 θ 后 X、H、T 与公式一致。
- [ ] 空格 / R 可用，且焦点在输入/按钮时不触发。
- [ ] 播放速度默认 0.5×，可调，只影响动画快慢。
- [ ] s-t 图中 y 曲线能看清先升后降；气泡仍为真实米值；有「独立缩放」说明。
- [ ] 参数区写明「θ 与 90°−θ 射程相同、45° 射程最大」。
- [ ] 播放中读屏不刷屏；暂停/结束后恢复 polite。
- [ ] 滑条禁用变灰；无 `formatValue` / `apexTime` 死代码；方向角 id 为 `gamma`。
- [ ] 原有轨迹、矢量、最高点/落地、v-t 图（vₓ 水平直线、vᵧ 过零）行为不回归。
