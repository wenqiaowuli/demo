# 板块运动演示 · v9 地面位置调整需求

## 1. 背景与目标
- **对标文件**：`block-plate.html`（板块运动演示 通用版 v8）。
- **现状**：主画布（880×520 基准）地面线由 `groundY = view.height − PAD_BOTTOM` 计算，`PAD_BOTTOM = 58`，即地面距画布底部仅 58px（约占总高 11%），地面整体偏下、下方留白过少。
- **目标**：将地面线调整到**距演示画面底部 1/3 整体高度**处：`groundY = round(view.height × 2/3)`（520 基准下距底部 ≈ 173px，地面线位于 y ≈ 347px）。

## 2. 改动点
- **JS**：`drawForces()` 与 `drawScene()` 中 `const groundY = view.height - PAD_BOTTOM;` → `const groundY = Math.round(view.height * 2 / 3);`（`PAD_BOTTOM` 常量随之废弃删除）。
- 采用比例式而非固定像素：窗口缩放时 `view.height` 变化，地面始终保持在总高 2/3 处（距底部 1/3）。
- 地面距离标注（groundY+24）、板块/物块投影阴影（groundY+3 起）、地面摩擦箭头（板上沿基准）等均以 groundY 为基准相对绘制，**自动跟随，不改绘制逻辑**。

## 3. 非目标（本次不做）
- 不改板块/物块尺寸、世界坐标比例尺（viewScale 仅依赖水平方向）、活动区域 [0, 60] 定义。
- 不改受力箭头画法、v-t / s-t 双图、参数面板、按钮、图例与文案。
- 不改物理逻辑（事件驱动积分、共速锁死、滑出判定等）。
- 不新增地面装饰（纹理/草地等）。

## 4. 验收标准
- [ ] 地面线距画布底部 = 总高 1/3（880×520 基准下 groundY ≈ 347 = 520×2/3，误差 ≤ 1px）。
- [ ] 窗口缩放后地面仍保持距底部 1/3 的比例关系（DPR + ResizeObserver 沿用）。
- [ ] 地面下方距离标注（groundY+24）完整可见；板块/物块投影阴影正常。
- [ ] 上方活动空间加大后，板块、物块、摩擦力箭头、正方向标注均不被裁切。
- [ ] 运动/共速/滑出行为与双图无回归。
