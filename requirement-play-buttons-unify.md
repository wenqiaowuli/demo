# 需求：全系列演示页播放控制统一为标准三按钮（开始 | 暂停/继续 | 重置）

## 背景与目标
- 用户要求：检查所有演示网页，凡按钮不是"开始 | 暂停 | 重置"三个的，统一更新为标准三按钮；按钮功能参照已符合规范的页面（`apparent-weight-demo.html`、`horizontal-linear-motion.html`，合并约定见 `_repo/requirement-horizontal-linear-motion-v2-button-merge.md`）。
- 标准状态矩阵（所有页一致）：
  | 状态 | 按钮1（开始） | 按钮2（暂停/继续） | 按钮3（重置） |
  |------|--------------|-------------------|--------------|
  | 初始/重置后 | 启用"开始" | 禁用"暂停" | 启用 |
  | 运行中 | 禁用"运行中…" | 启用"暂停" | 禁用 |
  | 暂停中 | 启用"重新开始" | 启用"继续" | 启用 |
  | 结束 | 启用"重新开始" | 禁用"暂停" | 启用 |
- 标准快捷键：空格 = 播放/暂停切换（待机/结束时按下即开始）、R = 重置；焦点在输入控件/按钮时不触发。

## 排查结论（12 个页面）
**已符合，不改动（7 个）**：`apparent-weight-demo.html`、`block-plate.html`、`free-fall-motion.html`、`horizontal-linear-motion.html`、`spring-vs-rope-demo.html`（均为 开始|暂停|重置）、`force-composition-demo.html` 与 `force-equilibrium-demo.html`（▶ 自动演示|暂停|重置——播放行为与矩阵完全一致，首钮文案"自动演示"为各自需求文档确认过的语义，保留）。

**需修改（5 个）：**
| 文件 | 现状 | 改动 |
|------|------|------|
| `vertical-throw-demo.html` | 开始\|暂停\|继续\|重置（4 钮）；无快捷键 | 合并暂停/继续；补空格/R 快捷键 |
| `oblique-throw-motion.html` | 开始\|暂停\|继续\|重置（4 钮）；已有快捷键 | 合并暂停/继续 |
| `horizontal-conveyor-belt.html` | ⬒左端\|⬒右端\|▶开始\|⏸暂停\|▶继续\|⟲重置 | 合并暂停/继续；结束态"已离带"→"重新开始"并可重跑 |
| `inclined-conveyor-belt.html` | ⬒底端\|中部\|顶端 + 4 播放钮 | 同上（"已离开"→"重新开始"） |
| `chasing-meeting-model.html` | ▶开始\|单步±0.1s\|重播\|重置（5 钮，无暂停） | 重构为标准三按钮；删除单步×2 与重播（时间轴滑条保留，可替代步进微调）；补 R 键 |

## 改动清单
### R1 `vertical-throw-demo.html` / R2 `oblique-throw-motion.html`
- 删除 `btnContinue` 按钮及全部引用；`btnPause` 变为暂停/继续切换钮。
- 按状态矩阵更新 start/pause/resume/reset/ended 五处按钮文字与禁用态（运行中按钮1改显"运行中…"）。
- vertical-throw 新增 keydown（Space/R，带焦点保护）；oblique 已有，逻辑不变。

### R3 `horizontal-conveyor-belt.html` / R4 `inclined-conveyor-belt.html`
- 删除"继续"按钮（`bContinue`）及引用；`bPause` 变为切换钮。
- done（离带）态：按钮1由"已离带/已离开"（禁用）改为"重新开始"（启用），点击后重置并重新运行（离带结果仍在 HUD 数值区可见）。
- togglePlay 补 done 态分支（空格在结束时按下 = 重新运行）。
- 场景预设按钮（左端/右端、底端/中部/顶端）为参数类控件，不属于播放控制，保留。

### R5 `chasing-meeting-model.html`
- 按钮行改为标准三按钮：`▶ 开始` | `暂停`（新增 btnPauseResume）| `重置`；删除"单步 −0.1s""单步 +0.1s""重播"。
- 按状态矩阵实现 setButtons()：idle(t=0) / running(playing) / paused(0<t<T 且暂停) / ended(t≥T)。
- 按钮1：idle/ended 点击从 t=0 开始播放；暂停中点击=重新开始。
- 按钮2：运行中暂停、暂停中继续；idle/ended 禁用。
- 时间轴滑条保留（拖动即暂停并定位，替代单步功能）；预设选择、速度选择、模式单选等参数控件保留。
- keydown：空格按标准切换（结束时按下=从头重播）；新增 R=重置（直接调用重置逻辑，不经由可能处于禁用态的按钮）。

### R6 视觉评审补充（R1~R4 各页）
- "运行中"状态下**重置按钮同样禁用**（矩阵第三列），恢复/结束时重新启用——首次实现遗漏，评审发现后补齐（竖直上抛、斜抛、水平/倾斜传送带四页的状态转换函数均已加 reset 列）。
- 追及相遇页补充 `button:disabled` 灰化样式（原页面无禁用态视觉区分）。

### R7 评审发现的历史遗留 bug 顺带修复（非本次改动引入，git 旧版本可复现）
- 追及相遇页：实时数据"B 的速度 v_B""相对速度 v_A − v_B"恒为 NaN——`updateData()` 调用 `vBo()` 漏传 `t`，已补为 `vBo(t)`。
- 追及相遇页：公式行"位置表达式"正系数缺"+"号（如"0 10.00t"）——exprTerm 初速度项补正号前缀。
- 斜抛页：v-t / s-t 图两个当前值标签在曲线接近时叠印——标签改为按纵坐标排序后统一排布（最小间距 14px）。

## 保持不变
- 各页动画物理、图表、参数面板、预设控件、图例等全部功能。
- 快捷键焦点保护约定（INPUT/SELECT/TEXTAREA/BUTTON 聚焦时不触发）。

## 验收标准
- [ ] 5 个页面按钮行符合标准矩阵：初始/运行/暂停/结束四态的文字与禁用态正确（含运行中重置禁用）。
- [ ] 合并后的暂停/继续按钮在 running↔paused 间正确切换。
- [ ] 传送带两页离带结束后按钮1显示"重新开始"且可直接重跑。
- [ ] 追及相遇页仅 3 个播放按钮；拖时间轴后为暂停态（按钮1"重新开始"/按钮2"继续"）；空格可从头重播。
- [ ] 空格/R 在全部 5 页可用（vertical-throw 为新增），输入框聚焦时不触发。
- [ ] 追及页数据面板 v_B / v_rel 显示正常数值（非 NaN）；斜抛页图表当前值标签不叠印。
- [ ] 全部页面浏览器加载零报错。
