# 需求：板块运动演示网页优化（优化版）

## 一、 优化目标
在最新版板块模型演示网页（`block-plate.html`，具有双摩擦因数、摩擦生热及共速不锁死滑动等物理特征）的基础上，进行物理建模的扩展、直观受力分析的可视化、以及动画视觉流畅度的优化，使其成为一个完全体、可覆盖高中物理所有经典板块习题的教学演示工具。

**注意：本次仅编写需求文件，暂不进行任何代码编写或修改。**

---

## 二、 物理参数与建模扩展需求

### 1. 新增物块 A 与木板 B 的质量可调功能
*   **需求描述**：目前物块 $m_A$ 和木板 $m_B$ 均硬编码在代码中。需将它们提取为用户可通过滑动条实时调节的参数。
*   **具体规格**：
    *   **物块 A 质量 $m_A$**：
        *   滑动条范围：`1.0` – `5.0 kg`，步长 `0.1 kg`，默认值 `2.0 kg`。
    *   **木板 B 质量 $m_B$**：
        *   滑动条范围：`1.0` – `10.0 kg`，步长 `0.1 kg`，默认值 `4.0 kg`。
    *   **联动设计**：运行中两滑条禁用；在 `idle` 状态下拖动时，实时重新运行 `physics()` 物理计算并更新数据面板。

### 2. 扩展木板 B 初速度 $v_{0B}$ 可调功能（支持双初速物理场景）
*   **需求描述**：支持木板具有初速度，突破“木板始终从静止开始”的限制，支持同向不同速、反向相向运动等更复杂的物理情景。同时，本功能可以完全激活原本因条件限制难以触发的“共速后不能锁死再次相对滑动（`slipAfter`）”的物理现象。
*   **具体规格**：
    *   **木板 B 初速度 $v_{0B}$**：
        *   滑动条范围：`-10.0` – `10.0 m/s`（负数代表向左，正数代表向右），步长 `0.5 m/s`，默认值 `0.0 m/s`。
    *   **物理公式重构**：
        *   需重构 `physics()` 计算，将初始条件从 $v_{0B}=0$ 拓宽为任意 $v_{0B}$。
        *   滑动阶段判定、加速度方向判定需结合 A 与 B 的相对运动方向（即 $v_A - v_B$ 的正负）。
        *   锁死判定：共速后，A 和 B 整体在地面摩擦下做减速运动（加速度为 $a_{together} = \mu_2 g$）。要使两者不相对滑动，A 所需的摩擦力大小为 $f_{req} = m_A \mu_2 g$。由于 A 与 B 间最大静摩擦力为 $f_{max} = \mu_1 m_A g$，因此锁死条件为：$\mu_1 \ge \mu_2$。若 $\mu_1 < \mu_2$，则共速后无法锁死，将发生二次相对滑动（`slipAfter = true`）。
    *   **v-t 图像重构**：v-t 图纵坐标需支持负速度，或者将纵坐标自适应范围调整为最小速度到最大速度（如 `[-10, 20] m/s` 之间自适应）。

---

## 三、 视觉表现与受力分析 (FBD) 可视化需求

### 3. 动态受力分析矢量箭头（重难点）
*   **需求描述**：在物体运动或静止过程中，在画布中实时绘制出水平方向上的摩擦力矢量箭头。这能使学生在观察动画的同时，直观地进行受力分析。
*   **具体规格**：
    *   **A 受到的摩擦力 $f_{BA}$**：
        *   绘制在物块 A 的中心或底部。
        *   方向：当 $v_A > v_B$ 时向左；当 $v_A < v_B$ 时向右。若共速锁死后 $\mu_2 > 0$，则受到向左的静摩擦力。
        *   大小：由物理引擎实时计算，箭头长度与力的大小成正比。
    *   **B 受到 A 的摩擦力 $f_{AB}$**：
        *   绘制在木板 B 的上表面。
        *   方向：始终与 A 受到的摩擦力相反（牛顿第三定律作用力与反作用力）。
    *   **B 受到地面的摩擦力 $f_{ground}$**：
        *   绘制在木板 B 的下表面与地面接触处。
        *   方向：与 B 的对地运动方向相反。若 $v_B = 0$ 但有运动趋势，则对应静摩擦力。
    *   **视觉规范**：箭头使用高对比度颜色（如黄色 `#ffdd57` 或红色），并在箭头旁边标注符号（如 $f_1$、$f_2$）。

### 4. 优化物块 A 滑出后的视觉过渡
*   **需求描述**：消除 A 滑出木板时，瞬间由矩形缩成圆点并产生高度突变的违和感。
*   **具体规格**：
    *   物块 A 滑出木板后，**继续保持蓝色矩形形态**。
    *   当 A 的中心位置滑离木板边缘时，在 $0.1$ – $0.2$ 秒内，将其垂直坐标 $y$ 从木板上表面平滑降低至地面高度（模拟下落或滑落过程），并在此后一直以矩形在地面上滑动、减速直至停止。

---

## 四、 动画控制与性能优化需求

### 5. 灵敏的动画结束（Terminate）条件
*   **需求描述**：优化当前在 A 滑出后导致动画无意义拖延（必须等待理论共速时间 $t_1$）的逻辑。
*   **具体规格**：
    *   **智能状态监控**：
        *   未滑出：若 $\mu_2 > 0$，当两者共同速度降至小于 `0.001 m/s` 时，停留 1 秒后自动结束；若 $\mu_2 = 0$，保持现状。
        *   已滑出：当 A 在地面停稳且 B 在地面也停稳（若 $\mu_2 > 0$）或 A、B 均滑出边界时，延时 1 秒自动结束，不强制等待原本就不适用的共速时间 $t_1$。

---

## 五、 交互与数据面板优化需求

### 6. 数据显示与图例对齐
*   *   在数据面板中增加 $m_A$ 与 $m_B$ 的质量实时显示卡片。
    *   当木板初速度 $v_{0B} \neq 0$ 时，数据面板中的“共速后速度”等物理量的计算公式和显示状态进行相应调整。

---

## 六、 页面布局与图像重构需求（参照水平直线运动演示页面）

> 页面整体结构改为与 `horizontal-linear-motion.html` 一致的分区布局，自上而下依次为：
> **演示窗口 → v-t 图 + s-t 图（左右并排） → 参数设定 + 实时数据显示（左右并排）**。

### 7. 页面整体布局重构
*   **布局结构**（自上而下）：
    1. 最上方：演示窗口（场景画布，保留现状）。
    2. 演示窗口下方：**一行两列图表区**（参照水平直线运动页面的 `.charts-row`）：
        *   左侧：速度-时间图（v-t）。
        *   右侧：位移-时间图（s-t，**本次新增**）。
    3. 再往下：**一行两列控制区**（`.control-panel`）：
        *   左侧：参数设定。
        *   右侧：实时数据显示。
    4. 底部：开始/重置按钮与图例。
*   **原布局调整**：删除当前“主画布下方单独一行的 v-t 图面板”与“底部 6 张数据卡片行（共速时刻/共速后速度/相对滑移/是否滑出/摩擦生热/质量）”；v-t 图并入图表区左列，6 项数据卡片并入右侧“实时数据显示”面板（与现有的速度显示 vA/vB/相对速度/运动状态合并）。
*   两列布局在窄屏（≤720px）时自动单列堆叠（沿用现有响应式规则）。

### 8. 新增位移-时间图（s-t）
*   新增 s-t 图画布（如 `#stChart`），置于图表区右侧，尺寸与 v-t 图一致。
*   **曲线**：同时绘制两条位移曲线——
    *   物块 A 的位移（蓝色，与场景/图例中 A 颜色一致）；
    *   板块 B 的位移（橙色，与 B 颜色一致）。
*   **同步机制**：与 v-t 图完全同步——
    *   随动画播放逐帧向后绘制；
    *   播放头（当前时刻竖线/圆点）与 v-t 图一致；
    *   标注关键事件（共速、滑出）竖线与文字。
*   **坐标自适应**：纵轴（位移 s）支持负值（如 $v_{0B}<0$ 反向运动、A 从后缘滑出），横轴（时间 t）与 v-t 图采用同一时间范围；纵轴范围按完整序列一次性固定，播放中不跳变。

### 9. 场景内速度与位移标注
*   物块 A 上显示当前速度 $v_A$（保留现状）。
*   板块 B 上显示当前速度 $v_B$（保留现状）。
*   受力分析箭头（f₁ / f₁′ / f₂）、相对滑移、摩擦生热等既有内容保持不变。

### 10. 物块与板块比例调整
*   **物块 A**：比例与大小适当缩小——
    *   长度约占板长的 **8%**（当前 12%，减小约 33%）；
    *   高度由当前 **54px** 降至约 **34px**。
*   **板块 B 厚度**：由当前 **40px** 减至约 **24px**（保持轻薄长板形态，突出物块运动）。
*   缩放仍沿用“按板长自适应”方式，任意板长下比例协调、不破坏画面。

---

## 七、 代码质量与交互细节优化

### 11. 清理受力分析绘制冗余分支
*   移除 `drawForces()` 中与外层条件重复的内层 `if (|v_B| > 1e-6)` 及不可达的 `else` 分支；B 运动时，地面摩擦力直接按其对地运动方向计算。仅作代码整洁，不影响功能。

### 12. 动画结束后参数直接可调（无需先点"重置"）
*   动画结束（结束态）后，参数滑条恢复可用。
*   用户拖动任一参数滑条时，页面自动回到"参数预览"状态：场景复位至初始位置、v-t / s-t 图清空为占位提示、数据面板刷新、运动状态复位为"待开始"、按钮恢复"开始"可用状态。
*   仍可点击"重置"或直接改参后点"开始"重播。

### 13. v-t 纵轴负值处理与 s-t 一致
*   v-t 图纵轴改为与 s-t 图一致的规则：仅在数据出现负速度时才向下扩展 20%；全部为正时纵轴从 0 起始，移除无意义的负值预留区。

---

## 八、 物理基准：标准板块模型情形归纳（教学对照）

> 本页面对应高中“板块模型”的标准教学逻辑，以下为正方向约定与三分支判定，供实现与验收对照。

### 14. 正方向约定与初速度设定
*   正方向：**水平向右为正**。
*   物块 A 初速度 $v_{0A}$：水平向右（> 0）。
*   板块 B 初速度 $v_{0B}$：默认 0（可调范围 -10 ~ 10 m/s，负值表示向左）。

### 15. 标准情形三分支
按“地面与板块之间有无摩擦”，以及“物块给板的摩擦力 vs 地面给板的摩擦力”分情况：

**情形一：地面与板间无摩擦（$\mu_2 = 0$）**
*   物块 A：受 B 的摩擦力向后 → 匀减速（$a_A = \mu_1 g$）。
*   板块 B：受 A 的摩擦力向前 → 匀加速（$a_B = \mu_1 m_A g / m_B$）。
*   板足够长时：**相对滑动停止（达到共速）** → 之后一起做匀速直线运动。
*   措辞约定：本情形中的“物块在板上停下来”指 **相对板块停止滑动（共速）**，并非速度减为零。

**情形二：地面与板间有摩擦（$\mu_2 > 0$）**
比较 物块给板的摩擦力 $\mu_1 m_A g$ 与 地面给板的摩擦力 $\mu_2 (m_A + m_B) g$：

*   **2a. $\mu_1 m_A \le \mu_2 (m_A + m_B)$（物块给板的摩擦力 ≤ 地面给板的摩擦力）**
    *   板块 B：保持静止（不动）。
    *   物块 A：匀减速滑动；板足够长时，A 在板上减速至 0 停下，B 始终不动。
*   **2b. $\mu_1 m_A > \mu_2 (m_A + m_B)$（物块给板的摩擦力 > 地面给板的摩擦力）**
    *   物块 A：匀减速（$a_A = \mu_1 g$）。
    *   板块 B：匀加速（$a_B = [\mu_1 m_A - \mu_2 (m_A + m_B)] g / m_B$）。
    *   板足够长时：达到共速 → 之后 **一起匀减速**（$a = \mu_2 g$）→ 最后停下。

### 16. 锁死条件与 slipAfter 的关系（教学说明）
*   共速后要“一起匀减速”，A 需要的静摩擦力为 $f_{req} = m_A \mu_2 g$，最大静摩擦力为 $\mu_1 m_A g$，故 **锁死条件为 $\mu_1 \ge \mu_2$**。
*   **标准情形 2b 已蕴含 $\mu_1 > \mu_2 (1 + m_B / m_A) > \mu_2$**，因此标准板块模型（$v_{0B} = 0$）下共速后必然可锁死，不会出现“再次相对滑动”。
*   “共速后无法锁死、再次相对滑动（slipAfter）”仅在双初速度场景（$v_{0B} \neq 0$）或 $v_{0A} = v_{0B}$ 时出现，属于双初速扩展的正确物理行为，并非标准情形的默认结果。

---

## 九、进一步优化需求（待确认）

### 17. 精确计算物块中心过缘时刻
*   当前 `tCenter` 通过滑出时刻与滑出瞬间相对速度倒推，低速滑出时存在近似误差。
*   改为在事件驱动积分中直接求解中心过缘事件：
    *   从前缘滑出：相对位移达到 $L - b_A/2$；
    *   从后缘滑出：相对位移达到 $-b_A/2$；
    *   其中 $b_A = L \times 8\%$ 为物块 A 的可视宽度。
*   `tCenter` 仅用于视觉下落起点，不改变物理滑出事件、相对滑移、摩擦生热和滑出判定。

### 18. 下落过渡期间的受力箭头协调
*   在 `tCenter` 至物块落地的 0.15 s 下落阶段，受力箭头不得继续固定在原板面高度而与物块明显错位。
*   下落阶段可采用以下方式之一：
    *   隐藏或降低 f₁/f₁′ 箭头透明度；
    *   让 f₁/f₁′ 箭头随物块 A 的垂直位置同步下降。
*   物块完全落地后，切换为 A、B 各自受到的地面摩擦力箭头。

### 19. “B 静止、A 停在板上”情形的数据语义优化
*   当 $\mu_1 m_A \le \mu_2(m_A+m_B)$、B 始终静止且 A 在板上停止时：
    *   “共速时刻”卡片改为显示 **A 停止时刻**；
    *   “共速后速度”卡片显示 **0.00 m/s**；
    *   或将两个卡片改为更通用的“相对运动结束时刻”和“结束共同速度”，覆盖共速、A 停止和滑出等分支。
*   结束提示仍保留“B 保持静止，A 在板上停止”的物理说明。

### 20. s-t 图长时间滑出场景的可读性优化
*   保持 s-t 图按完整轨迹固定纵轴，不改变当前坐标范围规则。
*   当 $\mu_2=0$ 且 A 滑出后继续匀速、导致 A 位移远大于 B 位移时：
    *   在 A、B 曲线末端增加最终位移数值标注；或
    *   在实时数据显示面板增加“最终位移 A/B”数据项。
*   优先选择不改变坐标比例的方式，避免破坏 A/B 位移的真实尺度关系。

### 21. 浏览器级视觉验收
*   在代码完成后进行实际浏览器渲染检查，重点验收：
    *   板厚 24 px 时板块文字是否拥挤或超出；
    *   物块高度 34 px 时 A 的名称和速度文字是否清晰；
    *   下落阶段受力箭头是否与物块错位或遮挡；
    *   移动端 v-t/s-t 图表单列堆叠后的高度、文字和数据面板是否正常。
*   若测试环境无法提供浏览器自动化，则至少保留 JS 语法、DOM 结构、关键场景冒烟测试，并记录未完成真实截图验收的风险。
