# 需求：共点力平衡演示 v3（角度范围扩展至75° + 运动轨迹）

> 在 v2（`requirement-force-equilibrium-v2.md`）基础上扩展。未提及的部分（视觉风格、布局、物理模型与公式、图表、颜色约定、按钮状态矩阵、v1/v2 验收中未被本文件修订的条款）维持不变。

## 背景与目标
- **角度范围扩展**：目标夹角最大值从 60° 调整为 75°，覆盖更大角度范围的教学需求。
- **运动轨迹可视化**：自动演示过程中记录小球运动轨迹，随动画逐步显现，直观展示小球沿圆弧移动的路径。

## 修订清单

### R1 角度范围扩展
- `TH_MAX` 常量从 60 改为 75。
- 目标夹角滑条 `max` 从 60 改为 75。
- 公式卡片中曲线范围描述从"0~60°"更新为"0~75°"。

### R2 小球运动轨迹
- 新增 `trail` 数组存储轨迹点 `[{x, y}]`。
- 自动演示每帧记录小球当前位置到 `trail`，上限 500 点防止内存溢出。
- 暂停恢复时追加当前点，保证轨迹连续。
- 重置时清空 `trail`。

### R3 轨迹绘制
- 轨迹在绳和小球之前绘制（不遮挡主体）。
- 轨迹由两部分组成：
  - **连线**：蓝色细线（`rgba(37,99,235,0.35)`，1.5px），连接所有轨迹点。
  - **圆点**：蓝色渐变圆点（半径 2.5px），越新的点 alpha 越大（0.15→0.70），体现运动方向。
- 图例新增"运动轨迹"说明项。

## 验收标准
1. 目标夹角滑条可拖动至 75°。
2. 自动演示过程中，小球后方出现蓝色渐变轨迹点和连线，随动画同步显现。
3. 暂停/继续后轨迹连续无断裂。
4. 重置后轨迹清空。
5. 图表范围覆盖 0~75°，曲线正确延伸。
6. v1/v2 所有功能不回归（竖直悬挂、缓慢拉动、目标角语义、平行四边形、力矢量等）。
