# 需求：倾斜传送带演示 v4（漏洞修复与物理模型修正）

> 在 v3（`requirement-inclined-conveyor-v3.md`）基础上修复已知问题。未提及的部分（视觉风格、布局、DOM 结构、图表、控件功能）维持 v3 不变。

## 背景与目标
- 对 `inclined-conveyor-belt.html` 进行全面代码审查，修复 UI 逻辑 Bug、物理模型缺陷、数值稳定性隐患和文档不一致问题。

## 修复清单

### F1 暂停态"重新开始"按钮逻辑修复
- **问题**：暂停时 `bStart` 文本变为"⟲ 重新开始"，但点击无效（`start()` 在 `phase === 'pause'` 时直接 return）。
- **修复**：`start()` 中 `phase === 'pause'` 时调用 `reset()` 后正常启动，用户可直接从暂停态重新演示。

### F2 初始帧 HUD 值修正
- **问题**：`reset()` 中手动计算 `acc`、`fMag`、`fType`，与物理引擎可能不同步。
- **修复**：`reset()` 末尾调用 `step(0)` 让物理引擎实际计算初始状态值，确保 HUD 显示值来自引擎。

### F3 离带后物理模型修正
- **问题**：物块离带后以恒定速度运动（匀速），忽略了重力沿斜面分量。
- **修复**：离带后应用 `a = -g·sinθ`（仅重力沿斜面分量，无摩擦），改为匀变速运动。
- 标签文字从"匀速"更新为"匀变速"。

### F4 共速解析解零值保护
- **问题**：`h1 = (vBelt - v) / a` 在 `a ≈ 0`（μ 恰好等于 tanθ 且 vrel < 0）时可能产生 NaN。
- **修复**：同步穿越条件增加 `Math.abs(a) > 1e-12` 保护。

### F5 头部注释修正
- **问题**：注释提及"g 5–15 默认 10"暗示有 g 滑条，实际不存在。
- **修复**：改为"g = 10 m/s²（固定值）"。

### F6 addScratch 区间合并不完全修复
- **问题**：原实现只合并第一个重叠区间，多个重叠区间可能残留。
- **修复**：重写为完整区间合并算法，遍历所有区间，一次合成单一重叠区间。

### F7 prevQ 每帧更新
- **问题**：`state.prevQ` 仅在物块在带时更新，离带态残留过时值。
- **修复**：`prevQ` 更新移至刮痕记录逻辑外，每帧无条件更新。

## 验收标准
1. 暂停后点击"重新开始"可直接重置并启动。
2. 初始 HUD 值与物理引擎实际输出一致。
3. 物块离带后做匀变速运动（加速度 = -g·sinθ）。
4. μ = tanθ 且 vrel < 0 时不产生 NaN。
5. 注释与实际功能一致。
6. 长时间运行无冗余擦痕区间累积。
7. 所有原有功能不回归（θ角、摩擦力方向、共速、v-t/s-t 图、滚筒动画等）。
