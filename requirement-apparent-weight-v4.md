# 需求：超重和失重演示网页 v4（主画布文字与图表框对齐修订）

> 在 v3（`requirement-apparent-weight-v3.md`）基础上修订主画布读数文字样式与图表区对齐。未提及部分维持 v3 不变。

## 一、修订内容

### 1. 主画布：去掉视重读数气泡外框
- 删除 "视重 = xxx N" 外面的圆角矩形边框与深色背景，只保留文字。
- 文字颜色仍随状态变化：超重橙 `#ff9933` / 正常白 `#ffffff` / 失重蓝 `#66aaff` / 完全失重青 `#33ddcc`。
- 字号维持 13px 粗体；文字仍在电梯右侧，垂直位置随轿厢中心（极端高度时保持画布内）。

### 2. 主画布：左侧加速度文字与视重文字样式对齐
- 加速度文字（含 `a = 0` 标注）字号由 12px 常规改为 13px 粗体，与视重文字一致。
- 加速度文字基线与右侧视重文字基线始终同一水平线（共用同一基线计算，轿厢在顶部/底部时也对齐）。
- 位置不变：加速度文字在电梯左侧（有向线段旁），视重文字在电梯右侧。

### 3. 图表区：蓝色图框与主画布两侧对齐
- 左侧 v-t 图蓝色外框左竖线 = 主画布蓝框左竖线（左对齐）。
- 右侧 F-t 图蓝色外框右竖线 = 主画布蓝框右竖线（右对齐）。
- 实现：图表面板（chart-panel）去掉包裹层的横向内边距与边框，蓝色图框直达主画布两侧边缘；面板标题移至图框上方；两图中缝 14px 保持不变。
- 窄屏单列堆叠时同样与主画布左右对齐。

### 4. 图例同步
- "视重气泡：超重/正常/失重/完全失重" 4 项改为 "视重读数：超重/正常/失重/完全失重"（颜色含义保留在文字颜色上）。

## 二、验收标准
1. 主画布无气泡矩形框（无深色底、无边框），仅 "视重 = xxx N" 文字，颜色随状态变化。
2. 加速度文字为 13px 粗体，基线与视重文字基线全程同水平线。
3. v-t 图蓝框左缘与主画布左缘对齐；F-t 图蓝框右缘与主画布右缘对齐；中缝不变。
4. 图例 4 项为 "视重读数：..."，无 "视重气泡" 字样。
5. v3 全部验收标准无回归（速度无显示、左侧加速度有向线段规则、气泡仅视重一项等）。
6. 交付时同步 `_repo` 仓库并推送，线上生效。
