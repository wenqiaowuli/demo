# 需求：水平直线运动演示网页

## 目标
- 新建网页：`horizontal-linear-motion.html`（水平直线运动演示｜高中物理）
- 风格参照：`vertical-throw-demo.html`（竖直上抛运动演示网页）——深色主题、上中下三段式布局
- 物理内容：水平方向匀变速直线运动（加速 / 匀速 / 减速），展示位移、速度、加速度及 v-t / s-t 图像
- 交付目录：`/home/wq/physics-work/horizontal-linear-motion.html`
- 在线地址（部署后）：`https://wenqiaowuli.github.io/demo/horizontal-linear-motion.html`

---

## 一、视觉风格（对齐竖直上抛）
1. 深色主题：`#121212` 背景、白字、`system-ui` 字体。
2. 主画布：深蓝底 `#0b1828`、蓝色边框 `#3388dd`、圆角、位于页面顶部整宽（880:520 比例自适应，水平轨道场景可适当调整，保持整宽）。
3. 面板：蓝调半透明分组（`rgba(255,255,255,0.05)` + 细边框），主色 `#3388dd`。
4. 按钮：主色 `#3388dd`，带 hover / disabled 态，居中一行。
5. 底部图例（legend）说明各元素颜色。

## 二、页面布局（上中下三段式）
- 整体自上而下排列（页面居中，max-width 定宽）。
- **第 1 段（上部）**：主场景画布——水平直线轨道：坐标轴、原点、刻度标尺，向右为正方向；运动物体（小球/小方块）沿轨道运动，绘制当前位置、速度矢量箭头（红）、加速度矢量箭头（橙）、已走轨迹/参考点。
- **第 2 段（中部）**：主画布正下方并排两张副图——左侧 v-t 图、右侧 s-t 图，深色风格，随运动实时延伸并带当前点标记。
  - 两图外框颜色统一，与动画演示区外框一致（蓝色 `#3388dd`）。
- **第 3 段（下部）**：控制区，左右两面板并排（对齐竖直上抛 control-panel 的左右比例）。
  - 左侧：参数设定面板。
  - 右侧：实时数值数据网格。
- **页面最底部（整宽）**：图例（legend）。

## 三、可调参数
| 参数 | 范围 | 步长 | 默认值 | 说明 |
|------|------|------|--------|------|
| 运动状态（加速度 a） | −6 ~ +6 m/s² | 0.1 | 0 | 单一带符号加速度；加速/减速状态由 `a×v` 与速率变化判断 |
| 初速度 v₀ | −10 ~ +10 m/s | 0.5 | 0 | 带符号：正=向右、负=向左 |
| 播放速度 | 0.1 ~ 2× | 0.1 | 0.5 | 与竖直上抛一致 |

- 运动状态采用**单一加速度滑条** `a`（−6 ~ +6 m/s²），不设独立的三按钮切换。
- 正方向固定为**向右**。
- 播放速度运行中允许实时调节，只改变动画推进速度，不重置物理时间、位置或轨迹。

## 四、主场景演示窗口
1. 水平坐标轴、原点（x=0）、刻度标尺（0 / 各整数刻度），向右为正。
2. 运动物体沿轴从原点出发运动。
3. 绘制内容：
   - 运动物体（白色小球，半径约 `7px`，与竖直上抛网页风格一致）
   - 速度矢量箭头（红色，向右为正、向左反向）
   - 加速度矢量箭头（橙色，恒指向 a 方向；a=0 时不绘制或显示 a=0）
   - 当前位置对应轨迹参考
4. 边界处理：物体中心越过画布左、右可视边界后进入 done 态并停止；重置恢复到 `x=0、t=0`。
5. 状态提示依据速率变化判断：
   - `a = 0`：匀速；若同时 `v = 0` 则物体静止；
   - `a×v > 0`：加速（速率增大）；
   - `a×v < 0`：减速（速率减小）；
   - `v = 0 且 a ≠ 0`：从静止开始加速。

## 五、图表窗口
- **v-t 图**：直线，斜率 = a，截距 = v₀；当前点实时标记。
- **s-t 图**：抛物线 s = v₀t + ½at²，开口方向由 a 决定；当前点实时标记。
- 两图随时间推进实时延伸，直到运动结束（done）为止。
- 坐标轴范围自适应：v-t 纵轴包含 `v₀`、当前速度和 0；s-t 纵轴包含正负位移和 0；横轴显示最近 10 秒滑动窗口。

## 六、参数设定面板（左卡）
- 运动状态（加速度 a）滑条
- 初速度 v₀ 滑条
- 播放速度滑条
- 公式说明框（fixed-note）：
  - 匀变速公式：v = v₀ + at；s = v₀t + ½at²
  - 正方向约定：向右为正
  - 速度为零时刻：t₀ = −v₀/a（仅当 a ≠ 0 且 t₀ > 0 时显示）
  - 转向点有符号位移：s₀ = −v₀²/(2a)（仅当 a 与 v₀ 反向时显示）
  - 转向点位移大小：|s₀| = v₀²/(2|a|)

## 七、实时数值面板（右卡，实时更新）
| 名称 | 含义/公式 |
|------|-----------|
| 时间 t | 当前时刻 |
| 位移 s | s = v₀t + ½at² |
| 速度 v | v = v₀ + at（向右为正） |
| 加速度 a | 当前加速度（滑条值） |
| 运动状态 | 加速 / 匀速 / 减速（依据 `a×v` 与速率变化） |
| 速度为零时刻 | t₀ = −v₀/a（仅当 a ≠ 0 且 t₀ > 0 时显示，否则“—”） |
| 转向点位移 | s₀ = −v₀²/(2a)，保留正负方向（仅当 a 与 v₀ 反向时显示，否则“—”） |

## 八、操作按钮
- 开始 / 暂停 / 继续 / 重置（四按钮，含 disabled 逻辑，与竖直上抛一致）。
- 运行中禁用参数滑条（v₀、a）；播放速度滑条允许实时调节，不改变物理状态。

## 九、标题 / 元信息
- 标题：`水平直线运动演示｜高中物理`
- 加 description meta，描述页面功能。

---

## 十、物理公式（实现参考，勿改动）
- v = v₀ + a·t
- s = v₀·t + ½a·t²
- 速度为零时刻（a ≠ 0 且 t₀ > 0）：t₀ = −v₀/a
- 转向点有符号位移（a 与 v₀ 反向）：s₀ = −v₀²/(2a)
- 转向点位移大小：|s₀| = v₀²/(2|a|)
- 正方向：向右为正；运动状态由 `a×v` 判断，不能只依据 `a` 的正负判断加速或减速

## 十一、验收标准
1. 页面深色主题，与竖直上抛网页风格一致。
2. 参数：加速度 a 滑条（±6 m/s²，单一滑条）、初速度 v₀ 滑条（±10 m/s）、播放速度滑条可调且实时联动。
3. 主画布动画正确：向右为正、物体沿轴运动；a 与 v₀ 反向时先减速到零再反向加速。
4. v-t / s-t 副图深色风格，随播放同步；横轴显示最近 10 秒，s-t 图不裁剪负位移。
5. 实时数值面板正确更新（t / s / v / a / 运动状态 / 速度为零时刻 / 转向点位移）。
6. 四按钮开始/暂停/继续/重置正常；物体中心越界后进入 done 态并可重置。
7. 标题为“水平直线运动演示｜高中物理”。
8. `a=0` 时正确表现为匀速或静止；`a×v` 正负正确对应速率增大或减小。

## 十二、参考文件
- 风格参照：`/home/wq/physics-work/vertical-throw-demo.html`
- 交付目录：`/home/wq/physics-work/horizontal-linear-motion.html`
