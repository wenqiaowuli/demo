# 平抛运动演示网页 · 修改需求

## 1. 背景与目标
- **对标文件**：`projectile-motion.html`（平抛运动演示｜高中物理）。
- **参考风格/内容**：`vertical-throw-demo.html`（竖直上抛演示）。
- **目标**：使平抛页在「显示内容」与「交互/视觉风格」上与竖直上抛页保持一致，并消除两页间的参数不一致。
- **已核对**：之前 `平抛演示检查结果.md` 指出的"竖直位移 / 离地高度"标签矛盾，当前代码已改为「离地高度 y」，无需再改；公式 `x=v₀t`、`y=h−½gt²`、`tEnd=√(2h/g)`、`range=v₀·tEnd` 与默认参数自洽，物理正确。

## 2. 已确认的关键决策（用户确认于 2026-09-11）
- **重力加速度 g 固定为 10 m/s²**：将现有 `const g = 9.8` 改为 `const g = 10`，作为常量固定，**不引入 g 切换下拉**。→ 与竖直上抛页默认 g=10 统一。
- **v-t 图、s-t 图 必须实现**（双图联动，缺一不可）。

## 3. 功能需求

### FR0 正方向约定（关键，全页统一）
- **水平方向**：向右为 `+x` → `vₓ` 为正（`= +v₀`），水平位移 `x` 为正（`= +v₀t`）。
- **竖直方向**：取**竖直向下为正**（与平抛下落方向一致）→ `vᵧ` 为正（`= +gt`），竖直位移 `Δy` 为正（`= +½gt²`）。
- **速度的竖直正方向与位移的竖直正方向保持一致**（均向下为正），两者同号。
- 由此：方向角 `θ = atan2(vᵧ, vₓ)` 为正（水平线向下的偏转角）。
- 「离地高度 `= h − Δy`」是高度而非位移，仍为正值保留显示（落地时为 0），与 `Δy` 区分；其标签不再带 `y`，避免与"竖直向下为正"的轴名混淆。

### FR1 重力加速度 g
- 平抛页重力加速度由 `9.8` 改为 `10` m/s²。
- 页面中如显示"a = g"等读数，应跟随该常量。
- 默认参数（h=45 m，v₀=20 m/s）下：落地时间 `tEnd = √(2h/g) = √(90/10) = 3.00 s`，水平射程 `range = v₀·tEnd = 60.00 m`（原 9.8 时为 3.03 s / 60.61 m，需同步更新初始读数）。

### FR2 速度-时间图（v-t）【必须】
- 在画布下方新增「速度-时间图 (v-t)」面板，**只渲染两条曲线（不显示合速度）**：
  - `vₓ`（水平分速度，**正**，恒定水平直线）；
  - `vᵧ`（竖直分速度，**正**，`vᵧ = gt`，随时间线性增大）。
- 纵轴范围 `0 ~ vMax`（均为正值，曲线位于零线以上）。
- 带"当前时刻"竖直游标线 + "当前点"圆点 + 数值气泡（气泡标注 `vᵧ`）。
- 坐标轴：横轴 `t/s`，纵轴 `v/(m/s)`，含网格与刻度。
- 渲染风格与竖直上抛页 `drawChart` 一致（深色网格 `#1f2d3d`、刻度文字 `#8899aa`、当前点 `#ff5555`、游标半透明蓝）。

### FR3 位移-时间图（s-t）【必须】
- 新增「位移-时间图 (s-t)」面板，渲染：
  - `x-t`（水平位移，**正**，随时间线性增大的直线）；
  - `Δy-t`（竖直位移，**正**，`Δy = ½gt²`，从 0 增至 `h` 的抛物线）。
- **不使用**「离地高度」作为 s-t 的竖直曲线（它是高度不是位移，与图名不符）。
- 纵轴范围 `0 ~ sMax`（按 `max(射程, h)` 取整齐值），曲线均位于零线以上。
- 同样带当前时刻游标、当前点圆点与数值气泡（气泡标注 `Δy`）；坐标轴横轴 `t/s`，纵轴 `s/m`。
- 与 FR2 并列，响应式（窄屏单列堆叠）。

### FR3.1 实时数值区符号与读数
- 「竖直速度 `|vᵧ|`」改为「竖直速度 `vᵧ`」，**显示正值**（`gt`，向下为正）。
- 新增「竖直位移 `Δy`」读数项（显示 `½gt²`，正值，落地时 `= h`）。
- 「离地高度」保留（正值，`h − ½gt²`，落地时为 0），标签去掉 `y`。
- 「方向角 `θ`」按 `atan2(vᵧ, vₓ)` 计算，显示为正值（水平线下方偏转角）。

### FR4 键盘快捷键（与竖直上抛页一致）
- **空格**：播放 / 暂停（toggle）。
- **R**：重置。
- 焦点在输入框 / 按钮时不触发，避免误触（与竖直上抛页逻辑一致）。

## 4. 风格 / 一致性需求
- 保持平抛页现有深色主题（`#121212` 底、`#3388dd` 强调、卡片面板、DPR 高清画布、ResizeObserver、开始/暂停/继续/重置四键、无障碍标签）不变。
- 新增图表沿用竖直上抛页的图表视觉语言与 `drawChart` 实现，保证系列统一。
- 图表区布局建议：在「控制面板」下方新增 `charts-row`（两套 chart-panel，宽屏并排、窄屏单列），与竖直上抛页结构对齐。

## 5. 可选 / 非必须（建议，不阻塞验收）
- 图例 emoji（🟢🟠🔵）改为彩色圆点 + 文字，与竖直上抛页风格一致。
- v-t 图旁补一句「vₓ 恒定、vᵧ 匀加速 (a=g)」说明，呼应竖直上抛页对"加速度恒定"的标注。

## 6. 非目标（本次不做）
- 不重构为含重力下落 + 桌面反弹的真实乒乓/抛掷物理，保持现有"水平抛出"模型。
- 不加 g 切换下拉（按确认固定 g=10）。

## 7. 验收标准
- [ ] 页面重力加速度显示为 10 m/s²，落地时间 / 射程按 g=10 计算（默认 h=45、v₀=20 → `tEnd=3.00 s`、`range=60.00 m`）。
- [ ] v-t 图只显示 vₓ / vᵧ 两条曲线（**均为正**、无合速度），动画推进时有当前点游标与数值气泡。
- [ ] s-t 图显示 x-t（直线）与 Δy-t（抛物线，落至 `h`），带当前点游标。
- [ ] 实时数值区「竖直速度 vᵧ」为正值，新增「竖直位移 Δy」读数（落地时 `= h`），「离地高度」落地时为 0。
- [ ] 空格 / R 快捷键可用且不与输入框冲突。
- [ ] 物理公式与数值自洽，无回归；原有轨迹、矢量箭头、落地点标记等行为保持不变。
