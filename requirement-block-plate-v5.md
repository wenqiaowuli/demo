# 需求：板块运动演示 v5（物块信息外移 / 摩擦力方向箭头与数值显示 / 物块放大 1/10）

> 本需求由用户于 2026-09-13 确认，基于 v4 需求文件修订。
> 待修改文件：`/home/wq/physics-work/_repo/block-plate.html`

---

## 一、物块上不再显示速度
1. 移除物块 A 上的速度文字（板上与滑出后两处），物块上仅保留名称。
2. 速度继续由"实时数据显示区"的"物块 A vA"卡片承载（现状保留）。

## 二、物块摩擦力箭头：只示方向、不示大小
1. 物块 A 受到的摩擦力箭头改为**固定长度**，仅指示方向（向左/向右），
   长度不再与力的大小成正比。
2. **箭头线段长度 = 物块长度的 3 倍**（3 × 1.1 m = 3.3 m，随比例尺换算像素）。
3. **箭头颜色为红色 #ff4444**（与地面摩擦 f₂ 的橙红 #ff6644 相区别）。
4. 适用状态：板上滑动（滑动摩擦 f₁）、共速锁死（静摩擦 f₁）、
   滑出后地面摩擦（μ₂ > 0，标 f₂）；物块不受摩擦时不画箭头。
5. 下落过渡期的箭头渐隐/渐入透明度逻辑保留。
6. 箭头旁保留符号标注（f₁ / f₂），不标注数值。
7. 板上 f₁′（反作用力）与板身地面 f₂ 箭头**维持现状**
   （长度与力大小成正比、原配色），本次仅改物块上的箭头。

## 三、摩擦力大小进入"实时数据显示区"
1. 新增数据卡片 **"A 摩擦力 f₁"**：实时显示物块受摩擦力大小（N，两位小数）。
   - 板上相对滑动：μ₁·m_A·g；
   - 共速锁死且 μ₂ > 0：静摩擦 m_A·μ₂·g；
   - 滑出后且 μ₂ > 0：μ₂·m_A·g；
   - 不受摩擦（如 μ₂ = 0 且已滑出）：0.00。
2. idle 预览态显示 "—"；运行/结束态显示实时/末值；重置后恢复 "—"。
3. 数据面板由 10 张卡片增至 **11 张**（3 列网格自动换行）。

## 四、物块再放大 1/10
1. 物块世界尺寸由 1.0 m → **1.1 m 正方形**（BLOCK_W_M = 1.1），
   保持"正方形、不随板长/运动/缩放变化"约定。

## 五、板默认板长跟随 20 倍关系
1. 默认板长 = 20 × 1.1 m = **22 m**（滑条默认值 20 → 22，范围 2–40 m 不变）。

## 六、验收标准
1. 物块上无速度文字；速度在数据面板正常显示。
2. 物块摩擦力箭头为红色 #ff4444、定长 3.3 m（约 39 px），方向正确
   （A 比 B 快时向左；锁死减速时与运动反向；滑出后与 vA 反向），
   长度不随 μ/质量变化。
3. 数据面板 11 张卡片；滑动阶段"A 摩擦力 f₁"显示 3.92 N（默认参数
   μ₁=0.2、m_A=2）；重置后为 "—"。
4. 物块像素约 15×15（1.1 m × scale + 描边），板长 2/22/40 m 下不变。
5. 默认板长显示 22.0 m；默认参数下相对滑移 17 m < 22 m 不滑出，
   共速 5.33 m/s 后匀速，约 t ≈ 9.1 s 穿出右界结束（与 v4 一致）。
6. 回归：双摩擦、锁死/slipAfter、生热、越界停止、移动端自适应均不受影响。
