# 需求：板块运动演示（图表蓝框与主画布对齐）

> 按照超重和失重演示页 v4 的界面风格，调整板块运动演示（`block-plate.html`，以 `_repo` 当前版本为准：charts-row 双图布局）图表区对齐。其余内容不变。

## 一、修订内容

### 图表区：蓝色图框与主画布两侧对齐
- 左侧 v-t 图（速度-时间图）蓝色外框左竖线 = 主画布蓝框左竖线（左对齐）。
- 右侧 s-t 图（位移-时间图）蓝色外框右竖线 = 主画布蓝框右竖线（右对齐）。
- 实现：图表面板（chart-panel）去掉包裹层的横向内边距与边框，蓝色图框直达主画布两侧边缘；面板标题移至图框上方；两图中缝保持不变。
- 窄屏单列堆叠时同样与主画布左右对齐。

## 二、验收标准
1. v-t 图蓝框左缘与主画布左缘对齐；s-t 图蓝框右缘与主画布右缘对齐；中缝不变。
2. 页面其余功能无回归（v9 地面位置、v10 默认初速度等既往修订保持不变）。
3. 交付时同步 `_repo` 仓库并推送，线上生效。

## 附注
- `physics-work/block-plate.html` 曾为旧版副本（9月1日），本次以 `_repo` 新版为基准修改，并已将新版回灌至 `physics-work/` 消除双副本不一致。
