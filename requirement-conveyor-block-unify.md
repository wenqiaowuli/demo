# 需求：水平/倾斜传送带演示 · 物块尺寸统一

## 背景与目标
- 水平传送带（`horizontal-conveyor-belt.html`）与倾斜传送带（`inclined-conveyor-belt.html`）两个演示页中的物块为同一样式（橙色渐变方块 + 描边 + m 标签），但绘制尺寸不一致：水平页 40×40 px、倾斜页 34×34 px。
- 目标：两页物块大小统一，视觉规格一致。

## 现状与决策
| 页面 | 现状 | 统一后 |
|------|------|--------|
| 水平传送带 | `blockW = blockH = 40`（drawBlock 内） | **40×40 px（保持不变）** |
| 倾斜传送带 | `const BLOCK = 34` | **34 → 40** |

- 统一基准取 **40×40 px（水平页现值）**，理由：
  1. 水平页擦痕偏移数学中硬编码 `halfW = (40/2)/G.pix`（物块半宽 40px），改水平页需同步改擦痕逻辑；改倾斜页则零连带。
  2. 倾斜页所有随物块的偏移（力箭头 `BLOCK+8`、标注 `BLOCK+30`、阴影、m 标签）均由 `BLOCK` 派生，改一处自动适配，40px 下仍在画布安全区内。

## 改动清单
- R1 `inclined-conveyor-belt.html`：`const BLOCK = 34` → `const BLOCK = 40`（注释同步）。
- R2 水平页不做任何改动。

## 保持不变
- 物块配色（渐变 `#d8a050→#a07030`）、描边（橙 `#ffaa44` / ORANGE）、m 标签、阴影样式。
- 倾斜页力箭头/标注与物块的相对布局（随 BLOCK 自动外移 6px）。
- 两页其余全部功能与布局。

## 验收标准
- [ ] 两页物块绘制尺寸均为 40×40 px（倾斜页随带面旋转后边长仍为 40px）。
- [ ] 倾斜页默认参数下物块、力箭头、标注无重叠或出界。
- [ ] 水平页无任何变化。
