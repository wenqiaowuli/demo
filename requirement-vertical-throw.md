# 需求：竖直上抛运动演示网页改造

## 目标
- 修改已有网页：`vertical-throw-demo.html`（位于仓库根，对应远程 GitHub 仓库 `wenqiaowuli-coder/physics` 的 `main` 分支）
- 改造后文件保存到 `/home/wq/physics-work/vertical-throw-demo.html`，并推送到远程仓库
- 风格统一切换为“高中物理”深色系列，与 `oblique-throw-motion.html`（斜抛运动网页）一致
- 物理内容严格限定高中教学范围

---

## 一、视觉风格
1. 深色主题：`#121212` 背景、白字、`system-ui` 字体。
2. 主画布：深蓝底 `#0b1828`、蓝色边框 `#3388dd`、圆角、位于页面顶部整宽（880:520 比例自适应）。
3. 面板：蓝调半透明分组（`rgba(255,255,255,0.05)` + 细边框），主色 `#3388dd`。
4. 按钮：主色 `#3388dd`，带 hover / disabled 态，居中一行。
5. 底部图例（legend）说明各元素颜色。

## 二、物理参数
6. 重力固定为 `g = 10 m/s²`（删除“重力加速度”滑块）。
7. 可调参数：
   | 参数 | 范围 | 步长 | 默认值 |
   |------|------|------|--------|
   | 初速度 v₀ | 5–40 m/s | 1 | 20 m/s |
   | 播放速度 | 0.1–2× | 0.1 | 0.5× |

## 三、页面布局（上中下三段式）
- 整体自上而下排列（页面居中，max-width 定宽）。
- **第 1 段（上部）**：主场景画布（竖直上抛动画：小球、上升时速度矢量【红】、下落时反向、恒定向下加速度矢量【橙】、地面线、标尺刻度）。画布比例 880:520，自适应缩放。
- **第 2 段（中部）**：主画布正下方并排放置两张副图——左侧 v-t 图、右侧 s-t 图，深色风格，带播放头同步；各自高度由实现方自适应（参考浅色原版约 180px 上下）。两图并排占中部整宽。
  - **颜色要求**：两张图的外框颜色统一，并与上方动画演示区外框颜色一致（蓝色 #3388dd）。
  - **对齐要求**（两图作为一个整体对齐动画区）：两张图并排（VT 左、ST 右），整体宽度与动画区同宽；VT 图左侧线框与动画区左边缘对齐，ST 图右侧线框与动画区右边缘对齐，两图之间留小间距。
- **第 3 段（下部）**：控制区，左右两面板并排（对齐斜抛网页 control-panel 的左右比例）
  - 左侧：参数设定面板（初速度 v₀ 滑条、播放速度滑条，各带实时数值标签）。
  - 右侧：实时数值数据网格。
- **页面最底部（整宽）**：图例（legend）说明各元素颜色。

## 四、实时数值面板（7 格 + 1 整行）
| 名称 | 含义/公式 |
|------|-----------|
| 时间 t | 当前时刻 |
| 高度 y | y = v₀t − ½gt² |
| 速度 v | v = v₀ − gt（向上为正） |
| 加速度 a | 恒为 −10 m/s² |
| 飞行时间 T | T = 2v₀/g |
| 落地速度 | = v₀（落回抛出点） |
| 最大高度 H | H = v₀²/2g（作整行 span-full 显示；最高点处 v=0） |

## 五、按钮
- 开始 / 暂停 / 继续 / 重置（四按钮，含 disabled 逻辑，与斜抛网页一致）。

## 六、标题 / 元信息
- 标题：`竖直上抛运动演示｜高中物理`
- 加 description meta，描述页面功能。

---

## 七、物理公式（实现参考，勿改动）
- y = v₀·t − ½gt²
- v = v₀ − g·t（向上为正）
- T = 2·v₀/g
- H = v₀²/(2g)
- 落地速度 = −v₀（大小 v₀）

## 八、验收标准
1. 页面深色主题，与斜抛网页风格一致。
2. 无重力滑块，g 固定 10；初速度、播放速度滑块可调且实时联动。
3. 主画布动画正确：上升减速、最高点速度 0、下落加速、落地。
4. v-t / s-t 副图深色风格，随播放同步。
5. 实时数值面板正确更新。
6. 四按钮开始/暂停/继续/重置正常。
7. 标题为“竖直上抛运动演示｜高中物理”。

## 九、参考文件
- 风格参照：`/home/wq/physics-work/_repo/oblique-throw-motion.html`
- 待修改原文件：`/home/wq/physics-work/_repo/vertical-throw-demo.html`
- 交付目录：`/home/wq/physics-work/vertical-throw-demo.html`