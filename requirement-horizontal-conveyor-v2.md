# 水平传送带演示网页 · 优化需求 v2

## 1. 背景与目标
- **对标文件**：`horizontal-conveyor-belt.html`（水平传送带演示｜高中物理）。
- **在线地址**：https://wenqiaowuli.github.io/demo/horizontal-conveyor-belt.html
- **前置**：v1（滚筒/环形带面/机架/v-t 拆面板/s相对+Q/落下动画/离带/快捷键/g=10/aria）已上线。
- **目标（v2）**：
  1. 物块**默认在传送带最左端开始**，被运往右端（演示全程带长）；
  2. 演示窗口下方放 **VT 图 + ST 图两张并排**，分别展示物块与传送带的速度、位移随时间变化；
  3. 滚筒**辐条增至 8 根**并加双轮缘、轴钉、旋转亮斑，更美观；
  4. 落实用户确认的补充项 A–H（见 FR）。
- **已核对（保持不变）**：相对速度→动摩擦方向、分段积分、共速判定、Q=μmg·s相对、v-t 面积阴影等物理逻辑。

## 2. 关键决策
- **物块放上位置成为情景属性** `state.x0`：默认/轻放/刹停 = 带左端内 1 m；反向放上 = 带中央（避免物块 v₀<0 时立即从左端滑出）。
- **传送带长度 L 可调**（5–40 m，默认 24 m），替代常量 `BELT_LEN_M`。
- **位移参考**：物块位移（地面系，从放上点起）= `state.x − state.x0`；传送带位移 = `v带·t`；两者之差 = 相对位移（与 v-t 面积一致）。
- **ST 图纵轴共用**（物块位移与带位移量级接近，可共用 0~sMax 整齐刻度）。

## 3. 功能需求

### FR1 物块默认从左端开始（必改）
- 初始 / 轻放 / 刹停情景：`state.x0 = −(L/2 − 1)`（左滚筒中心内侧 1 m），`state.x` 初始 = x0。
- 反向放上情景：`state.x0 = 0`（中央）。
- 出界判定不变：物块中心越出滚筒中心即离带（右端 `x > L/2`、左端 `x < −L/2`），随后匀速离带滑出。
- 物块在屏幕上的位置仍用地面位移 `state.x` 绘制（共速后物块在屏幕上匀速前进，视觉正确）。

### FR2 VT 图 + ST 图双面板（必改 + G）
- 演示窗口下方改为 `charts-row` 两列并排：左 **VT 图**、右 **ST 图**；窄屏（≤720px）单列堆叠（复用平抛/斜抛 charts-row 风格：深色卡片、蓝框 `#3388dd`、DPR）。
- **VT 图**（沿用 v1 逻辑）：物块 v 橙实线、带速 v带 青虚线、两线间阴影=相对位移、共速竖线标注、播放头、当前点。
- **ST 图（新增）**：
  - 物块位移 `x物 = state.x − state.x0`（橙实线，从 0 起）；
  - 传送带位移 `x带 = v带·t`（青虚线，从 0 起线性）；
  - 两线**间距 = 相对位移**，两线间画半透明阴影（与 VT 呼应）；
  - **共速时刻竖线**（与 VT 联动标注）、播放头、当前点；
  - 图内/图例注明「两线间距 = 相对位移」。
- `state.trace` 升级为 `[t, v, x物]` 三元组，VT 取 v、ST 取 x物；带位移由 `v带·t` 现算。

### FR3 滚筒美化（必改 + B）
- 辐条 **5 → 8 根**（等分 45°），随带速旋转（逻辑沿用）。
- **双轮缘**：在半径 R−1 与 R−3 处各画一圈细圆环（轮辋感）。
- **中心轴钉**：轮毂中心更亮的高光小圆 + 深色外圈。
- **旋转亮斑**：轮缘上画一个亮色标记点（角度随带速旋转），强化"正在转动"的视觉（v带=0 时静止）。

### FR4 带面滑动擦痕（D）
- 物块相对带滑动（`|v−v带|>ε` 且物块在带上）时，在带上记录**划痕区间**（带参考系坐标 `q = state.x − v带·t`，区间 = 物块覆盖的 `[q−w/2, q+w/2]`），与已有区间合并。
- 共速后 `q` 恒定 → 不再产生新划痕 → **划痕长度 = s相对**（经典考点：物块在带上留下多长痕迹）。
- 绘制：带面（上承载段）上以**半透明红色**画划痕段，位于带面厚度内。
- 物块滑出带后不再留痕；重置清空。

### FR5 物块 m 标签（C）
- 物块中央写 **m**（白色、居中），与斜面版受力分析风格一致。

### FR6 带长刻度与端部标记（E）
- 带下方（回程带之下、横撑附近）标注两端：左端 **0**、右端 **L m**；带中央下方标「带长 L = XX m」。
- 左侧放上点：在 `x0` 上方画小三角/箭头并注「放上点」（轻放情景语意）。
- 右滚筒外侧画「出料」箭头，指向带外。

### FR7 播放倍速滑条（F）
- 传送带参数卡新增「播放速度」滑条：范围 0.25–4，步长 0.25，**默认 1×**（物块全程约 8~9 s，需可调速）。
- 仅缩放 `loop` 的 dt，不改物理时间 t、公式与读数。

### FR8 传送带长度 L 滑条（H）
- 传送带参数卡新增「带长 L」滑条：范围 5–40，步长 1，默认 24。
- `BELT_LEN_M` 改为运行期取值 `state.L`；`pix = (xR − xL)/L` 动态。
- 改 L 后 `x0 = −(L/2 − 1)` 随之更新；运行中禁用；重置/改参实时重绘。

## 4. 布局 / 样式
- 主画布保持专注传送带（高 240）；下方 charts-row 两张并排（VT/ST），样式对齐平抛/斜抛。
- 新增元素配色沿用：物块 v/位移 橙 `#ffaa44`、带速/带位移 青 `#44ddff`、摩擦/擦痕 红 `#ff5577`、相对 绿 `#aaff66`、阴影半透明。
- 传送带卡内控件增多（带速、方向、带长 L、播放速度），按钮行、note 保持。

## 5. 非目标（本次不做）
- 擦痕随带面纹理滚动 / 绕滚筒循环（划痕固定在带矩形上，长度=s相对 即可传达教学点）。
- 物块质量可调、空气阻力。
- 时间轴拖动。
- 静摩擦-动摩擦细化。

## 6. 验收标准
- [ ] 初始/轻放/刹停：物块在带**左端**（左滚筒内 1 m）开始；反向放上：物块在**中央**开始。
- [ ] 演示窗口下方 **VT + ST 两张并排**（窄屏单列），样式与系列一致。
- [ ] VT 图显示物块 v 与带速 v带；ST 图显示物块位移与带位移 v带·t，两线间距=相对位移，均含阴影、共速竖线、播放头、当前点。
- [ ] 滚筒：8 根辐条、双轮缘、轴钉、旋转亮斑；v带=0 时静止，旋转方向随带向。
- [ ] 物块滑动时带面上出现红色划痕，共速后停止增长，划痕总长 = s相对 读数。
- [ ] 物块中央有 m 标签。
- [ ] 带下方标注 0 / L m 与「带长 L = XX m」；左端有放上点标记、右端有出料箭头。
- [ ] 播放速度滑条（0.25–4×，默认 1×）只调快慢不改物理数值。
- [ ] 带长 L 滑条（5–40 m，默认 24）实时生效，物块起点/刻度/离带判定跟随更新。
- [ ] 运行中滑条禁用；空格/R 快捷键、aria、g=10、s相对/Q 等 v1 行为不回归。
- [ ] 三情景解析解仍正确（轻放 s相对 = v带²/2a、Q = μmg·s相对 等）。
