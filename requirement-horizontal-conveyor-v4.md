# 水平传送带演示网页 · 修复需求 v4

## 1. 背景与目标
- **对标文件**：`horizontal-conveyor-belt.html`（水平传送带演示｜高中物理，仓库根目录）。
- **前置版本**：v1.2（离带后平抛 + 落地滑行至停止，提交 eb290af）。
- **背景**：对 v1.2 页面做全量漏洞与逻辑审计（物理积分、离带行为、图表口径、状态机、文案一致性），确认核心传送带物理正确（共速判定、分段积分、s相对/Q 数值），发现 5 处问题，本版全部修复。
- **原则**：不改动传送带本体物理模型（f = μmg、a = μg、共速后无摩擦匀速），仅修正离带段行为精度、图表口径、状态机与文案。

## 2. 审计结论（修复前）
已验证正确的部分：
- `step()` 共速前梯形/三角形分段积分：默认参数（v₀=0、带+3、μ=0.3）s相对 = 1.50 m、tSync = 1.00 s，与 |v₀−v带|²/(2μg) 完全一致。
- 物块快于带（v₀=+6）、对开（v₀=−6 与带反向被拖回）等场景的加速度方向与共速处理。
- 擦痕带参考系（q = x − v带·t，随带面平移，共速定格）、v-t/s-t 以带方向为正、状态机控件禁用矩阵、R 键重置、DPR 适配。

发现的问题：
1. **（P0）落地滑行只支持向右**：`step()` 落地滑行分支 `if (state.v <= dv)` 对 v<0 恒成立，向左落地瞬间判停。仿真：v=−3 落地应滑 −1.50 m，实际 −0.013 m。
2. **（P0）推导卡文案与 v1.2 实现矛盾**：卡片写"离带后做匀速直线运动"，实现为平抛 + 落地滑行。
3. **（P1）图表阴影与 s相对/Q 口径不一致**：v-t/s-t 阴影覆盖整个运行时段，物块落地减速段阴影继续增大而 s相对/Q 已定格。
4. **（P1）离带判定为帧级**：`loop()` 在 4 个子步积完后才判越界，越界后多算摩擦与 s相对（1× 约 0.02–0.15 m，4× 最坏约 0.6 m）。
5. **（P2）暂停态「重新开始」按钮无效**：`pause()` 启用 bStart 显示"重新开始"，但 `start()` 对 pause 态直接 return。

## 3. 修复方案

### F1 落地滑行双向减速（对应问题 1）
- 判停改为 `Math.abs(state.v) <= dv`，减速改为 `state.v -= Math.sign(state.v) * dv`。
- 落地滑行逻辑抽为 `stepOff(dt)`（平抛 → 落地双向滑行 → 停止），供 `step()` 各分支复用。

### F2 推导卡文案对齐实现（对应问题 2）
- 判断行改为："物块滑出带端后不再受带面摩擦力：离带后做平抛运动（水平匀速 + 竖直自由落体），落地后受地面滑动摩擦（沿用 μ）匀减速滑行直至停止，s相对 与 Q 在离带时定格。"
- 图例与两图表 aria-label 同步注明"阴影（离带后定格）""竖线：共速（绿）/ 离带（红）时刻"。

### F3 图表阴影离带定格 + 离带竖线（对应问题 3）
- 新增 `state.tExit`（离带时刻），`drawVT()/drawST()` 的 `shadeA`（物块曲线侧）过滤 `t ≤ tExit`，`shadePts` 闭合到 `(tExit, 带值)`——阴影面积/两线间距在离带后不再变化。
- `drawTimeChart()` 新增红色虚线竖线标注 `离带 t=…s`（与绿色共速竖线并列，颜色区分）。

### F4 离带判定精确到子步（对应问题 4）
- 匀加速段：解析求带端穿越 `beltEndCross(x0, v0, a)`——对 ±L/2 两端解 ½a·τ² + v₀·τ + (x₀−端点) = 0，取最短非负 τ；τ ≤ min(dt, tToSync) 时积分到端点（s相对 用端点速度梯形），离带后剩余时间转平抛。
- 共速段：`rideSynced(dt)` 按端点距离求穿越时间 tc，tc ≤ dt 时精确离带，剩余时间平抛。
- `loop()` 保留帧级兜底判定（正常路径不会触发）；擦痕扫掠在离带帧只记到带端（q 取端点带坐标），共速帧不再推进 prevQ。

### F5 暂停态重新开始生效（对应问题 5）
- `start()` 入口改为：run 态 return；pause/done 态先 `reset()` 再进入 run。
- 与系列页差异说明：竖直上抛/倾斜传送带页存在同样的"暂停态重新开始无效"问题，本页先行修复，系列页可后续跟进。

## 4. 保持不变
- 传送带本体物理（f = μmg、a = μg、共速后无摩擦匀速、带静止+v₀=0 直接结束）。
- 平抛参数（g=10 竖直、水平匀速）、落地滑行沿用 μ、离地高度 y 读数。
- 擦痕带参考系记录与绘制、起点按钮、滑条禁用矩阵、快捷键（空格/R）、aria 惯例。
- 880×520 画布、场景字体 sceneFont 窄屏缩放、图表 10 s 滑动窗。

## 5. 验收标准
- [ ] 反向带（v带=−3，默认其余）演示：物块左端离带 → 平抛落地 → 向左滑行 ≈1.0 m 后停止，总时长 ≈2.9 s（修复前 ≈2.1 s 瞬间冻结）。
- [ ] s相对/Q 与理论一致：默认 1.50 m / 4.50 J；对开（v₀=−6、带+3、L=40）1.523 m；1× 与 4× 倍速结果一致（子步级离带）。
- [ ] 推导卡、图例、aria-label 与实际行为一致（平抛 + 落地滑行 + 阴影/竖线口径）。
- [ ] v-t/s-t 阴影在离带时刻定格，红色虚线标注"离带 t=…"；共速竖线仍为绿色。
- [ ] 暂停 → 点「重新开始」= 重置并重新运行（t 归零重新增长）。
- [ ] 回归：默认场景 s相对=1.500、tSync=1.000、tExit=8.167、停点 ≈17.36 m、总时长 ≈10.45 s；带静止 + v₀=0 约 0.2 s 结束；运行中 R 键重置正常。

## 6. 验证记录
- Node 数值仿真（复刻 step/stepOff/rideSynced/beltEndCross）：6 场景（默认 / 反向带 / 物块快于带 / 对开 1× 与 4× / 带静止 / 右端出发）全部与解析理论吻合，见第 5 节数值。
- DOM 桩端到端（真实脚本 + 桩 canvas/rAF）：语法检查、初始化无异常、5 个交互场景（默认跑完 / 反向带跑完 / 暂停重开 / R 键重置 / 带静止快速结束）通过。
- 本环境无可用浏览器后端（`agent.browsers.list()` 为空），浏览器像素级验证留待上线前人工抽查。
