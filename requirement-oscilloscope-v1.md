# 需求：示波器演示网页（v1 最新版）

> 状态：v1 范围已确认（2026-09-20，五项开放问题全部裁定纳入）；本版根据总体复核建议完成结构化修订，统一了正交偏转区几何、模式一/模式二参数语义、扫描回扫与余辉规则、模式状态机、图表边界、理想模型边界和验收矩阵。2026-09-20 完成首版编码与验证后，追加 v1.1 审计修订要求（见第十六节）；v1.1 五项修订已全部实施并通过 88 项物理核 + 45 项浏览器端到端验证（记录见 16.7）。二次只读审计发现显示、暂停、无障碍和调试接口问题，追加 v1.2 审计修订要求（见第十八节）；**v1.2 六项修订已全部实施并通过 88 项物理核 + 65 项浏览器端到端验证（记录见 18.8），已推送 GitHub**。本文件是后续修改的唯一依据；后续如需增删主题功能，请另立 v2 需求文件。

## 目标
- 新建网页：`oscilloscope-demo.html`（示波器演示｜高中物理）。
- 主题：示波管（阴极射线管）的工作原理——电子枪加速、正交偏转、荧光屏成像；通过三个核心参数展示成像变化：加速电压 U1、竖直偏转电压/信号、水平偏转电压/扫描。
- 风格参照：`force-composition-demo.html`（力的合成与分解演示）——深色主题、上中下三段式布局、图表蓝框对齐、按钮矩阵与系列页交互约定一致。
- 物理内容：人教必修第三册 10.5 带电粒子在电场中的运动——示波管原理（动能定理加速 + 电场中的类平抛偏转 + 出电场后匀速直线 + 偏转量 ∝ U偏/U1）。
- 交付目录：`/home/wq/physics-work/oscilloscope-demo.html`。
- 在线地址（部署后）：`https://wenqiaowuli.github.io/demo/oscilloscope-demo.html`。
- 交付时同步：将页面复制到 `_repo` 仓库，并在 `_repo/index.html` 导航页新增本演示卡片（置于“动量守恒”之后，badge 为“电场 · 中级”）。

## 一、视觉风格与通用规范
1. 浅色主题（对齐 `requirement-light-theme-unify.md` 全系列统一约定；实现说明见 14.3）：页面背景 `#eef2f6`、正文墨色 `#1f2937`、`system-ui` 字体。
2. 主画布：白底 `#ffffff`、蓝色边框 `#2563eb`（2px）、圆角，整宽，880:520 比例自适应；DPR 高清画布 + `ResizeObserver` 响应式。
3. 场景画布按 880×520 设计坐标整体等比缩放（`fit = cssWidth / 880`）；场景文字通过 `sceneFont()` 等比缩放并保 10 px 可读下限；图表字号不随场景缩放（对齐 `requirement-mobile-narrow-screen-font-v1` 规范）。
4. 面板：白卡片 `#ffffff` + 边框 `#e5e7eb` + 轻阴影，主色 `#2563eb`；图表网格 `#e5e7eb`、刻度文字 `#6b7280`、当前点游标蓝 `#2563eb`。
5. 触屏控件：滑条和按钮保持可触控尺寸；拖拽目标在窄屏上仍须容易命中。
6. 键盘快捷键：**空格** 播放/暂停当前动画，**R** 重置当前模式；焦点在输入框、滑条或按钮时不触发快捷键。
7. 无障碍：按钮、模式切换、滑条带 `aria-label`；实时数值区带 `aria-live="polite"`；状态提示使用可读文本，不只用颜色表达。

## 二、页面布局与教学流程
- **第 1 段（上部）**：主场景画布。左约 63% 为示波管侧视结构图，右约 37% 为荧光屏正面放大图。两区之间画淡虚线，示意“管内电子束 → 屏上成像点/波形”的对应关系。
- **第 2 段（中部）**：主画布下方并排两张副图——左“出射速度 v₀–U1 图”，右“屏上偏移–偏转电压图”，右图支持 `Y–Uy/Ay` 与 `X–Ux/Ax` 切换；外框蓝色 `#3388dd`，图表面板比例约 20:9。
- **第 3 段（下部）**：左右两面板并排——左侧参数设定面板（模式切换、模式参数、播放速度、公式说明），右侧实时数值面板。
- **最底部（整宽）**：控制按钮行 ▶ 自动演示 | 暂停/继续 | 重置；按钮行之下为图例。
- **主场景固定教学阶段标记**：
  1. `① 电子发射与加速`
  2. `② 正交偏转`
  3. `③ 出射后匀速传播`
  4. `④ 荧光屏成像`
- 阶段标记始终可见；核心电子束、加速方向、偏转方向和屏上结果默认显示。速度箭头、板内侧移标注、完整电场线和辅助投影线归入“详细视图”辅助层，可用一个“显示教学辅助”开关显示/隐藏，默认显示在宽屏、窄屏默认隐藏以避免拥挤。
- **图例内容**：电子束（青）、荧光亮点/波形（荧光绿）、加速区电场线（黄）、偏转区电场线（橙）、极板（灰蓝）、管壳描边、模式一亮点拖尾、模式二波形余辉、待机可拖拽亮点、快捷键。

## 三、物理模型与统一几何约定
### 3.1 固定常量
1. 粒子为电子。电子电荷量用正值大小表示 `e = 1.6×10⁻¹⁹ C`，电子质量 `m = 9.1×10⁻³¹ kg`；初速度视为零。
2. 使用高中理想模型和非相对论公式；常量在公式说明框中固定显示。
3. 加速电压 U1 始终为正，范围 500~2000 V。

### 3.2 正交偏转区（必须按此定义实现）
1. 两组偏转极板不是沿电子运动方向前后串联的两个区域，而是**同一个正交偏转区内相互垂直的两组极板**：
   - 上下两块水平放置的极板（YY 极板）负责竖直偏转；
   - 左右两块竖直放置的极板（XX 极板）负责水平偏转。
2. 两组极板沿电子运动方向占据相同的有效长度 `L = 4 cm`，极板间距均为 `d = 1 cm`，共同从同一偏转区入口到同一出口；两组偏转场在理想模型中可独立叠加。
3. 偏转区出口到荧光屏的距离为 `L′ = 18 cm`。荧光屏有效范围为 `±6 cm × ±6 cm`，主网格 2 cm，副网格 1 cm。
4. 侧视图只直接绘制 YY 方向的上下极板和竖直轨迹；XX 极板在同一位置以透视/半透明交叉结构表示，不能画成位于 YY 区之后的第二个独立偏转区。水平偏转轨迹在侧视图中以虚线投影或“出纸面方向”标注，实际偏移在右侧荧光屏正面图中呈现。
5. 两个方向严格使用相同的几何系数，保证 X/Y 对称：
   - `K = L(L + 2L′)/(4d) = 0.4 m`
   - `X = K·Ux/U1 = 0.4·Ux/U1（m）`
   - `Y = K·Uy/U1 = 0.4·Uy/U1（m）`

### 3.3 方向与符号约定
- `Uy > 0`：上极板为正，电子受力向上，屏上亮点向上；`Uy < 0`：亮点向下。
- `Ux > 0`：右极板为正，电子受力向右，屏上亮点向右；`Ux < 0`：亮点向左。
- 模式二的 `Ay`、`Ax` 是非负幅度；瞬时电压由波形函数决定，正负方向由函数相位/扫描方向产生。
- 屏幕绘图坐标以屏中心为原点，数学坐标 y 轴向上；Canvas 像素坐标换算为 `screenPixel = center + (X, −Y)·pxPerCm`。

### 3.4 运动分解与成像公式
以任意一个偏转方向为例，另一个方向完全对称：
1. 加速：`eU1 = ½mv₀²`，因此 `v₀ = √(2eU1/m)`。
2. 偏转场：`E = U偏/d`，加速度大小 `a = eU偏/(md)`；进入极板后运动时间 `t = L/v₀`。
3. 板内侧移：
   - `s偏 = U偏L²/(4dU1)`；
   - 竖直方向记为 `y`，水平方向记为 `x`。
4. 出射角：`tanθ = U偏L/(2dU1)`。
5. 出偏转区后做匀速直线运动，屏上偏移：
   - `Y = (L/2 + L′)·tanθy = UyL(L+2L′)/(4dU1)`；
   - `X = (L/2 + L′)·tanθx = UxL(L+2L′)/(4dU1)`。
6. 教学要点（公式说明框必须呈现）：
   - U1 增大 ⇒ `v₀ ∝ √U1` 增大、通过偏转区时间变短、相同偏转电压下亮点/波形向中心收缩，偏转灵敏度 `K/U1 ∝ 1/U1`；
   - Ux 或 Uy 的绝对值增大 ⇒ 对应方向偏移增大，方向由极性决定；
   - 成像位置由 `Ux/U1` 与 `Uy/U1` 两个比值决定，偏转电压与加速电压存在竞争关系；
   - 实际示波器使用水平锯齿扫描和竖直信号电压，使屏上的亮点轨迹展开为波形。

### 3.5 打极板与屏幕边界
1. 每一方向的板内侧移超过半间距即撞击对应极板：
   - `|y| > d/2` ⇔ `|Uy| > 0.125·U1`：撞击 YY 极板；
   - `|x| > d/2` ⇔ `|Ux| > 0.125·U1`：撞击 XX 极板。
2. `|s偏| = d/2` 为临界状态：电子恰沿极板边缘通过，提示“临界：恰沿极板边缘通过”；严格大于临界才判定“电子打在极板上”。
3. 固定几何下，打极板临界对应的屏上偏移恒为：
   - `K·0.125 = 0.4×0.125 = 0.05 m = 5 cm`。
   - 因此屏边界为 ±6 cm，而电子在本模型中会先在 ±5 cm 临界处打极板；“亮点出屏”在模式一和模式二均结构性不可达，不设置为独立状态。
4. 两个方向同时超限时，状态提示优先显示“电子同时打在 XX、YY 偏转极板上”；单方向超限时显示具体方向；模式二按时间采样点局部判定，不因一个采样点超限而让整条波形消失。

## 四、主场景演示窗口
### 4.1 示波管侧视与正交偏转结构
1. 左侧为管轴水平的示波管侧视结构：
   - 左端：灯丝（暗红发光）+ 阴极板 K（“K 阴极”）+ 加速阳极板 A（带小孔，“A 加速”）；
   - K/A 之间绘制黄色加速电场线与方向箭头，实时标注 `U1 = XXX V`；
   - 中部同一偏转区绘制上下 YY 极板（橙色描边、标注“YY 竖直偏转 / Uy”）；
   - 在同一位置以透视或半透明交叉结构表示左右 XX 极板（标注“XX 水平偏转 / Ux”，不要将其画到 YY 区之后）；
   - 右端为管壳圆锥轮廓与荧光屏出口，标出出射方向和 `L′` 示意。
2. 偏转区两方向电场箭头分别显示 `Ey`、`Ex`；箭头方向随正负电压变化，大小可按幅度适度缩放，但不要求像素长度与真实电场严格成比例。
3. 屏上亮点/波形与侧视轨迹使用同色高亮或淡虚线对应，保证用户能看出“管内轨迹如何映射到屏上位置”。

### 4.2 电子束动画
1. 电子束由约 8~12 个青色发光小圆点组成，从阴极发射，经加速区间距逐渐拉大，表示速度提高。
2. 进入正交偏转区后：
   - 侧视图绘制 YY 方向抛物线弯曲段，出区后沿切线直线传播；
   - XX 方向用出纸面/入纸面箭头、透视虚线或屏上对应点表达，不强行在二维侧视图中伪造水平偏转。
3. U1、Ux、Uy 改变时，束流速度、偏转方向和偏转程度即时重算；撞击极板时，电子束在对应极板处中断。
4. 详细视图打开时，附加显示 `v₀` 速度箭头、板内侧移 `x/y`、出射角 `θx/θy` 和临界边界虚线；关闭时保持画面清爽。

### 4.3 荧光屏正面放大图
1. 深色屏面 + 绿调坐标网格，主格 2 cm、副格 1 cm；中心十字、边缘刻度和 ±6 cm 屏边界清楚可见。
2. 模式一显示荧光绿亮点、光晕和当前坐标 `(X,Y)`；模式二显示荧光绿扫描波形与余辉。
3. 模式一亮点拖尾：改变电压时旧亮点位置以约 1 s 渐隐，表现“电压变化 → 亮点移动”。
4. 模式二使用独立波形缓冲层，保留最近 **2 个完整扫描周期**；波形缓冲按扫描周期更新，不与模式一的 1 s 拖尾共用衰减逻辑。
5. 打极板时，模式一亮点消失；模式二仅对应的采样段不成像，波形出现局部上下削顶或左右截断。屏面中央显示灰色可读提示。
6. 顶部状态文字：
   - 模式一左侧：“偏转演示：`X=0.4·Ux/U1，Y=0.4·Uy/U1`”；
   - 模式二左侧：“波形扫描：`XX 锯齿扫描 + YY 正弦信号 ⇒ 屏上显示波形`”；
   - 右侧显示“正常成像”“临界通过”“电子打在 YY/XX 极板上”等状态。

## 五、两种演示模式与参数语义
模式切换位于参数面板顶部。两种模式的参数含义不能混用；U1 为公共参数，各模式保存自己的偏转参数。

### 5.1 模式一：偏转演示（默认）
- U1：加速电压，正值直流，范围 500~2000 V。
- Uy：竖直直流偏转电压，范围 −100~+100 V；上下极板电压差的等效带符号值。
- Ux：水平直流偏转电压，范围 −100~+100 V；左右极板电压差的等效带符号值。
- 屏上显示一个静态亮点 `(X,Y)`；拖动滑条或拖动亮点时实时更新。
- 模式一使用亮点移动拖尾约 1 s；不绘制连续波形。

### 5.2 模式二：波形扫描
#### 参数含义
- U1：公共加速电压，范围 500~2000 V。
- Ay：竖直信号峰值，非负，范围 0~100 V；`Uy(t)` 的峰值。
- Ax：水平扫描幅度，非负，范围 0~100 V；`Ux(t)` 在 `−Ax` 与 `+Ax` 之间扫描。
- 模式二默认值：`U1=1000 V、Ay=50 V、Ax=80 V、播放速度=1×`。此时波形包络约为 `X=±3.20 cm、Y=±2.00 cm`，不触发极板碰撞。
- `Ay=0` 时屏上为水平基线；`Ax=0` 时失去水平展开，仅作为退化情形保留，不作为默认演示状态。

#### 扫描函数与时间规则
1. 名义频率关系：信号 `f_signal=50 Hz`，扫描 `f_scan=25 Hz`，即 `T_scan=2T_signal`；这个频率只定义相位关系，动画采用教学慢放，不模拟真实 25/50 Hz 的刷新速度。
2. 每个扫描周期由“正向扫描 + 回扫”组成：
   - 正向扫描阶段，`q` 从 0 线性变化到 1，`Ux(t) = −Ax + 2Ax·q`；
   - 同一阶段，`Uy(t) = Ay·sin(4πq + φ0)`，正好显示两个完整正弦周期；`φ0` 固定为 0，不提供额外相位控件；
   - 到达右端后进入短暂回扫阶段（建议占一个扫描周期的 8%~12%），`Ux` 快速回到左端；
   - **回扫阶段电子束熄灭/不写入波形缓冲**，屏上不得出现从右端斜连回左端的非物理亮线；
   - 正向扫描重新开始后写入下一条波形。
3. 1× 播放速度下，一个完整扫描周期建议映射为约 4 s；播放速度只改变动画时间，不改变“一次扫描包含两个信号周期”的比例。
4. 波形缓冲保留最近两个完整扫描周期；新周期完成后再淘汰最旧周期，保证用户始终能看到完整波形。

#### 模式二碰板与波形缺失
- 对每个采样点独立判定：
  - `|Uy(t)| > 0.125·U1`：该采样点撞击 YY 极板，波形在上下方向缺失/削顶；
  - `|Ux(t)| > 0.125·U1`：该扫描位置撞击 XX 极板，波形在左右方向缺失；
  - 两者同时超限：该采样点记为“双向碰板”。
- 当 `Ay > 0.125·U1` 时，在正弦峰值附近出现上下削顶，并提示“信号峰值过大：电子打在 YY 极板上”。
- 当 `Ax > 0.125·U1` 时，扫描两端采样点在 ±5 cm 临界处截断，并提示“扫描幅度过大：电子打在 XX 极板上”。
- 只要仍有有效采样点，屏上保留有效波形；只有整周期没有有效采样点时，才显示“当前参数无有效成像”。

#### 模式二交互
- 扫描是模式二的常态动画；三项参数（U1、Ay、Ax）**全程实时可调**，波形和状态即时响应。
- 空格 = 暂停/继续扫描；暂停时滑条仍可调，调参会清空旧波形缓冲并以当前相位生成新的预览状态，点击继续后重新积累两个完整周期。
- ▶ 自动演示按钮在模式二不可用：扫描运行时显示“扫描中”，扫描暂停时显示“扫描模式”；四段自动演示只属于模式一。
- 模式二下数值面板中的 Ay/Ax、场强、加速度、板内侧移、出射角和 X/Y 均按**峰值/幅度**计算并在行名标注“（峰值）”，不显示高速跳变的瞬时值。

### 5.3 模式切换与参数保存
- U1 为公共参数；模式一最后使用的 Uy/Ux 和模式二最后使用的 Ay/Ax 分开保存。
- 从模式一切换到模式二时，模式二使用上次保存的 Ay/Ax；首次进入使用默认 `Ay=50 V、Ax=80 V`，并自动开始扫描。
- 从模式二切换到模式一时，模式一恢复上次保存的 Uy/Ux，进入待机态。
- 只有当前动画未运行时才允许切换模式；切换会清除另一模式的画布余辉/波形缓冲，不改变 U1。

## 六、图表窗口
### 6.1 左图：出射速度 v₀–U1
- 曲线：`v₀ = √(2eU1/m)`，U1 ∈ [500, 2000] V，青色曲线；v₀ 约 1.33~2.65×10⁷ m/s。
- 当前 U1 游标、当前点圆点和数值气泡（如“U1=1000 V / v₀=1.88×10⁷ m/s”）。
- 直观呈现 `v₀ ∝ √U1` 的非线性增长。

### 6.2 右图：屏上偏移–偏转电压
- 右图顶部提供小型方向切换：
  - `Y–Uy/Ay`：竖直方向；
  - `X–Ux/Ax`：水平方向。
- 横轴：对应偏转电压/幅度，模式一为 −100~+100 V；模式二为 0~100 V（幅度）。
- 纵轴：偏移 −6~+6 cm，荧光屏边界为 ±6 cm。
- 三条固定参考线（淡色）：U1=500 V、1000 V、2000 V，对应斜率分别为 0.8、0.4、0.2 mm/V。
- 另画当前 U1 的动态直线（荧光绿加亮，随 U1 实时重绘）；当前 U1 与参考值相等时两线重合。
- 在 ±5 cm 处绘制红/橙色虚线并标注“打极板临界”；±5~±6 cm 区域使用淡色背景表示“屏幕边界内但本模型不可达”。
- 模式一显示当前点 `(Uy,Y)` 或 `(Ux,X)`；模式二显示包络点 `(Ay,Ymax)` 或 `(Ax,Xmax)`；带数值气泡。
- 图内说明：“斜率 = 偏转灵敏度 ∝ 1/U1；水平与竖直偏转在本模型中完全对称”。
- 两图游标随滑条、模式一自动演示或模式二幅度改变实时更新。

## 七、参数设定面板
### 7.1 模式一控件
| 参数 | 范围 | 步长 | 默认值 | 说明 |
|------|------|------|--------|------|
| 模式切换 | 偏转演示 / 波形扫描 | — | 偏转演示 | 两按钮组 |
| 加速电压 U1 | 500~2000 V | 10 V | 1000 V | 增大 ⇒ 电子更快、亮点向中心收缩 |
| 竖直偏转电压 Uy | −100~+100 V | 1 V | +50 V | 上下极板等效直流电压 |
| 水平偏转电压 Ux | −100~+100 V | 1 V | 0 V | 左右极板等效直流电压 |
| 播放速度 | 0.1~2× | 0.1× | 1× | 自动演示用，运行中可调 |

### 7.2 模式二控件
| 参数 | 范围 | 步长 | 默认值 | 说明 |
|------|------|------|--------|------|
| 模式切换 | 偏转演示 / 波形扫描 | — | 波形扫描 | 两按钮组 |
| 加速电压 U1 | 500~2000 V | 10 V | 1000 V | 公共参数 |
| 竖直信号峰值 Ay | 0~100 V | 1 V | 50 V | 正弦信号峰值，非负 |
| 水平扫描幅度 Ax | 0~100 V | 1 V | 80 V | 锯齿扫描幅度，非负 |
| 播放速度 | 0.1~2× | 0.1× | 1× | 扫描动画速度，比例关系不变 |

- 模式一滑条显示 Uy/Ux 的显式正负号；模式二滑条显示 Ay/Ax 的“峰值/幅度”单位，不显示正负号。
- 公式说明框（fixed-note）必须呈现：电子加速公式、正交偏转公式、`X/Y = 0.4·U偏/U1`、灵敏度随 U1 变化、打极板临界、波形扫描与回扫熄灭规则。
- 另设“显示教学辅助”开关，控制速度箭头、板内侧移、出射角和电场线细节；不改变物理计算。

## 八、实时数值面板
右卡采用三列 grid，实时更新；模式二的幅度行明确标注“（峰值）”。

### 8.1 模式一显示项
| 名称 | 含义/公式 |
|------|-----------|
| 加速电压 U1 | 当前值 |
| 竖直偏转电压 Uy | 当前值 |
| 水平偏转电压 Ux | 当前值 |
| 出射速度 v₀ | `√(2eU1/m)`，×10⁷ m/s |
| 竖直场强 Ey | `Uy/d`，带方向 |
| 水平场强 Ex | `Ux/d`，带方向 |
| 竖直加速度 ay | `eUy/(md)`，带方向 |
| 水平加速度 ax | `eUx/(md)`，带方向 |
| 通过偏转区时间 t | `L/v₀`，ns |
| 板内竖直侧移 y | `UyL²/(4dU1)`，mm |
| 板内水平侧移 x | `UxL²/(4dU1)`，mm |
| 竖直出射角 tanθy | `UyL/(2dU1)` |
| 水平出射角 tanθx | `UxL/(2dU1)` |
| 屏上水平偏移 X | `0.4·Ux/U1`，cm，带方向 |
| 屏上竖直偏移 Y | `0.4·Uy/U1`，cm，带方向 |
| 成像状态（span-full） | 正常成像 / 临界通过 / 电子打在 XX、YY 或两组偏转极板上 |

### 8.2 模式二显示项
- U1、Ay（峰值）、Ax（幅度）、v₀。
- Ey,max、Ex,max、ay,max、ax,max、t、xmax、ymax、tanθx,max、tanθy,max。
- Xmax、Ymax（波形包络偏移）。
- 波形状态（span-full）：正常波形 / YY 局部削顶 / XX 两端截断 / 双向局部缺失 / 当前参数无有效成像。

## 九、操作按钮、状态机与拖拽交互
### 9.1 模式一按钮状态
| 状态 | 自动演示按钮 | 暂停/继续按钮 | 重置按钮 | 参数滑条 | 模式切换 |
|------|--------------|---------------|----------|----------|----------|
| idle | 启用，“▶ 自动演示” | 禁用，“暂停” | 启用 | 启用 | 启用 |
| running | 禁用，“演示中…” | 启用，“暂停” | 禁用 | 禁用（播放速度除外） | 禁用 |
| paused | 启用，“▶ 重新演示” | 启用，“继续” | 启用 | 启用 | 启用 |
| ended | 启用，“▶ 再来一次” | 禁用，“暂停” | 启用 | 启用 | 启用 |

- paused 状态用于分析当前画面；若用户修改 U1/Ux/Uy 或切换模式，则取消当前自动演示时间线，转为 idle/目标模式初始态，避免“继续”后动画覆盖手动修改。
- 播放速度在 running、paused 中均可调；自动演示时间线按新速度继续或重新开始，不改变物理参数比例。

### 9.2 模式二按钮状态
| 状态 | 自动演示按钮 | 暂停/继续按钮 | 重置按钮 | U1/Ay/Ax 滑条 | 模式切换 |
|------|--------------|---------------|----------|---------------|----------|
| scan-running | 禁用，“扫描中” | 启用，“暂停” | 启用 | 启用 | 禁用 |
| scan-paused | 禁用，“扫描模式” | 启用，“继续” | 启用 | 启用 | 启用 |

- scan-running 时三项参数实时调节；scan-paused 时仍可调节，调参会清空旧波形缓冲并显示当前相位预览。
- 模式二“重置”恢复 `U1=1000 V、Ay=50 V、Ax=80 V、速度=1×`，清空缓冲并继续扫描。
- 模式切换只在当前动画没有运行时进行；从 paused/scan-paused 切换会清理当前余辉或波形缓冲。

### 9.3 模式一自动演示动画
默认模式一 1× 总时长约 10 s，四个教学阶段之间允许明确标注的参数过渡段；过渡段不冒充主要教学阶段：
1. **加速电压的影响（0~3 s）**：Uy 固定 +50 V，Ux=0；U1 从 500 V 缓升到 2000 V。全程不打极板（`0.125×500=62.5 V > 50 V`），亮点从 +4.00 cm 连续收缩至 +1.00 cm；电子束间距拉大。
2. **竖直偏转的影响（3.5~6 s）**：U1=1000 V、Ux=0；Uy 从 −100 V 扫到 +100 V，亮点自下而上通过屏中心并留下竖直余辉轨迹。
3. **水平偏转的影响（6.5~9 s）**：U1=1000 V、Uy=0；Ux 从 −100 V 扫到 +100 V，亮点自左向右通过屏中心并留下水平余辉轨迹。
4. **回默认（9~10 s）**：从过渡起点 `U1=1000 V、Uy=0、Ux=+100 V` 平滑恢复模式一默认 `U1=1000 V、Uy=+50 V、Ux=0`，结束后回到 idle。

- **3.0~3.5 s 参数过渡**：U1 从 2000 V 平滑回落到 1000 V，Uy 从 +50 V 平滑变为 −100 V，Ux 保持 0；此段只用于衔接，不计入竖直扫动时长。
- **6.0~6.5 s 参数过渡**：Uy 从 +100 V 平滑变为 0，Ux 从 0 平滑变为 −100 V，U1 保持 1000 V；此段只用于衔接，不计入水平扫动时长。
- 验证点：3.25 s 应处于“竖直偏转准备/过渡”，6.25 s 应处于“水平偏转准备/过渡”，不得将这两个时刻误报为完整主阶段。

### 9.4 模式一亮点拖拽
- 仅模式一、仅 `idle`/手动分析用 `paused` 状态可拖拽；`running` 和 `ended` 状态均不可直接拖拽；模式二不可拖拽。
- 鼠标/触屏必须先命中当前荧光亮点才开始拖拽，建议命中半径为 12~18 个设计像素；在荧光屏空白网格处按下不能瞬移亮点。
- 命中亮点后按屏幕坐标反推：
  - `Uy = Y_cm·U1/40`；
  - `Ux = X_cm·U1/40`；
  - 四舍五入到 1 V。
- 可拖区域直接限制在有效范围内，拖到边界即停、无回弹：
  - `Veff = floor(min(100, 0.125·U1))`；
  - `|X_cm|、|Y_cm| ≤ 40·Veff/U1`。
- U1=500 V 时 `Veff=62 V`，可拖范围为 ±4.96 cm，板内侧移 4.96 mm < 5 mm；U1≥800 V 时由滑条上限 ±100 V 决定，可拖到 ±4.00 cm。
- 拖动实时同步对应滑条、数值面板、右图方向切换后的当前点和主场景束流；光标使用 `grab/grabbing`。

### 9.5 键盘与通用控制
- 空格：模式一 running/paused 间切换；模式二 scan-running/scan-paused 间切换。
- R：重置当前模式；焦点在输入框、滑条、按钮时不触发。
- 重置不改变当前模式，但两个模式都恢复播放速度 `1×`：模式一恢复 `U1=1000 V、Uy=+50 V、Ux=0` 并回 idle；模式二恢复 `U1=1000 V、Ay=50 V、Ax=80 V` 并继续扫描。

## 十、标题与元信息
- 标题：`示波器演示｜高中物理`。
- `description`：示波管工作原理——电子加速、正交偏转，以及加速电压、水平/竖直偏转电压对荧光屏成像的影响。

## 十一、物理公式与几何（实现参考，勿改动）
- `v₀ = √(2eU1/m)`，`e = 1.6×10⁻¹⁹ C`，`m = 9.1×10⁻³¹ kg`。
- `Ey = Uy/d`，`Ex = Ux/d`。
- `ay = eUy/(md)`，`ax = eUx/(md)`。
- `t = L/v₀`。
- `y = UyL²/(4dU1)`，`x = UxL²/(4dU1)`。
- `tanθy = UyL/(2dU1)`，`tanθx = UxL/(2dU1)`。
- `Y = (L/2+L′)·tanθy = 0.4·Uy/U1（m）`。
- `X = (L/2+L′)·tanθx = 0.4·Ux/U1（m）`。
- 几何：`L=0.04 m`，`d=0.01 m`，`L′=0.18 m`，`K=0.4 m`，屏 ±6 cm。
- 关键值：U1=500/1000/2000 V 时，`v₀=1.33/1.88/2.65×10⁷ m/s`（√3.5165×10¹⁴ = 1.875×10⁷，两位小数标准舍入为 1.88；实现与验收均按 1.88），`t=3.02/2.13/1.51 ns`；灵敏度分别为 0.8/0.4/0.2 mm/V。
- 碰板临界：`|U偏|=0.125·U1` 对应板内侧移 5 mm、屏上偏移 5 cm；严格大于该值判定撞击极板。
- 侧视图 YY 轨迹：以 YY 区入口为原点、x 沿管轴，按类平抛解析式逐点绘制；XX 同一区域的水平分量在正面屏图中呈现，不在侧视图伪造第二个纵深区域。

## 十二、理想模型边界（实现范围）
本页只服务高中物理定性与定量演示，不追求真实示波器工程细节，不实现以下因素：
- 管内空间电荷效应和电子束之间的相互作用；
- 偏转极板边缘电场、场区非均匀性和真实极板形状修正；
- 电子初速度分布、热发射速度差异、散射和束流发散；
- 相对论速度修正；
- 荧光粉真实余辉曲线、亮度控制、聚焦/辉度电极和真实触发同步；
- 真实示波器的输入阻抗、带宽、噪声、触发模式和扫描校准。

实现必须保留的教学核心只有：
1. 加速电压改变电子速度；
2. 两个正交偏转电压改变两个方向的轨迹；
3. 屏上位置由偏转电压与加速电压的比值决定；
4. 水平扫描与竖直信号共同形成屏上波形；
5. 加速不足或偏转过大时电子会撞击偏转极板。

## 十三、验收标准与测试矩阵
### 13.1 基础布局与风格
1. 页面浅色主题与系列一致；布局为主画布（管侧视 + 屏正面）→ 双图 → 参数/数值面板 → 底部按钮 → 图例。
2. 主场景按 880×520 设计坐标等比缩放；窄屏 `sceneFont` 有 10 px 可读下限，图表字号不随场景缩放。
3. 四个教学阶段标记存在；同一正交偏转区内同时表示 YY 与 XX，不能把 XX 画成 YY 之后的第二个偏转区。

### 13.2 默认值与公式
4. 模式一默认 `U1=1000 V、Uy=+50 V、Ux=0`：
   - `v₀=1.88×10⁷ m/s`（真值 1.875×10⁷，两位小数标准舍入）；
   - `Ey=5000 V/m`、`ay=8.79×10¹⁴ m/s²`；
   - `t=2.13 ns`；`y=2.0 mm`；`tanθy=0.100`；
   - `X=0.00 cm`、`Y=+2.00 cm`；亮点在屏中心正上方一个 2 cm 主格。
5. 模式二默认 `U1=1000 V、Ay=50 V、Ax=80 V`：波形包络 `X=±3.20 cm、Y=±2.00 cm`，状态为正常波形。
6. U1=500/1000/2000 V 时速度、时间和偏转灵敏度分别符合第十一节关键值；当前 U1 动态直线与右图参考线一致。

### 13.3 对称性与极性
7. `U1=1000、Ux=50、Uy=0` ⇒ `(X,Y)=(+2,0) cm`；`U1=1000、Ux=0、Uy=50` ⇒ `(X,Y)=(0,+2) cm`；两者轨迹和偏移量完全对称。
8. `Uy=−50` 向下，`Uy=+50` 向上；`Ux=−50` 向左，`Ux=+50` 向右。
9. `Ux=Uy=50、U1=1000` ⇒ 亮点在右上 `(X,Y)=(+2,+2) cm`，侧视与屏正面标注不矛盾。

### 13.4 碰板与边界
10. `U1=500、Uy=+80 V`：`y=6.4 mm>5 mm`，束流在 YY 区中断，状态为“电子打在 YY 极板上”，屏无亮点。
11. `U1=500、Uy=+62 V`：`y=4.96 mm<5 mm`，正常成像；恰好达到理论临界值时显示“临界：恰沿极板边缘通过”。
12. `U1=500、Ux=+80 V`：XX 方向同理；两方向同时超限时提示“双向碰板”。
13. 右图 ±5 cm 临界线、±6 cm 屏边界和 ±5~±6 cm 不可达区域显示正确；不存在独立“亮点出屏”状态。

### 13.5 动画与余辉
14. 电子束加速区间距拉大；偏转区 YY 曲率随 Uy 符号变化；XX 方向用正交/出纸面标记，不出现虚假的后置偏转区。
15. 模式一快速拖动 Uy/Ux 时亮点留下约 1 s 渐隐轨迹；拖拽边界无回弹。
16. 模式一自动演示四段顺序和时长正确：第 1 段亮点 +4→+1 cm 全程成像，第 2 段竖直扫动，第 3 段水平扫动，第 4 段恢复默认。
17. **自动演示时间线必须与第九节 9.3 的明确时间线一致**：3.0~3.5 s 的 U1 回落/参数过渡、6.0~6.5 s 的水平段起始过渡若保留，必须在画面和需求中明确为过渡段；不得将过渡过程误标为主要教学阶段。3.25 s、6.25 s 等中间时刻的参数状态须可验证。

### 13.6 波形扫描
18. 模式二一个正向扫描周期恰好展开 2 个完整正弦周期；回扫阶段电子束熄灭，不产生右端到左端的斜线。
19. **波形笔画按扫描周期独立管理**：进入回扫的瞬间必须立即结束当前正向扫描笔画；下一次正向扫描必须创建新笔画，不能把两个周期首尾连成一条路径。波形缓冲保留最近 2 个完整扫描周期，且每个周期可独立验证。
20. 播放速度改变不改变扫描/信号周期比；一个正向扫描周期的采样点、回扫空白段和下一周期起点之间不能绘制连线。
21. U1 从 1000 调到 2000 V，波形 X/Y 包络均缩小约一半；Ay 改变只改变竖直幅度，Ax 改变只改变水平展开。
22. `U1=500、Ay=100 V` 时波形上下在 ±5 cm 临界处削顶/缺失；`U1=500、Ax=100 V` 时波形两端在 ±5 cm 截断/缺失；提示文字和有效波形同时存在。
23. 模式二扫描运行中三滑条可调；自动演示按钮禁用；空格暂停/继续；暂停调参会清空旧缓冲并重建预览。

### 13.7 交互、响应式与无障碍
24. 模式一 U1=1000 V 时，**必须先命中当前荧光亮点（建议命中半径 12~18 设计像素）**才开始拖拽；在荧光屏空白网格处按下不能瞬移亮点。拖动亮点到 `(2.0,3.0) cm`，反推并同步 `Ux=+50 V、Uy=+75 V`；模式二不可拖拽。
25. U1=500 V 时拖拽边界为 ±4.96 cm、对应 ±62 V，拖到边界即停、无回弹；U1≥800 V 时可拖满 ±100 V。
26. 模式一只有 `idle` 与手动分析用的 `paused` 状态允许亮点拖拽；`running` 和 `ended` 状态均不允许直接拖拽。ended 状态须通过“再来一次”或“重置”回到可交互状态。
27. 模式一/模式二状态机按钮文本、启用/禁用状态、模式切换和重置语义符合第九节；暂停态调参不会被自动时间线覆盖。
28. **两个模式的重置都恢复播放速度 `1×`**：模式一恢复 `U1=1000 V、Uy=+50 V、Ux=0、速度=1×`；模式二恢复 `U1=1000 V、Ay=50 V、Ax=80 V、速度=1×`。
29. 右图方向切换 `Y–Uy/Ay` 与 `X–Ux/Ax` 正确，当前 U1 动态线和当前点随模式更新。
30. 至少验证 320 px、375 px、768 px、1440 px 宽度：阶段文字不重叠、网格可辨、控件不溢出、拖拽命中区域可用。
31. 空格/R 快捷键不干扰表单输入；按钮、模式切换、滑条有 aria-label；实时状态和数值有 aria-live。

### 13.8 碰板状态下的图表与成像点
32. 模式一当前 `|Ux| > 0.125·U1` 或 `|Uy| > 0.125·U1` 时，右图不得把无效成像点夹到 ±6 cm 屏边界上；应隐藏普通当前点，或改为在对应偏转电压位置显示“碰板/无效”标记，并在图例或提示中说明。
33. 模式一恰好达到临界值时可以显示临界标记，但不能显示为“正常成像”点；严格超临界时数值气泡可以保留理论外推值，但必须明确标注“理论外推/碰板无效”，不能让用户误认为屏上实际亮点位置。
34. 模式二某些采样点碰板时，右图显示的是峰值理论包络与临界线的关系；屏正面只绘制有效采样段，不能把无效采样段连接成普通波形。

### 13.9 交付
35. 页面文件已生成并同步 `_repo`；`index.html` 新增“示波器演示”卡片，badge 为“电场 · 中级”，链接正确。

## 十四、v1 范围确认与本版修订记录
### 14.1 已确认纳入 v1 的五项功能
1. 纳入模式二“波形扫描”。
2. 保留双图：v₀–U1 + 偏转电压/屏上偏移图；本版将右图升级为 Y/X 方向切换，并加入动态 U1 直线和 ±5 cm 临界线。
3. 保留模式一亮点拖尾，并为模式二单独建立按扫描周期管理的波形余辉缓冲。
4. 保留电子打偏转极板的极端情形提示，模式一按单点、模式二按采样点局部判定。
5. 纳入待机态亮点拖拽反推 Uy/Ux。

### 14.2 本版总体优化记录
- 将 YY、XX 从“前后串联区域”改为同一正交偏转区，确保 X/Y 使用同一系数并满足对称性。
- 将模式一的 Uy/Ux 直流量与模式二的 Ay/Ax 幅度量分开定义，分别保存和显示。
- 明确正向扫描、回扫熄灭、波形缓冲和模式二局部碰板逻辑。
- 增加模式状态机、教学阶段标记、辅助视图、图表物理边界和理想模型边界。

### 14.3 实现对齐与验证记录（2026-09-20，实现完成后补记）
- **主题对齐**：本文件初稿沿用旧深色主题描述，但全系列已按 `requirement-light-theme-unify.md` 统一为浅色主题；实现按浅色执行，第一节已同步更正。
- **v₀ 显示口径**：U1=1000 V 时 v₀ 真值为 1.875×10⁷ m/s，初稿关键值写作 1.87×10⁷（截断）与 500 V 的 1.33（舍入）口径不一；统一为两位小数标准舍入，1.88×10⁷（第一节 6.1、十一、十三已同步）。
- **窄屏 compact 模式**：`fit < 0.62` 时场景自动精简——状态文字分两行、阶段条只留编号、隐藏名称类标签（YY/XX 名称、荧光屏侧视、灯丝），K/A 缩写，保证 375 px 下无重叠、无横向溢出；“显示教学辅助”在窄屏首次加载默认关闭。
- **首版验证结果**：物理核 Node 断言 83/83 通过（`osc-test.mjs`，覆盖公式值、打极板临界、拖拽反推、扫描波形、时间线、侧视几何）；真实浏览器 CDP 端到端 41/41 通过（`osc-cdp.mjs`，chrome-headless-shell 153：DOM 读数、画布像素采样、模式切换、自动演示、拖拽、快捷键、窄屏 375 px、全程控制台零错误）。桌面 1200 px 与窄屏 375 px 截图人工复核通过。上述是首版基线结果；第十六节 v1.1 修订完成后必须重新验证，不能直接沿用为最终结果。

## 十六、v1.1 审计修订要求（编码模型必须实施）

> 本节记录首版代码审计发现的确定性问题与规格不一致。其他编码模型修改时必须先阅读本节，不得只做表面文案调整；完成后须按 16.6 的新增测试清单重新验证。除本节列出的行为外，不扩大功能范围。

### 16.1 模式二波形笔画边界（必须修复）
- 当前/首版常见错误：仅在扫描进度从周期末回到 0 时关闭笔画，进入回扫阶段时没有立即结束当前笔画，可能导致回扫后新周期与旧周期首尾相连。
- 正确规则：当扫描进度第一次跨过正扫结束边界 `fwd = 1 − RETRACE`（即 `p0 ≤ fwd` 且 `p1 > fwd`）时，立即结束当前正向扫描笔画；回扫阶段不向当前笔画写入任何点；下一次正扫开始时创建新笔画。
- 每个周期的最后一个有效点和下一个周期的第一个有效点之间必须有断笔标记/独立路径，绝不能有横向回扫线。
- 波形缓冲最多保留最近两个**完整且独立**的扫描周期；切换模式、暂停后调参、重置时清空旧笔画。

### 16.2 模式一重置速度（必须修复）
- 模式一点击“重置”必须同时恢复：`U1=1000 V、Uy=+50 V、Ux=0、播放速度=1×、phase=idle`。
- 模式二重置保持现有规则：`U1=1000 V、Ay=50 V、Ax=80 V、播放速度=1×、继续扫描`。
- 验收必须包含：模式一先将速度调至 2×，点击重置后显示恢复 `1.0×`，自动演示实际按 1× 运行。

### 16.3 亮点拖拽命中和状态限制（必须修复）
- 只能在模式一 `idle` 或手动分析用 `paused` 状态拖拽；`running`、`ended` 和模式二均不可拖拽。
- 必须先命中当前有效荧光亮点才开始拖拽；建议在设计坐标中使用 12~18 px 命中半径。点击荧光屏空白网格不得瞬移亮点。
- 若当前状态为碰板、屏上无有效亮点，则不允许开始拖拽，用户应先调回可成像参数或点击重置。
- 拖动过程中仍使用当前 U1 对 X/Y 电压限位，边界到达即停且无回弹；拖拽反推公式与第 9.4 节一致。

### 16.4 自动演示时间线（必须与需求统一）
- 主教学阶段保持：0~3 s 加速电压、3.5~6 s 竖直偏转、6.5~9 s 水平偏转、9~10 s 回默认。
- 3~3.5 s 是明确的“回落/竖直准备过渡”：`U1:2000→1000 V、Uy:+50→−100 V、Ux=0`。
- 6~6.5 s 是明确的“水平准备过渡”：`U1=1000 V、Uy:+100→0、Ux:0→−100 V`。
- 过渡段必须在状态文字、需求说明或阶段提示中可辨识；3.25 s、6.25 s 的内部参数必须与上述值域一致，不能被测试误判为完整竖直/水平阶段。
- 如果编码模型选择删除过渡段而改为阶段边界瞬间切换，必须同步修改第 9.3 节和第 13.5 节，不得只改代码。

### 16.5 碰板时右图当前点（必须修复）
- 模式一当 `|Ux| > 0.125·U1` 或 `|Uy| > 0.125·U1` 时，当前点是“理论外推但无效成像”，不得把它夹到 ±6 cm 屏边界后继续作为普通蓝点显示。
- 推荐实现：隐藏普通当前点，并显示“碰板/无效”标记；或保留理论坐标气泡但明确标注“理论外推、碰板无效”。两种实现只能选一种，并在状态提示中保持一致。
- 恰好临界时显示“临界通过”标记，不显示为正常成像；模式二右图继续显示峰值理论包络，但屏正面只画有效采样点。
- 模式二波形的无效采样段必须断笔，不能与前后有效段连成普通线段。

### 16.6 v1.1 新增验证清单（完成修改后必须全部通过）
1. 让模式二运行至少 3 个扫描周期，检查波形缓冲始终最多 2 条独立笔画；回扫和周期边界均没有横线连接。
2. 在模式二 `p≈0.895→0.905` 的回扫边界暂停取样，确认当前笔画已经关闭；下一次正扫从独立笔画开始。
3. 模式一播放速度改为 2×后点击重置，确认速度显示 `1.0×`、滑条回中间位置、自动演示按 1×运行。
4. 模式一 idle 状态在荧光屏空白网格按下，确认 Ux/Uy 不改变；命中亮点后拖到 `(2.0,3.0) cm`，确认反推 +50/+75 V。
5. 自动演示在 `t=3.25 s`、`t=6.25 s` 读取参数，分别落在两段过渡定义内；在 `t=3.5 s`、`t=6.5 s` 读取主阶段起点参数。
6. 模式一 ended 状态点击屏幕空白或亮点均不能改变参数；点击“再来一次”或“重置”后才恢复对应交互。
7. 设置 `U1=500、Uy=100 V` 或 `U1=500、Ux=100 V`，确认右图不显示夹在 ±6 cm 边界的普通成像点，并清楚标注碰板无效/理论外推。
8. 模式二设置 `U1=500、Ay=100 V、Ax=80 V`，确认屏正面无效采样段断开、有效波形仍保留、状态提示双向局部缺失。
9. 重新运行原有物理核 83 项测试，并新增上述边界测试；重新运行浏览器端到端测试，要求控制台零错误。

### 16.7 v1.1 修改记录
- 本节由首版只读审计追加（2026-09-20）；追加前未修改页面代码。
- **2026-09-20 v1.1 实施完成**，五项修订全部落实：
  1. 16.1 波形笔画：主循环在跨过正扫/回扫边界（或跨周期回绕）时立即 `closeStroke()`，回扫不写点，新周期新建笔画；缓冲保持 ≤2 条独立笔画；
  2. 16.2 模式一重置：恢复 `U1=1000、Uy=+50、Ux=0、速度=1×、idle`（与模式二口径一致）；
  3. 16.3 拖拽：`dragAllowed` 收紧为 `idle/paused`；新增 `spotDesignPos()` + 15 设计像素命中半径（`DRAG_HIT_R`），空白网格按下不再瞬移；碰板无亮点时不可拖；
  4. 16.4 时间线：`demoPose` 本就符合 3~3.5 s / 6~6.5 s 过渡定义；新增状态文字“· 参数过渡”标注（`statusLeft()`），并暴露只读验证钩子 `window.getOscDebug()`；
  5. 16.5 右图当前点：模式一 ok→蓝点、临界→琥珀点“（临界通过）”、碰板→±5 cm 临界线处红色 × + 气泡“理论外推 + 碰板：荧光屏无亮点”；模式二超临界包络改空心点区分理论值；模式二无效采样段保持断笔。
- **v1.1 最终验证**：物理核 Node 断言 **88/88 通过**（原 83 项 + 新增过渡段时间线 5 项；期间修正一处测试侧中点值笔误）；真实浏览器 CDP 端到端 **45/45 通过**（`osc-cdp.mjs` v3，chrome-headless-shell 153，完整覆盖 16.6 九项清单：3 周期笔画独立性与回扫断笔、重扫边界 curPts=0、速度重置、空白按下无效、ended 禁拖、过渡窗口采样、碰板 × 标记像素检查、无效段断笔、控制台零错误；另含既有 41 项回归）。测试期间修正一处测试流程问题（模式二重置后处于 scan-running，切回模式一前须先暂停，与需求一致）。桌面 1200 px（模式一/模式二）与窄屏 375 px 截图人工复核通过；页面、测试脚本与 `_repo` 副本哈希一致。

## 十八、v1.2 二次审计修订要求（编码模型必须实施）

> 本节记录 v1.1 完成后的第二次只读审计发现。其他编码模型修改时必须逐项实施，不得只修改测试或文案；除本节列出的行为外，不扩大功能范围。完成后须按 18.6 的新增清单重新验证。

### 18.1 跨方向碰板时的右图当前点（必须修复）
- 当前模式一右图只检查当前选中的方向，可能出现：`U1=500、Uy=50 V、Ux=100 V` 时页面整体状态已为“电子打在 XX 极板上”，但右图若处于 `Y–Uy` 仍显示普通蓝色 Y 当前点。
- 定义整体成像状态：`anyHit = hitState(Ux,U1)=="hit" || hitState(Uy,U1)=="hit"`。
- 模式一只要 `anyHit=true`，右图不得绘制普通蓝色“正常成像”点；当前选中方向应显示碰板/无效标记（建议红色 × 位于 ±5 cm 临界线），气泡写明“理论外推、碰板无效”或“另一方向碰板，屏无有效亮点”。
- 两方向均未碰板时，保持当前方向的正常点/临界点逻辑；恰好临界但未超限仍显示“临界通过”，不能误判为碰板。
- 模式二的右图仍显示峰值理论包络，但若任一方向峰值超临界，必须用“理论包络/部分无效”样式，不得误称为屏上完整波形。

### 18.2 暂停必须冻结全部动画（必须修复）
- 模式一 `paused`：自动演示时间 `m1.t`、电子束 `beamClock`、亮点拖尾新增和其他动画时钟全部冻结；暂停期间画布不得因时间流逝自行变化。
- 模式二 `scan-paused`：扫描相位 `m2.p`、`beamClock`、波形笔画新增和余辉状态全部冻结；暂停期间不得继续写入采样点。
- 暂停后调节参数是例外：按第 5.2 节规则清空旧波形/拖尾并绘制静态预览，不能恢复时间动画；点击“继续”后才从当前相位继续。
- 建议主循环将时间增量仅在 `running`/`scan-running` 状态应用；暂停态只重绘当前静态画面，不推进任何动画时钟。
- 重置、模式切换和重新开始必须显式清理并初始化所有相关时钟和缓存，避免旧动画状态泄漏。

### 18.3 无障碍属性完整性（必须修复）
- 所有控制按钮必须有稳定、准确的 `aria-label`：自动演示、暂停/继续、重置、Y/X 图表方向切换、模式一/模式二切换。
- 动态按钮文本变化时同步更新 `aria-label`，例如“演示中，按钮不可用”“继续自动演示”“扫描中，按钮不可用”。
- 模式切换和 Y/X 方向切换使用 `aria-pressed` 或等效选中语义；禁用态使用真实 `disabled`，不能只靠 CSS 变灰。
- 滑条继续保留 `aria-label`，并建议同步 `aria-valuetext` 显示当前带单位值（如“加速电压 1000 伏”“竖直信号峰值 50 伏”）。
- `aria-live="polite"` 的状态更新不能高频朗读扫描瞬时值；模式二只播报峰值/状态变化，不播报每帧相位。

### 18.4 窄屏过渡提示必须可辨识（必须修复）
- 自动演示 3~3.5 s、6~6.5 s 的“参数过渡”在 `fit < 0.62` 的 compact 场景中仍必须可见。
- compact 模式可以缩短主标题，但不能用 `s.left.split('　')[0]` 直接丢掉“· 参数过渡”语义；建议将状态拆成两行，或保留“偏转演示 · 参数过渡”。
- 375 px 和 320 px 下均需验证：状态文字不与右侧状态重叠，且用户能区分主教学阶段与过渡段。

### 18.5 图表气泡必须按实际画布尺寸定位（必须修复）
- `bubble()` 不得使用固定的 `398` 作为右边界；图表在窄屏下不是固定 400 设计像素。
- 气泡定位必须使用当前图表 `W/H`：左右边界至少为 `2` 与 `W-bw-2`，上下边界至少为 `2` 与 `H-bh-2`；必要时根据锚点四向选择可用位置。
- 右图在 `Ux/Uy=+100 V`、`Ux/Uy=-100 V`、模式二 `Ax=100 V` 等边界状态下，气泡不得被裁切或溢出图框。
- 宽屏和 320/375 px 窄屏均需做右端点、左端点、上下临界线气泡检查。

### 18.6 零值显示与调试接口（建议一并修复）
- 模式一 `Ux=0` 时水平加速度应显示 `0.00×10¹⁴ m/s²`，不能显示 `+0.00×10¹⁴`；只有严格正值/负值才显示方向符号。其他零值带方向读数同理。
- `window.getOscDebug()` 是测试专用接口，不属于用户功能。建议生产交付前移除，或仅在明确的开发/测试构建开关下暴露；若暂时保留，必须加注释说明其用途，不得依赖它实现页面行为。

### 18.7 v1.2 新增验证清单（完成修改后必须全部通过）
1. 模式一设置 `U1=500、Uy=50、Ux=100`，分别在 Y 图和 X 图检查：整体状态为 XX 碰板，两个方向都不显示普通蓝色成像点；切回安全参数后恢复正常点。
2. 模式一 running 后暂停，至少等待 500 ms，确认 `m1.t`、`beamClock`、拖尾数量和画布动画均冻结；继续后恢复推进。
3. 模式二 scan-running 后暂停，至少等待 500 ms，确认 `m2.p`、`beamClock`、`curPts/strokes` 均冻结；暂停调参只生成静态预览，继续后才写入新点。
4. 逐项检查自动演示、暂停/继续、重置、模式切换、Y/X 方向切换的 `aria-label`、`aria-pressed`、`disabled` 状态；动态按钮状态变化后 accessible name 正确。
5. 自动演示在 320 px、375 px 下进入 3~3.5 s 和 6~6.5 s 过渡段，确认“参数过渡”仍可见且不重叠。
6. 宽屏和窄屏分别把右图当前点/包络置于左右端点与上下临界线，确认气泡完全位于图表画布内。
7. 模式一 `Ux=0`、`Uy=0` 等零值检查不出现无意义的 `+0.00`；调试接口按生产策略移除或受控暴露。
8. 重新运行现有物理核 88 项测试；新增跨方向碰板、暂停冻结、无障碍属性、compact 过渡文案、气泡边界和零值格式测试；浏览器端到端要求控制台零错误。

### 18.8 v1.2 修改记录
- 本节由 v1.1 完成后的第二次只读审计追加（2026-09-20）；追加前未修改页面代码。
- **2026-09-20 v1.2 实施完成**，六项修订全部落实：
  1. 18.1 跨方向碰板：右图模式一引入 `anyHit` 整体判定——任一方向超限即不绘制普通蓝点，当前方向在 ±5 cm 临界线画红色 ×，气泡标明“XX/YY/XX、YY 方向碰板：屏无亮点”与理论外推值；恢复安全参数后恢复普通点；模式二任一峰值超临界时包络点改空心并标注“理论包络·含无效段”；
  2. 18.2 暂停冻结：新增视觉时钟 `st.vclock`（余辉时间戳改用 vclock）——`paused/scan-paused` 时 vclock、`beamClock`、`m1.t`、扫描相位、拖尾新增与笔画写入全部冻结，画布 `toDataURL` 逐字节一致；暂停调参仅生成静态预览，继续后恢复推进；
  3. 18.3 无障碍：自动演示/暂停/重置按钮补 `aria-label` 并随 `setButtons()` 动态同步；模式切换与 Y/X 方向切换补 `aria-pressed`；三个滑条在 `updateParamLabels()` 同步 `aria-valuetext`（带单位）；`updateReadouts()` 改为内容变化才写 DOM，避免 aria-live 每帧播报；
  4. 18.4 窄屏过渡提示：compact 状态行压缩主标题后保留“· 参数过渡”（`drawStatus`），375 px 截图确认可见且不重叠；
  5. 18.5 气泡定位：`bubble()` 增加 `W/H` 参数——右侧放不下自动翻到锚点左侧，四向按实际画布尺寸钳制，删除硬编码 `398`；
  6. 18.6 零值与调试接口：新增 `fmtAcc()`，Ux/Uy=0 时加速度显示 `0.00×10¹⁴ m/s²`（无方向符号）；`window.getOscDebug()` 保留为测试专用并注明注释（本地教学页暂不移除，页面行为不依赖它）。
- **v1.2 最终验证**：物理核 Node 断言 **88/88 通过**；真实浏览器 CDP 端到端 **65/65 通过**（`osc-cdp.mjs` v4，chrome-headless-shell 153，覆盖 16.6 + 18.7 全部清单：跨方向碰板双图检查、模式一/模式二暂停冻结（含画布逐字节一致）、暂停调参静态预览、动态 aria 属性、窄屏过渡窗口截图、碰板气泡边界截图、零值格式、既有 v1.1/v1 回归、控制台零错误）。测试期间修正三处测试侧流程问题（用例未先恢复 Uy=50、运行态误点禁用的重置按钮、切回 Y 图前验证恢复点），应用行为均正确。截图人工复核：宽屏模式一/模式二、碰板气泡边界（气泡左翻不裁切）、窄屏 375 px 过渡提示“模式一 偏转演示 · 参数过渡”清晰可见。页面、测试脚本与 `_repo` 副本哈希一致，并已提交推送 GitHub 仓库。

## 十九、参考文件
- 风格与交互基准：`/home/wq/physics-work/force-composition-demo.html`。
- 同类需求文档：`/home/wq/physics-work/_repo/requirement-force-equilibrium-v1.md`。
- 按钮合并约定：`/home/wq/physics-work/_repo/requirement-horizontal-linear-motion-v2-button-merge.md`。
- 窄屏字体规范：`/home/wq/physics-work/_repo/requirement-mobile-narrow-screen-font-v1.md`、`docs/mobile-narrow-screen-audit.md`。
- 交付目录：`/home/wq/physics-work/oscilloscope-demo.html`。
