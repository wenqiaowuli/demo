# 需求：水平直线运动演示——合并"暂停/继续"按钮

## 目标
- 修改：`horizontal-linear-motion.html`（水平直线运动演示｜高中物理）
- 将底部控制按钮中的"暂停"和"继续"合并为一个切换按钮，最终保留 3 个按钮：开始、暂停/继续、重置

## 按钮状态行为

### 按钮布局
- 页面底部为 3 个按钮：`开始` | `暂停/继续` | `重置`
- 不再存在独立的"继续"按钮

### 状态矩阵
| 运行状态 | 开始按钮 | 暂停/继续按钮 | 重置按钮 |
|---------|---------|--------------|---------|
| 初始/重置后（idle） | 启用，显示"开始" | 禁用，显示"暂停" | 启用 |
| 运行中（running） | 禁用，显示"运行中…" | 启用，**显示"暂停"** | 禁用 |
| 暂停中（paused） | 启用，显示"重新开始" | 启用，**显示"继续"** | 启用 |
| 越界结束（ended） | 启用，显示"重新开始" | 禁用 | 启用 |

### 交互逻辑
- 点击"暂停/继续"按钮时：
  - `state === 'running'` → 暂停动画，按钮文字变为"继续"
  - `state === 'paused'` → 恢复动画，按钮文字变为"暂停"

## 代码改动点
1. **HTML**：删除 `<button id="btnContinue">继续</button>`，将 `btnPause` 改为 `btnPauseResume`
2. **JavaScript**：
   - 引用 `btnPauseResume`，删除 `btnPause` / `btnContinue` 引用
   - 点击事件：根据 `state` 判断调用 `pause()` 或 `resume()`
   - `start()`：按钮启用并显示"暂停"
   - `pause()`：按钮文字改为"继续"
   - `resume()`：按钮文字改为"暂停"
   - `reset()`：按钮禁用、文字改回"暂停"
   - `endRun()`：按钮禁用、文字改回"暂停"
