# 水平直线运动演示 · v5 数轴位置调整需求

## 1. 背景与目标
- **对标文件**：`horizontal-linear-motion.html`（v4 已将主画布对齐为 880×520）。
- **现状**：主画布中体现运动轨迹的 x 数轴（小球、v/a 矢量所在的水平轴线）位于画布竖直方向约 56% 高度处（`axisY = round(height × 0.56)`，880×520 基准下 ≈ 291px），视觉偏下。
- **目标**：将数轴上移至**画布竖直方向整体 1/3 高度处**：`axisY = round(height / 3)`（520 基准下 ≈ 173px）。

## 2. 改动点
- **JS 一处**：`sceneGeometry()` 中 `axisY = Math.round(sceneView.height * 0.56)` → `Math.round(sceneView.height / 3)`。
- x 轴刻度、±20m 越界虚线与标注、小球、v/a 矢量、轨迹点、`x = …` 读数等均以 `axisY` 为基准相对绘制，自动跟随，**不改任何绘制逻辑**。

## 3. 非目标（本次不做）
- 不改画布尺寸（维持 880×520）、双图、参数面板、按钮、图例与文案。
- 不改物理逻辑（匀变速公式、越界判定、暂停/继续/重置）。
- 不做其他视觉元素（如背景网格、地面）的增删。

## 4. 验收标准
- [ ] 数轴轴线位于画布竖直方向 1/3 高度处（880×520 基准下 axisY ≈ 173，误差 ≤ 1px）。
- [ ] 顶部说明「x 轴 · 向右为正」与 ±20m 标注、越界虚线不重叠、不被裁切。
- [ ] 轴线下方刻度标签（axisY+22）与 a 矢量（axisY+24 及其文字标注）完整可见。
- [ ] 小球、v/a 矢量、轨迹、读数随轴线上移，相对位置关系不变。
- [ ] 运行/减速转向/越界行为与双图无回归。
