# 需求：共点力平衡演示网页

## 目标
- 新建网页：`force-equilibrium-demo.html`（共点力平衡演示｜高中物理）
- 风格参照：`force-composition-demo.html`（力的合成与分解演示）——深色主题、上中下三段式布局、图表蓝框对齐、拖拽交互与三按钮矩阵完全同款
- 物理内容：人教必修第一册 3.5 共点力平衡——轻绳悬球在水平拉力作用下缓慢向右上方移动的准静态平衡过程，呈现 ΣFx=0 / ΣFy=0、三力封闭三角形与正交分解平衡
- 交付目录：`/home/wq/physics-work/force-equilibrium-demo.html`
- 在线地址（部署后）：`https://wenqiaowuli.github.io/demo/force-equilibrium-demo.html`
- 交付时同步：将页面复制到 `_repo` 仓库，并在 `_repo/index.html` 导航页新增本演示卡片（置于"力的合成与分解"之后）

## 一、视觉风格（对齐系列）
1. 深色主题：`#121212` 背景、白字、`system-ui` 字体。
2. 主画布：深蓝底 `#0b1828`、蓝色边框 `#3388dd`、圆角，整宽，880:520 比例自适应。
3. 面板：蓝调半透明分组（`rgba(255,255,255,0.05)` + 细边框），主色 `#3388dd`。
4. 按钮：主色 `#3388dd`，hover / disabled 态。
5. 图表渲染与系列 `drawChart` 一致：深色网格 `#1f2d3d`、刻度文字 `#8899aa`、当前点游标半透明蓝。
6. DPR 高清画布 + ResizeObserver 响应式；图表区宽屏并排、窄屏单列；图表面板比例 20:9。
7. 本页为**单场景**页面，无模式 Tab。
8. 键盘快捷键：**空格** 播放/暂停（toggle）、**R** 重置；焦点在输入框/按钮时不触发。
9. 无障碍：按钮/滑条带 `aria-label`，实时数值区 `aria-live="polite"`。

## 二、页面布局（上中下三段式）
- **第 1 段（上部）**：主场景画布——悬球三力平衡实验台（整宽）。
- **第 2 段（中部）**：主画布正下方并排两张副图——左"F、T–θ 图"、右"张力正交分量 Tx、Ty–θ 图"，实时游标，外框蓝色 `#3388dd`。
- **第 3 段（下部）**：图表下方左右两面板并排——左侧参数设定面板、右侧实时数值面板。
- **最底部（整宽）**：控制按钮行：▶ 自动演示 | 暂停/继续 | 重置；按钮行之下为图例。
- **图例内容**：绳张力 T（黄）、水平拉力 F（橙）、重力 G=mg（蓝）、灰白虚线（绳延长线 / 平行四边形辅助 / 初始竖直位置）、幽灵球（初始位置）、待机可拖拽小球、快捷键。

## 三、物理模型与约定
1. 模型：轻绳（不可伸长）上端固定于悬点 O，下端系质量 m 的小球；小球受三个共点力——重力 G = mg（竖直向下）、水平向右的拉力 F、绳张力 T（沿绳指向 O）。
2. **"缓慢移动"= 准静态过程**：每一时刻三力均平衡（合力为零），过程是平衡态的连续过渡；页面顶部状态文字明确注明。
3. 矢量颜色约定（全页统一）：绳张力 T 黄 `#ffcc44`、水平拉力 F 橙 `#ff9933`、重力 G 蓝 `#66aaff`、辅助虚线 `rgba(255,255,255,0.4)`；图表曲线与对应矢量同色。
4. 角度约定：θ 为绳与**竖直方向**的夹角，范围 0°~85°（避开 θ→90° 发散）；θ=0° 即初始竖直悬挂。
5. 平衡方程（正交分解）：
   - 竖直：T·cosθ = G = mg
   - 水平：F = T·sinθ
   - ⇒ **F = mg·tanθ，T = mg/cosθ**
6. 张力正交分量：Tx = T·sinθ = F（水平分量恒平衡拉力）；Ty = T·cosθ = mg（竖直分量恒"托住"重力）。
7. **教学要点（fixed-note 必须呈现）**：
   - 共点力平衡条件：**合力为零** ⇔ ΣFx = 0 且 ΣFy = 0；
   - 缓慢移动 = 准静态，每一时刻都按平衡态处理；
   - θ 增大 → F、T 都增大；**T = mg/cosθ ≥ mg 恒成立**（张力始终不小于重力）；θ→90° 时 F、T → ∞（绳越接近水平越难拉）；
   - 三力平衡 ⇒ **任意两力的合力与第三力等大反向**：G 与 F 的合力 = −T，方向沿绳延长线；三力首尾相接构成**封闭三角形**；
   - （拓展）相似三角形法：力三角形与几何三角形相似，T/L = mg/(L·cosθ) = F/(L·sinθ)。
8. **重力加速度 g 固定为 10 m/s²**（常量，无控件，说明框注明）——与系列页统一。

## 四、主场景演示窗口
1. 悬点 O 位于画布上方偏左（约 x=330、y=70），顶部画天花板斜线阴影支架；O 正下方画**虚线竖直参考线**（初始竖直悬挂位置）+ 半透明**幽灵球**（初始位置示意）。
2. 绳：O 到小球的直线（像素长度固定约 260 px，与质量无关）；小球：半径约 13 px 实心圆（青白色），沿以 O 为圆心、绳长为半径的圆弧运动。
3. 三力矢量（均自球心画出，长度 ∝ 大小，公共比例系数 k 自适应）：
   - T 黄色，沿绳指向 O（k 受约束使 T 箭头不越过 O，≤ 绳长 90%）；
   - F 橙色，水平向右；
   - G 蓝色，竖直向下。
4. **平行四边形辅助（三力平衡可视化）**：自球心作 F、G 的平移虚线（橙/蓝虚线）交于角点——角点恰在**绳延长线**上、距球 T·k（灰白虚线画绳延长线）；角点处标注"G+F = −T"。θ≈0°（F < 0.1 N）时退化为共线，隐藏辅助并提示"竖直悬挂：T = G"。
5. θ 角弧：O 处竖直虚线与绳之间，弧标签"θ=XX°"沿角平分线方向外侧（单一弧，无碰撞问题）。
6. 比例尺：左下角线段标注（N），随 k 自适应。
7. 顶部状态文字：左侧"准静态平衡：ΣFx=0，ΣFy=0"（常显）；底部右侧提示"待机时可拖拽小球沿弧移动（吸附 1°）"。
8. 自适应缩放：k = min(右侧空间/F, 下方空间/G, 0.9×绳长/T)，使摆几何与三矢量全部入画；k 随 θ 连续变化（矢量"呼吸"与力的增长一致，同力的合成页行为）。

## 五、图表窗口（双图，横轴均为 θ ∈ [0°, 85°]，步长 15°）
- **左图：拉力与张力-夹角图 (F、T–θ)**
  - F = mg·tanθ（橙）、T = mg/cosθ（黄）双曲线；
  - **G = mg 水平参考虚线**（蓝，标注"G = mg"）——T 曲线始终位于其上方，直观呈现 T ≥ mg；
  - 当前 θ 游标 + 双当前点圆点 + 数值气泡（"F=17.32 / T=20.00"）。
- **右图：张力正交分量图 (Tx、Ty–θ)**
  - Tx = T·sinθ（橙）、Ty = T·cosθ（蓝）双曲线；
  - **Ty 为水平直线（恒等于 mg）**，叠加 G = mg 参考虚线重合验证；
  - 图注（图内右下小字）："Ty 恒等于 mg（托住重力），Tx 恒等于 F（平衡拉力）"；
  - 当前 θ 游标 + 双当前点 + 气泡。
- 纵轴 0 ~ mg/cos85° 的 1.05 倍向上取整到 10 N（m=1 时约 130 N），随 m 自适应；两图结束后游标定格在滑条设定 θ。

## 六、参数设定面板（左卡）
| 参数 | 范围 | 步长 | 默认值 | 说明 |
|------|------|------|--------|------|
| 目标夹角 θ | 0° ~ 85° | 1° | 60° | 绳与竖直方向夹角；拖拽小球联动 |
| 质量 m | 0.5 ~ 5.0 kg | 0.5 | 1.0 | 影响 G、F、T 数值（比例关系不变） |
| 播放速度 | 0.1 ~ 2× | 0.1 | 1 | 自动演示用，运行中实时可调 |

- 公式说明框（fixed-note）：呈现第三节第 7 条全部教学要点 + 平衡方程 F = mg·tanθ、T = mg/cosθ + g=10 固定说明。

## 七、实时数值面板（右卡，3 列 grid，实时更新）
| 名称 | 含义/公式 |
|------|-----------|
| 夹角 θ | 绳与竖直方向夹角 |
| 质量 m | 当前值 |
| 重力 G = mg | 恒定（蓝） |
| 水平拉力 F | mg·tanθ（橙） |
| 绳张力 T | mg/cosθ（黄） |
| T 水平分量 Tx | T·sinθ = F |
| T 竖直分量 Ty | T·cosθ = mg |
| T / G | 张力与重力之比（≥ 1，= 1/cosθ） |
| 平衡状态（span-full） | "准静态平衡：ΣFx=0，ΣFy=0"（θ≈0 时附"竖直悬挂：T = G"） |

## 八、操作按钮（3 个：▶ 自动演示 | 暂停/继续 | 重置）
沿用系列合并按钮约定，状态矩阵与"力的合成与分解"页完全一致：

| 运行状态 | 按钮 1 | 按钮 2 | 按钮 3 |
|---------|---------|--------------|---------|
| 初始/重置后（idle） | 启用，显示"▶ 自动演示" | 禁用，显示"暂停" | 启用 |
| 运行中（running） | 禁用，显示"演示中…" | 启用，**显示"暂停"** | 禁用 |
| 暂停中（paused） | 启用，显示"▶ 重新演示" | 启用，**显示"继续"** | 启用 |
| 结束（ended） | 启用，显示"▶ 再来一次" | 禁用，显示"暂停" | 启用 |

- **自动演示动画**：θ 从 0° 匀速扫到 85°（默认 1× 时单程 8 s）——小球沿弧缓慢向右上方移动、F 与 T 矢量连续增长、平行四边形随 sinθ 张开、双图游标同步前进；结束后回 idle 姿态，球回到滑条设定 θ（默认 60°）。
- 运行/暂停中禁用 θ、m 滑条；播放速度实时可调。
- 键盘：空格 = 播放/暂停、R = 重置。
- **拖拽交互**（仅待机态）：鼠标/触屏拖动小球，约束在以 O 为圆心、绳长为半径的圆弧上；θ = atan2(水平位移, 竖直位移)，钳制 0°~85°，吸附 1°，实时同步滑条与全部读数；光标 grab/grabbing。

## 九、标题 / 元信息
- 标题：`共点力平衡演示｜高中物理`
- 加 description meta，描述页面功能。

## 十、物理公式（实现参考，勿改动）
- 平衡方程：T·cosθ = mg；F = T·sinθ
- 解：F = mg·tanθ；T = mg/cosθ
- 张力分量：Tx = T·sinθ = mg·tanθ = F；Ty = T·cosθ = mg
- T/G = 1/cosθ ≥ 1；θ→90° 时 F、T → ∞
- g = 10 m/s² 固定；θ ∈ [0°, 85°]
- 场景几何（屏幕坐标 y 向下）：球位置 = O + 绳长px·(sinθ, cosθ)；k = min(右侧空间/F, 下方空间/G, 0.9×绳长px/T)；角点 = 球心 + (F·k, G·k)，位于绳延长线上距球 T·k

## 十一、验收标准
1. 页面深色主题与系列一致；布局：主画布 → 双图 → 左参数 / 右数据 → 底部 3 按钮 → 图例；无模式 Tab。
2. 默认参数（m=1.0 kg、θ=60°）：G=10.00 N、**F=17.32 N、T=20.00 N**、Tx=17.32 N、Ty=10.00 N、T/G=2.00，画布标注、数值面板、图表气泡三处一致。
3. 特殊值自检：θ=30° → F=5.77 N、T=11.55 N；θ=0° → F=0.00 N、T=10.00 N（画布隐藏 F 矢量与平行四边形辅助，显示"竖直悬挂：T = G"）；θ=85° → F≈114.30 N、T≈114.74 N。
4. 恒等式验证：Tx ≡ F、Ty ≡ mg（右图两曲线与左图 F 曲线、mg 参考线的对应关系正确）；T 曲线全程位于 G=mg 参考虚线上方。
5. 主画布：球沿弧移动，θ 角弧与竖直虚线正确；平行四边形角点始终位于绳延长线上且距球 T·k（几何自洽）；T 箭头不越过悬点 O。
6. 拖拽：小球沿弧拖至 45° 时 F=10.00 N、T=14.14 N，θ 滑条同步显示 45°；运行中不可拖。
7. 自动演示：θ 扫描 0°→85°（约 8 s @1×），球沿弧缓慢上移、矢量增长平滑；按钮状态矩阵正确；结束后球回 θ=60°、读数恢复默认值。
8. 空格 / R 快捷键可用且不与输入框冲突；按钮与滑条带 aria-label，数值区 aria-live。
9. 交付时已同步 `_repo` 仓库并在 `index.html` 增加卡片（置于"力的合成与分解"之后）。

## 十二、参考文件
- 风格与交互基准：`/home/wq/physics-work/force-composition-demo.html`
- 同类需求文档：`/home/wq/physics-work/requirement-force-composition-v1.md`
- 图表蓝框对齐规范：`/home/wq/physics-work/requirement-apparent-weight-v4.md`
- 按钮合并约定：`/home/wq/physics-work/_repo/requirement-horizontal-linear-motion-v2-button-merge.md`
- 交付目录：`/home/wq/physics-work/force-equilibrium-demo.html`
