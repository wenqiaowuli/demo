# 需求：示波器演示网页 v2（波形组合版）

> 状态：2026-09-21 用户裁定对页面进行重构：电压波形可选（水平/竖直各五种——后经用户裁定追加余弦、手动组合），页面布局重排并整体参照平抛运动演示页风格，左上原理图静态化。本文件为 v2 编码实施的唯一依据；v1（含 v1.1/v1.2/v1.3 修订，`requirement-oscilloscope-v1.md`）保留为历史依据，凡与本文件冲突处一律以本文件为准。物理公式口径沿用 v1，不做变更。**v2 已于 2026-09-21 编码实施并通过 65 项物理核 + 63 项浏览器端到端验证（记录见第十六节），已推送 GitHub。**

## 一、目标与总体说明

1. 主题不变：示波管工作原理（加速 → 偏转 → 荧光屏成像）。核心变化是把"模式一偏转演示 / 模式二波形扫描"的双模式结构，重构为**统一的波形组合演示**：
   - 水平偏转电压 Ux(t) 可选**锯齿 / 正弦 / 余弦 / 方波 / 直线**五种波形之一（余弦为 2026-09-21 用户追加裁定）；
   - 竖直偏转电压 Uy(t) 同样五种可选；
   - 用户手动组合（5×5 = 25 种），示波器荧光屏按 X–Y 合成实时输出对应波形。
   - 默认组合：**水平 = 锯齿、竖直 = 正弦**（即课本常规示波器显示信号波形）。
2. 物理口径沿用 v1 第一节，全部不变：e = 1.6×10⁻¹⁹ C、m = 9.1×10⁻³¹ kg、L = 4 cm、d = 1 cm、L′ = 18 cm、K = 0.4 m、`X = 0.4·Ux/U1、Y = 0.4·Uy/U1`（cm 数 = 40·U/U₁）、灵敏度 ∝ 1/U₁、打极板临界 |U偏| = 0.125·U₁（板内 5 mm、屏上 ±5 cm）、屏有效范围 ±6 cm。
3. 交付文件仍为 `oscilloscope-demo.html`（整页重构）；物理核保持"纯函数、可脱离 DOM 验证"结构；`osc-test.mjs` 与 `osc-cdp.mjs` 按本需求同步升级；`_repo` 与交付目录哈希一致约定不变。

## 二、页面布局（参照 projectile-motion.html 风格，自上而下单列）

整体排版风格参照平抛运动模型演示网页 `projectile-motion.html`：单列 max-width 880px 居中、浅色主题（#eef2f6 底、白底卡片、#2563eb 蓝框圆角画布、`.group-title` 小标题、卡片阴影）、`group-title` + 网格 + 滑条行的组件样式。自上而下依次为：

1. **第 1 排（左右两栏）**：
   - 左栏：**示波管原理示意图**（静态，见第三节）；
   - 右栏：**示波器荧光屏**（动态合成波形，见第四节）。
   - 两栏宽度建议左 ~45%、右 ~55%（编码可微调）；窄屏（≤720px）改上下堆叠，原理图在上。
2. **第 2 排（左右两栏）**：左 **Ux–t 波形图**、右 **Uy–t 波形图**（动态，见第五节）。窄屏堆叠。
3. **按钮条（第 2 排两图下方，play-bar 样式）**：三个按钮 **开始 / 暂停 / 重置**（状态机见第六节），按钮右侧保留**播放速度下拉框**（0.5×/1×/2×/4×，select，参照平抛页）。
4. **第 4 排：实时数值**（独立模块，整行分列显示：宽屏 5 列网格、窄屏 2 列，样式同平抛页 data-group；字段清单见第七节）。
5. **第 5 排：参数设定**（独立模块，置于实时数值模块**下方**；波形选择控件在此处出现，见第八节）。
6. **最下面：推导与判断**（formula-card 样式，见第九节）。
7. 图例（legend）保留，置于推导与判断上方或按钮条下方，随布局微调。

## 三、示波管原理示意图（左上，静态）

1. 画法沿用 v1 第二十节已实施结果：灯丝 + K 阴极 + A 加速阳极、课本式加速电源符号（K 接 −、A 接 +）、串联极板 YY′（前，上下两块水平板）与 XX′（后，**竖直放置的两块极板**——水平偏转由左右夹住电子束的竖直极板承担，侧视以两根前后错位的竖直透视平行四边形表示，束流从两板之间通过，端部分别标注 X 与 X′）、椭圆荧光屏面、L′ = 18 cm 辅助线、"电子枪 / 偏转电极 / 荧光屏"整体标注、单板标注 Y/Y′/X/X′、全页带撇记法、"几何示意，未按比例"与串联画法说明。（**2026-09-21 用户勘误：XX′ 不得画成与 YY′ 类似的水平板；v1.3 20.1.1"前后板平行于纸面"的画法口径就此作废，以本条为准**。）
2. **不得出现任何动画效果**：无电子束流动画、无流动电子点、无余辉、无扫描、无闪烁；束流路径以静态示意线绘制（直线 + 极板区段的静态弯折示意即可，不随参数变形）。
3. 图上 U₁ / Uy / Ux 数值标注**仅随参数刷新数字**（纯文字更新，无过渡动画）；除此之外整图完全静态。

## 四、示波器荧光屏（右上，动态合成）

1. 以当前时刻 t 的瞬时电压实时合成光点位置：`X(t) = 0.4·Ux(t)/U1`、`Y(t) = 0.4·Uy(t)/U1`（cm），画布 y 轴向上、像素换算 `center + (X, −Y)·pxPerCm`。
2. 波形随时间推进逐点绘制，保留**最近 2 个扫描周期的轨迹**，旧迹余辉渐隐（沿用 v1.2 笔画缓冲与余辉规则；缓冲 ≤2 周期）。
3. 屏面沿用 v1 20.3：±6 cm 方形量化网格（主网格 2 cm、副网格 1 cm）、±5 cm 打极板临界辅助线、中心十字四端轴标 上Y 下Y′ 右X 左X′、标题含"从右向左看"、荧光绿亮点配色。
4. 打极板处理（逐时刻）：某时刻任一方向 |U(t)| > 0.125·U₁，该时刻电子打在对应极板，屏上轨迹缺失（削顶/截断）；两方向同时超限 → 双向局部缺失。状态文字沿用带撇记法口径（"电子打在 YY′/XX′ 极板上""电子同时打在 XX′、YY′ 偏转极板上"）。
5. 锯齿回扫阶段束流熄灭：该时段屏上不绘制（沿用 v1 5.2 / 18.2 口径）。

## 五、Ux–t 与 Uy–t 波形图（第 2 排）

1. 两图结构相同：横轴 t（显示窗口约 2 个锯齿周期），纵轴电压（V）；动态滚动或整周期刷新均可，**须带当前时刻游标点**（与屏上光点同一时刻，视觉联动）。
2. 波形形状按第八节定义绘制：锯齿正扫实线、回扫陡降红色虚线（图内标注"回扫（束流熄灭）"）；正弦光滑曲线；方波含垂直跳变沿；直线为水平线。
3. 超临界段（|U(t)| > 0.125·U₁）用红色虚线叠加标出，与屏上削顶/截断对应。
4. 图上标注当前幅度/直流值与 U₁；暂停时整图冻结（画布逐字节静止）；调参即时反映（暂停中调参按静态预览重画）。

## 六、播放控制（三个按钮 + 速度下拉）

1. **初始不自动运行（2026-09-21 用户裁定）**：页面加载后为待机态（t = 0、屏上无轨迹），**开始**按钮显示"开始"并可点击；运行中禁用（文字"运行中…"）；暂停后显示"重新开始"（从 t = 0 清屏重跑当前参数）。
2. **暂停/继续**：运行中可暂停；暂停后该按钮文字变"继续"；待机态暂停按钮禁用；暂停时冻结全部动画时钟（vclock 冻结规则沿用 v1.2 18.2：画布逐字节一致）。
3. **重置（2026-09-21 用户裁定）**：**任何状态随时可点**；恢复默认设置（波形组合 锯齿×正弦、幅度 80/50、U1=1000、速度 1×）并清屏回 t = 0、回到待机态。
4. 播放速度下拉：0.5×/1×/2×/4×（默认 1×），作用于所有波形推进与游标。
5. 快捷键沿用：空格 = 播放/暂停，R = 重置。

## 七、实时数值（第 4 排，整行分列）

独立模块，标题"实时数值"，网格分列（宽屏 5 列、≤800px 时 2 列，同平抛页）。字段清单（编码可增删措辞，但须覆盖以下信息）：

| 字段 | 说明 |
|---|---|
| 时间 t | 运行时间（s） |
| 加速电压 U1 | 实时值（V） |
| 水平电压 Ux | 按类型显示：瞬时 Ux(t)（V）；直线形显示直流值 |
| 竖直电压 Uy | 同上 |
| 屏上水平偏移 X | 瞬时 X(t)（cm） |
| 屏上竖直偏移 Y | 瞬时 Y(t)（cm） |
| 出射速度 v₀ | √(2eU1/m)，×10⁷ m/s |
| 通过偏转区时间 t | L/v₀（ns） |
| 成像状态 | 正常成像 / 电子打在 YY′(XX′) 极板上 / 同时打在 / 回扫熄灭（动态文字，带撇记法） |

## 八、参数设定（第 5 排，独立模块）

1. **水平电压 Ux**：
   - 波形选择：**锯齿 / 正弦 / 方波 / 直线** 四选一（分段按钮，沿用系列 .seg 样式，aria-pressed）；
   - 幅度滑条：锯齿/正弦/余弦/方波时为幅度 A，0~100 V（均为非负幅度）；直线时滑条切换为**直流电压值 U₀，−100~+100 V**；
2. **竖直电压 Uy**：控件与规则同上，独立取值；
3. **加速电压 U1** 滑条：500~2000 V（沿用）；
4. **显示教学辅助**开关：临界辅助线、游标标注等（沿用 v1 aux 规则，具体覆盖物随新布局定义）；
5. 波形类型、幅度/直流值、U₁ 调节均**即时反映**到荧光屏、两张波形图与实时数值。
6. **清屏规则（2026-09-21 用户裁定）**：切换波形组合或调节参数后，荧光屏上的旧轨迹立即清除，按新参数重新积累，不得出现新旧两组波形叠加。
7. **波形选择锁（2026-09-21 用户裁定）**：波形类型仅**待机态**可改（含加载后与重置后）；演示过程与暂停时波形分段按钮禁用。幅度/直流值与 U₁ 滑条不受此限，运行中仍可即时调节。
8. **初始幅度（2026-09-21 用户裁定）**：水平与竖直初始电压相等（默认 **80 V / 80 V**），两方向幅度滑条上限均为 **100 V**（直流模式 ±100 V）。

> 注：原第 5 条"运行中可调"自第 7 条裁定起仅适用于幅度/直流值与 U₁ 滑条；原第 6 条编号顺延。
6. **清屏规则（2026-09-21 用户裁定）**：切换波形组合或调节参数后，荧光屏上的旧轨迹立即清除，按新参数重新积累，不得出现新旧两组波形叠加。

## 九、推导与判断（最下面）

formula-card 样式（同平抛页），固定呈现 + 动态判断两部分：

1. **固定推导**：加速 eU₁ = ½mv₀² ⇒ v₀ = √(2eU₁/m)；板内位移 y = UL²/(4dU₁)；出射角 tanθ = UL/(2dU₁)；屏上偏移 X = 0.4·Ux/U₁、Y = 0.4·Uy/U₁（cm 数 = 40·U/U₁）；灵敏度 ∝ 1/U₁；打极板临界 |U偏| = 0.125·U₁；五种波形表达式（锯齿 −A→+A 线性 + 回扫熄灭、正弦 A·sin(2πq)、余弦 A·cos(2πq)、方波 ±A 交替、直线恒定 U₀）；周期关系（锯齿周期 = 正弦/余弦/方波周期 ×2）。
2. **动态判断**：当前组合的成像状态一句话（如"锯齿×正弦：屏显 2 个完整周期正弦波形""正弦×正弦：李萨如图形（椭圆/直线）""双向超临界：屏上无完整波形"），随参数即时更新。

## 十、波形定义（物理核，纯函数）

1. 周期基准（1× 速度）：正弦、余弦、方波周期 **T = 4 s**；锯齿周期 **2T = 8 s**（正扫占 90%、回扫占 10%，回扫熄灭——沿用 v1 比例；锯齿×正弦/余弦/方波时屏显 2 个完整周期，与课本图 10.5-5 t₁/2t₁ 对应）；直线无周期。
2. 波形函数（q 为该方向本周期内进度 ∈[0,1)，A 为幅度，U₀ 为直流值）：
   - 锯齿：正扫 `U = −A + 2A·q`，回扫返回"熄灭"标记（沿用 v1 phaseToQ → null 机制）；
   - 正弦：`U = A·sin(2πq)`；
   - 余弦：`U = A·cos(2πq)`；
   - 方波：`q < 0.5 → +A`，`q ≥ 0.5 → −A`（跳变沿在屏上保留垂直连线段）；
   - 直线：`U = U₀`（恒定）。
3. 合成与判定：`X(t) = 0.4·Ux(t)/U1`、`Y(t) = 0.4·Uy(t)/U1`；逐时刻 sampleValid 判定（|Ux(t)|、|Uy(t)| vs 0.125·U₁）；直线形电压恒定，超临界则整段缺失（屏上该方向无偏转轨迹）。
4. 物理核内部状态码（'YY'/'XX'/'both'、波形类型标识）可不带撇，展示文案一律带撇（沿用 v1 20.2.2 口径）。

## 十一、相对 v1 取消的功能（用户裁定重构）

1. 模式一/模式二划分与 10 秒自动演示剧情（demoPose 时间线）；
2. 拖拽屏上亮点交互；
3. v₀–U₁ 曲线图与屏上偏移–偏转电压直线族两张图表（由第 2 排两张波形图取代）；
4. v1.3 场景内嵌的 Ux–t 小图（升级为第 2 排整图）。

## 十二、无障碍、窄屏与调试钩子

1. 无障碍沿用 v1.2 18.3 规则：按钮动态 aria-label、波形选择分段按钮 aria-pressed、滑条 aria-valuetext 带单位、实时数值区 aria-live 仅变化时写入。
2. 窄屏：≤720px 两栏改上下堆叠（原理图在上、Ux 图在上）；实时数值 2 列；场景/图内文字按 14.3 精简规则压缩；无横向溢出。
3. `window.getOscDebug()` 保留为测试专用（注释注明），扩展暴露：两方向波形类型、相位、瞬时电压、成像状态码、笔画缓冲等。

## 十三、v2 验证清单（编码完成后必须全部通过）

1. 布局：五排结构完整、第 1/2 排两栏、按钮条在两图下方、实时数值整行分列、参数设定在其下方、推导与判断在最下面；整体风格与平抛页一致（蓝框、卡片、网格、play-bar）。
2. 静态原理图：无任何动画（连续帧逐字节一致）；课本画法要素齐全（串联极板、椭圆屏面、电源符号、带撇标注）；数值标注随参数仅刷新数字。
3. 25 种波形组合逐一验证屏上输出正确，至少覆盖：锯齿×正弦（默认，屏显 2 周期正弦）、锯齿×方波（2 周期方波）、锯齿×直线（水平直线）、直线×正弦（竖直直线）、正弦×正弦（李萨如椭圆/直线）、余弦×余弦（李萨如椭圆/直线）、锯齿×余弦（2 周期余弦）、方波×方波（矩形）、直线×直线（静止亮点）。
4. 打极板逐时刻判定：超临界削顶/截断与红色虚线标记一致；回扫熄灭；状态文字带撇。
5. 波形图：Ux–t/Uy–t 形状与所选波形一致、游标联动、超临界红色虚线、暂停冻结（逐字节一致）、调参即时反映。
6. 播放控制：开始/暂停/继续/重置状态机正确；速度 0.5~4× 生效；空格/R 快捷键。
7. 实时数值字段齐全且随参数/时间刷新；成像状态动态文字正确。
8. 无障碍属性完整；窄屏 375px 无横向溢出、布局正确堆叠。
9. 物理核 Node 断言（五种波形函数、周期关系、逐时刻临界、合成公式、v1 沿用公式回归）+ 浏览器 CDP 端到端全部通过，控制台零错误。
10. 截图人工复核：宽屏 1200px 全页（默认组合 + 至少 2 种代表性组合）、窄屏 375px。

## 十四、开放裁定记录

1. **布局排布**（用户 2026-09-21 裁定）：实时数据为单独模块整行分列显示；参数设定为单独模块置于实时数据下方；整体排版风格参照平抛运动模型演示网页。
2. 直线形 = 可调直流电压（−100~+100 V，滑条即电压值）——采用推荐方案 ①，如需固定 0 V 请指出。
3. 静态原理图数值标注仅刷新数字（无动画）——采用推荐方案。
4. 需求另立 v2 文件（本文件），v1 保留为历史——采用推荐方案（符合 v1 文件头"主题功能增删另立 v2"约定）。
5. 波形选择控件为分段按钮（.seg 样式）——采用推荐方案。
6. **（2026-09-21 用户勘误）** 原理图中水平偏转的 XX′ 极板应按**竖直放置**的极板绘制（侧视为前后错位的两根竖直透视平行四边形，束流从两板之间通过）；此前"与 YY′ 类似的上下错位水平平行四边形"画法有误，v1.3 20.1.1 对应口径一并作废。
7. **（2026-09-21 用户裁定）** 切换波形组合或调节参数后，荧光屏旧轨迹立即清除、按新参数重新积累，不得新旧叠加。
8. **（2026-09-21 用户裁定）** 水平/竖直电压在四种波形基础上各增加**余弦**波形（五种可选、5×5 = 25 种组合；余弦周期与正弦相同为 4 s）。
9. **（2026-09-21 用户裁定）** 页面加载后**不自动开始演示**（待机态）；点击**重置**恢复默认设置并回到待机态，重置按钮任何状态随时可用。
10. **（2026-09-21 用户裁定）** 波形类型仅待机态可手动修改（开始前与重置后）；演示过程与暂停时不能修改（分段按钮禁用）。
11. **（2026-09-21 用户裁定）** 水平与竖直方向初始电压相等（默认 80 V / 80 V），最大值均为 100 V。

## 十五、参考文件

- 风格与布局基准：`/home/wq/physics-work/projectile-motion.html`（平抛运动模型演示）。
- 上一版需求（历史依据）：`/home/wq/physics-work/_repo/requirement-oscilloscope-v1.md`。
- 课本插图依据：人教版《物理》必修第三册 10.5 节——图 10.5-4（示波管原理图）、图 10.5-5（扫描电压）。
- 交付目录：`/home/wq/physics-work/oscilloscope-demo.html`。

## 十六、v2 实施与验证记录

- **2026-09-21 v2 编码实施完成**（页面整体重构，交付文件仍为 `oscilloscope-demo.html`）：
  - 布局：五排结构落地——第 1 排左静态原理图（560×360 设计画布）+ 右动态荧光屏（400×360）；第 2 排 Ux–t / Uy–t 波形图（20/9）；按钮条（开始/暂停/重置 + 0.5/1/2/4× 速度下拉）置于两图下方；实时数值（5 列网格、9 字段含成像状态）；参数设定（每组"波形分段按钮 + 幅度/直流滑条"，U1 滑条，显示教学辅助开关）；底部推导与判断（固定公式 + 动态"当前组合 / 成像判断"行）。整体样式对齐平抛页（浅色主题、蓝框圆角、卡片、data-grid、play-bar）。
  - 波形系统：物理核新增 `wavePeriod / waveU / waveAt / sampleValid` 与 `WAVE_TYPES`；锯齿 8 s（正扫 90% + 回扫 10% 熄灭）、正弦/方波 4 s、直线恒定 U₀；`waveAt` 对负时间（回看窗口）按周期回绕。默认组合 锯齿(Ax=80)×正弦(Ay=50)、U1=1000。
  - 屏显：轨迹缓冲按时间窗口保留最近 2×max(周期)（≤5000 点、0.02 s 子步采样），打极板/回扫断笔；当前亮点荧光绿 + 辉光；网格/临界线/四端轴标/标题沿用 v1.3 口径。状态口径：瞬时回扫 → "回扫阶段：束流熄灭"；瞬时打极板 → "电子打在 XX′/YY′ 极板上 / 电子同时打在 XX′、YY′ 偏转极板上"；聚合削顶 → "波形局部削顶/截断（部分时段打在 XX′/YY′ 极板）"（回扫熄灭不计入削顶）；其余 → "正常成像 X=…、Y=…"。
  - 静态原理图：课本图 10.5-4 甲画法（串联 YY′/XX′、椭圆屏面、电源符号 K−/A+、电子枪/偏转电极/荧光屏整体标注、单板标注 Y/Y′/X/X′、L′ 与串联画法注），无任何动画；U1/Ay(Ax)/U₀ 数值仅刷新数字；compact 按 14.3 精简。
  - 取消项按第十一节执行：模式一/二与 demoPose 时间线、拖拽亮点、v₀–U1 与偏移–电压双图、场景内嵌小图均已移除；`getOscDebug` 改暴露 phase/t/speed/波形类型/瞬时电压/状态/轨迹点数。
- **v2 最终验证**：物理核 Node 断言 **65/65 通过**（`osc-test.mjs` 重写：波形周期与四波形函数、waveAt 时间映射与负时间回绕、逐时刻 sampleValid 与削顶占比、合成自洽、v1 公式回归）；浏览器 CDP 端到端 **63/63 通过**（`osc-cdp.mjs` 重写为 v6：五排布局顺序与两栏、默认组合动态波形、暂停四画布逐字节一致、4×4 波形切换遍历、直流×正弦竖直直线 / 直流×直流静止亮点 / 方波×方波矩形 / 正弦×正弦李萨如、直流 −100~+100 V 滑条、削顶红色虚线与"同时打在"轮询、速度 0.5~4× 相对推进、重置与空格/R、aria 完整性、原理图无动画 + 数值刷新、窄屏 375px 堆叠与 2 列、控制台零错误；测试结束自动关闭标签页）。连续 3 轮全通过。
- 实施期修复：① 初始化遗漏 `buildDataCells()` 调用（实时数值区空白）；② `clipAggregate` 将锯齿回扫误计为削顶 + `waveAt` 负时间相位为负导致"正常成像"误报"局部削顶"（核内修负时间回绕、聚合口径排除回扫）；③ 推导区动态行误用 `textContent` 致 `<b>` 字面显示（改 `innerHTML`，字符串均为页内常量）；④ 测试侧 `await read(...).includes` 优先级误写与固定墙钟阈值在无头 rAF 节流下不稳定（改轮询/相对推进比较）；⑤ 测试运行泄漏标签页（36 个并存 rAF 循环拖慢渲染帧率）——`osc-cdp.mjs` 结束时自动关闭标签页后连续 3 轮稳定。
- 截图人工复核（宽屏满窗口、削顶组合、窄屏 375px）：五排布局与两栏正确、满窗口屏显 2 个完整周期正弦 + 辉光游标、削顶/李萨如等组合图形正确、窄屏堆叠与 2 列数据无溢出、带撇字符渲染正常。`_repo` 与交付目录哈希一致，已提交推送 GitHub 仓库。
- **2026-09-21 勘误修订（用户发现 2 处，当日修复）**：
  - ① 原理图 XX′ 极板画法有误（画成了与 YY′ 类似的水平板）——按裁定改为竖直放置极板：侧视以两根前后错位的竖直透视平行四边形绘制（远板浅色、先画；近板深色、后画，束流呈现在两板之间），单板标注 X（近板上方）与 X′（远板下方），名称改为"XX′ 水平偏转（竖直放置）"；同步作废 v1.3 20.1.1"前后板平行于纸面"画法口径（见第十四节裁定 6）。
  - ② 切换波形组合后荧光屏旧轨迹未清除（新旧波形叠加至轨迹自然老化为止）——按裁定在 `afterParamChange()`（波形类型、幅度/直流值、U₁ 全部参数入口）中清空屏显轨迹缓冲，新参数从零重新积累；速度调节不清屏（波形形状不变）。
  - 验证：物理核 65/65（不受影响）；端到端 66/66（新增：原理图 XX′ 竖直极板紫色像素探测、YY′ 水平极板橙色像素探测、切换组合后轨迹点数清零并重新积累），控制台零错误；截图复核 XX′ 竖直板与清屏效果正确。`_repo` 与交付目录哈希一致，已提交推送。
- **2026-09-21 功能追加（用户裁定 2 项，当日实施）**：
  - ① 波形扩展：水平/竖直电压各新增**余弦**波形（`U = A·cos(2πq)`，周期 4 s，与正弦同频），五种可选、5×5 = 25 种组合；分段按钮各加"余弦"项，组合说明与锯齿组合口径同步覆盖余弦。
  - ② 播放控制：页面加载后**不再自动开始演示**（初始待机态：t=0、屏无轨迹、开始按钮"开始"可点、暂停禁用）；**重置随时可点**，恢复默认设置（锯齿×正弦、80/50、U1=1000、速度 1×）并回到待机态；空格在待机态即开始，R 等价于重置。
  - 验证：物理核 **70/70**（新增余弦类型/周期/函数值/waveAt 断言）；端到端 **75/75**（更新：待机态断言、手动开始、5×5 类型遍历、余弦×余弦李萨如、锯齿×余弦波形、运行态重置回待机、空格开始/暂停/继续、R 恢复默认、10 个波形按钮 aria-pressed），控制台零错误；截图复核待机态（屏上仅起始辉光点、按钮"开始/继续禁用/重置"）与五波形分段按钮渲染正确；`_repo` 与交付目录哈希一致，已提交推送。
- **2026-09-21 交互修订（用户裁定 2 项 + 修复 1 个缺陷，当日实施）**：
  - ① 缺陷修复：重置后波形分段按钮无法修改——`resetAll()` 以新对象替换 `st.Ux/st.Uy`，而分段按钮事件闭包持有旧对象引用，改动落在旧对象上；改为处理器内直引 `st.Ux/st.Uy`。
  - ② 波形选择锁：按裁定，波形类型仅待机态（加载后/重置后）可改；运行与暂停时波形分段按钮禁用（幅度/直流值与 U₁ 滑条不受限，运行中仍即时可调）。
  - ③ 初始幅度：水平/竖直默认幅度改为相等（80 V / 80 V），滑条上限 100 V 不变。
  - 验证：物理核 70/70；端到端 **77/77 连续 3 轮**（新增：待机态 5×5 遍历前先重置、运行/暂停中点击波形按钮无效且按钮禁用、重置解锁、待机配置→开始→探测→重置的组合验证流程、运行中调参清屏、初始幅度 80/80 断言），控制台零错误；`_repo` 与交付目录哈希一致，已提交推送。
