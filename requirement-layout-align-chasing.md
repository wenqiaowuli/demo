# 需求：全系列演示页布局对齐追及相遇页（播放条/倍速/公式判定的位置统一）

## 背景与目标
- 参照 `chasing-meeting-model.html`（追及相遇模型演示）的排版布局，调整其余 11 个演示页：
  1. **开始 / 暂停 / 重置 + 动画倍速** 放到与追及页相同的位置 —— 动画画布正下方的播放条内，顺序一致：`开始 | 暂停 | 重置 | 倍速`；
  2. **相关计算公式与判定方法** 放到与追及页相同的位置 —— 图表行与参数控制区之间的**全宽独立卡片**。

## 参照页布局（chasing-meeting-model.html，不改此页）
- 运动动画卡片：画布 → `.bar` 播放条：`▶ 开始 | 暂停 | 重置 | 倍速 select(0.5×/1×/2×/4×) | 时间轴 | 时间标签`
- 图表行（v-t / x-t 两列）
- 「公式与判定」全宽白卡（含判定结论）
- 底部两列：参数控制 | 实时数据

## 各页现状 → 改动
11 页同构：三按钮在页底 `.actions`；倍速 range 滑条与 `.fixed-note`（公式/判定文字）都在右侧参数面板内。

改动（每页相同）：
1. **播放条 `.play-bar`**：插入 `.canvas-wrap` 内、动画画布之后；按钮三枚（id/文案/禁用逻辑不变）+ 倍速改为与追及页同款 `<select>` 下拉框；删除页底 `.actions` 行及其 CSS。
2. **倍速控件**：range 滑条 → select 下拉（select 同样触发 input 事件，JS 监听不变）；删除参数面板中的倍速行与 `×` 数值显示（`speedVal/pSpeedVal/tsVal/spVal`）及其 JS 更新行。
   - 各页选项/默认值（保留原默认节奏）：
     - apparent-weight `1×`；free-fall / vertical-throw / horizontal-linear / oblique-throw `0.5×`；force×2 `1×`；block-plate `1×`（选项 0.5/1/2/4，JS 由 `parseFloat(elTs.value)/10` 改为直接取值）
     - horizontal/inclined-conveyor：`0.25×/0.5×/1×/2×/4×`，默认 `1×`
     - spring-vs-rope：`0.25×/0.5×/1×/2×`，默认 `0.5×`（原 0.6 最近取整）
3. **公式与判定卡片 `.formula-card`**：把参数面板内的 `.fixed-note` 原文移入，标题「公式与判定」，置于 charts-row 之后、control-panel 之前；卡片样式与页内 param/data 卡一致。
4. 按钮的状态矩阵、快捷键、场景预设按钮（左端/右端、底端/中部/顶端）、图例行、各页功能全部不变。

## 不改
- `chasing-meeting-model.html`（基准）；`_repo/` 内页面；动画物理、图表、数据面板、交互逻辑。

## 验收标准
- [ ] 11 页播放条均位于动画画布正下方：开始 | 暂停 | 重置 | 倍速下拉，与追及页同位同序。
- [ ] 11 页「公式与判定」为全宽卡片，位于图表行与参数控制区之间，与追及页同位。
- [ ] 倍速下拉选择后动画速度正确变化；运行/重置等按钮行为与改造前一致。
- [ ] 页面底部无残留空播放行；参数面板无残留倍速行。
- [ ] 全部页面浏览器加载零报错，动画可正常播放。
