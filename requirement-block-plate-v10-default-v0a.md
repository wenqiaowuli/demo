# 板块运动演示 · v10 物块默认初速度调整需求

## 1. 背景与目标
- **对标文件**：`block-plate.html`（板块运动演示，当前含 v9 地面位置变更）。
- **现状**：物块 A 初速度滑条（`v0A`，范围 2–20 m/s、步长 0.5）默认值为 **12 m/s**。
- **目标**：物块 A 默认初速度调整为 **10 m/s**。

## 2. 改动点
- **HTML**：滑条 `<input id="v0A" ... value="12">` → `value="10"`；初始显示 `<div id="v0AVal">12.0 m/s</div>` → `10.0 m/s`。
- **JS**：参数默认 `P = { v0A: 12, ... }` → `v0A: 10`；`reset()` 中兜底 `parseFloat(elV0A.value) || 12` → `|| 10`。
- **文件头注释**：补一行 v10 变更记录（v₀A 默认 12→10）。

## 3. 非目标（本次不做）
- 不改 v₀B（默认 0）、质量、摩擦因数、板长、动画速度等其余默认参数。
- 不改滑条范围（2–20 m/s）与步长（0.5）。
- 不改物理逻辑、绘制与交互。

## 4. 验收标准
- [ ] 页面加载后（未拖动滑条时）物块 A 初速度为 10.0 m/s，预览态与数据面板一致显示。
- [ ] reset 后恢复 10.0 m/s；兜底路径（滑条值异常时）同为 10。
- [ ] 其余默认参数与行为无回归；v₀A 仍可在 2–20 m/s 范围内调节。
