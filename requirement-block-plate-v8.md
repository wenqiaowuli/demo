# 需求：板块运动演示 v8（摩擦力箭头比例长度 / 板块灰色 / 图表颜色一致）

> 本需求由用户于 2026-09-13 确认，基于 v7 需求文件修订。
> 待修改文件：`/home/wq/physics-work/_repo/block-plate.html`

---

## 一、摩擦力箭头统一改为"比例长度"（撤销定长）
1. **物块**（红色 #ff4444）：线段长短代表摩擦力大小；尾部锚定物块几何中心；
   尖端外侧 22px 标红色小 "f"（均不变，仅长度规则变更）。
2. **板块**（黄色 #ffdd57）：线段长短代表摩擦力大小；尾部**锚定板块几何中心**
   （板身中心线）；尖端外侧 22px 标**黄色小 "f"**；涵盖两个力——
   物块给板块的摩擦力（原 f₁′）与地面给板块的摩擦力（原 f₂，橙红改黄），
   两力方向相反时呈对称受力图样式，同向时允许重叠（合成视觉效果）。
3. **长度明显化**：比例系数 2.4 → **5 px/N**，长度上限 60px → **80px**、
   下限 10px。示例：默认滑动摩擦 3.92 N ≈ 20px；地面摩擦最大约 80px 封顶。
4. 预览态不显示箭头、下落过渡渐隐渐入等既有规则保留。
5. 底部图例更新："→ f（红，物块受摩擦）"、"→ f（黄，板块受摩擦）"。

## 二、板块颜色调整为灰色（去渐变），图表颜色与板块填充一致
1. **场景**：板身纯灰 **#8a8f98** 填充（无渐变），描边 #c0c4cc，
   去除板身装饰纹理线；阴影保留。
2. **v-t 图 / s-t 图**：板块曲线、图内图例色块、播放头圆点、
   s-t 末端位移标注由橙色 #ff9933 改为 **#8a8f98**（与板身填充一致）。
3. 页面底部图例：板块图标由 🟧 改为灰色样式（■ + #8a8f98）。

## 三、验收标准
1. 默认参数滑动阶段：物块红箭头 ≈ 20px（3.92N×5），μ₁ 调大后箭头变长；
   板块黄箭头从板中心出发、长度随摩擦力变化，旁边有黄色小 f。
2. 板身无渐变、无纹理线，填充 #8a8f98；场景中无橙色板块残留。
3. v-t/s-t 图板块曲线为 #8a8f98，与板身一致；图例、播放头同步。
4. 回归：v7 的 f 符号位置、区域外裁剪、v6 的暂停/预览态无标识/结束判定不受影响。

---

## 修订记录（2026-09-13，用户报告演示错误后修订）

### 修订 1：【缺陷修复】μ₂ > 0 共速锁死瞬间动画冻结
- **现象**：μ₂ > 0 且 μ₁m_A > μ₂(m_A+m_B) 时（板块加速→共速→应一起减速），
  动画在共速瞬间冻结，速度数据不再变化。
- **原因**：v8 批量替换箭头长度时，物块静摩擦分支误引用未定义变量 `fMag`
  （应为 `fStat`），共速锁死后首次绘制受力箭头时抛 ReferenceError，
  动画帧循环崩溃。
- **修复**：改回 `arrowLen(fStat)`；回归验证 μ₂>0 全流程
  （板块加速→共速→锁死共同减速→双停→"演示结束"）恢复正常。

### 修订 2：物理行为确认（用户描述的两种 μ₂ > 0 情形）
1. **μ₁m_A > μ₂(m_A+m_B)**：物块减速、板块从静止（或初速）开始加速，
   两者同向运动 → 先共速 → 共速后一起匀减速（μ₂g）→ 最终停止。
   经数值与浏览器双重复核，物理引擎行为正确。
2. **μ₁m_A ≤ μ₂(m_A+m_B)**：板块保持不动；**板块受到的地面静摩擦力与
   物块给它的滑动摩擦力大小相等**（均为 μ₁m_A·g）——
   场景中板块的黄色摩擦力箭头长度据此绘制（与物块红色箭头等长），
   数据面板"物块摩擦力 f"不受影响。此为既有正确行为，书面确认。

### 修订 3：几何中心标记点
1. 运行/结束态下，在**物块几何中心**与**板块几何中心**各绘制一个明显的
   白色实心圆点（半径约 2.5px），直观标示摩擦力箭头的起点位置。
2. 预览态仍保持无标识（沿用 v6 规则），圆点与箭头一同在开始后显示。

### 修订 4（2026-09-13 第二批，用户确认）：共速后 A-B 摩擦标记消失
1. **物理口径**：μ₂>0 且 μ₁m_A > μ₂(m_A+m_B) 时，共速后进入整体分析，
   物块与板块之间的摩擦力（一对静摩擦）**不再显示**——
   物块红色 f 箭头与板块黄色 f 箭头（A-B 间）在共速锁定后都消失；
   板块受到的地面摩擦箭头（黄，向左）保留，体现"一起减速"的原因。
2. **数据面板**：共速锁定阶段"物块摩擦力 f"显示 **0.00**（与画面口径一致）；
   滑出后仍显示 μ₂m_A·g（地面摩擦）。

### 修订 5（2026-09-13 第二批，用户确认）：v₀B 默认值改回 0
1. **根因**：用户报告的两个 μ₂>0 异常（"板块闪现运动轨迹/动了一下"、
   "瞬间出现大小等于最大静摩擦的向左线段"）系 v₀B 默认值 2 m/s 所致——
   板块带初速先减速滑行一小段（动摩擦箭头 μ₂(m_A+m_B)g 恰为最大静摩擦数值），
   与教学场景"板块一开始就保持静止"不符。
2. **修正**：v₀B 默认值 **2 → 0**（滑条仍可调 −10～+10）。
   v₀B=0 时：μ₁m_A ≤ μ₂(m_A+m_B) 情形下板块从第一帧起保持静止、全程无位移；
   板块受地面**静摩擦力**，大小 = 物块滑动摩擦力 μ₁m_A·g、方向向左，
   黄色箭头与物块红色箭头等长反向、持续正确显示。
3. **条款修订**：v6"默认 A、B 均向右运动"修订为
   "默认物块向右运动（v₀A=12），板块默认静止（v₀B=0）"。
